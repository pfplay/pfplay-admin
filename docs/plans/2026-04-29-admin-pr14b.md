# PR 14b Implementation Plan — 회원/파티룸 read-only (목록 + 상세 + 필터/정렬/페이징)

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Spec:** `docs/specs/2026-04-29-admin-pr14b-design.md`

**Goal:** 어드민이 가입 회원과 파티룸을 list/detail로 read-only 조회하고, 백엔드가 노출하는 모든 필터/정렬/페이징을 URL search params 기반으로 ad-hoc 조작할 수 있게 한다. 14a 인프라 위에 frontend-only로 올린다(backend 변경 0).

**Architecture:** Feature-Sliced Design 그대로 이어감. 도메인 슬라이스 `entities/{member,partyroom}` + 시나리오 슬라이스 `features/{members,partyrooms}`. 모든 HTTP는 14a `shared/api/http.ts` 단일 진입점. URL search params가 filter의 single source of truth — `useSearchParams` ↔ zod schema 양방향 동기화. react-query가 cache + retry + keepPreviousData 책임.

**Tech Stack:** React 18 + Vite 6 + TypeScript 5 (strict) + react-router-dom 7 + zustand 5 + **@tanstack/react-query 5** (신규) + react-hook-form + zod + tailwindcss 4 + radix-ui (shadcn) + sonner. 테스트: vitest 2 + @testing-library/react 16 + jsdom 25 + msw 2 (모두 14a에서 도입됨).

**Package Manager:** pnpm (lockfile `pnpm-lock.yaml`).

**Branch:** `feature/admin-platform-frontend` (PR 14a~14d 장기 통합 브랜치, 14a HEAD `bf7750f` 기준).

**Working dir:** `C:/Users/Eisen/Desktop/Labs/[projects] pfplay/pfplay-admin`.

**JDK 환경**: 백엔드는 다른 레포(`pfplay-platform`). 본 PR은 frontend only — JDK 무관.

---

## 0. Scope Check

PR 14b는 단일 sub-PR로 spec §1.1 목적만 다룸 (회원/파티룸 read-only 목록 + 상세 + 필터). 분리 가능한 독립 sub-system 없음 — 멤버/파티룸은 같은 어드민 read-only 도메인 군집 (테이블/필터/페이징 패턴 공유, FSD 슬라이스 컨벤션 동일). 단일 plan으로 진행. mutation·guest·avatar·신고는 14c/14d 또는 별 묶음.

## 1. File Structure

### 1.1 신규 파일

| 경로 | 종류 | 책임 |
|---|---|---|
| `package.json` | 수정 | `@tanstack/react-query`, `@tanstack/react-query-devtools`, `date-fns`(미설치 시), shadcn 누락분 추가 |
| `src/main.tsx` | 수정 | `QueryClientProvider` wrapping |
| `src/App.tsx` | 수정 | `/users`, `/rooms` 제거 + `/members`, `/members/:memberId`, `/partyrooms`, `/partyrooms/:partyroomId` 추가 |
| `src/app/layout.tsx` | 수정 | nav 4개 (대시보드/회원/파티룸/시나리오) — "가상 유저"→"회원" relabel + 경로/disabled 변경 |
| `src/shared/api/page.ts` | 신규 | `Page<T>`, `ApiCommonResponse<T>`, `unwrap()` |
| `src/shared/api/__tests__/page.test.ts` | 신규 | unit (unwrap, type guards) |
| `src/shared/lib/url-state.ts` | 신규 | `parseSearchParams(schema, params)`, `stripInvalidParams(params, error)`, `serializeQuery(query)` |
| `src/shared/lib/__tests__/url-state.test.ts` | 신규 | unit |
| `src/shared/lib/query-client.ts` | 신규 | `QueryClient` 인스턴스 (defaults: `refetchOnWindowFocus: false`, `staleTime: 30_000`, retry predicate) |
| `src/test/mocks/handlers/index.ts` | 신규 | spread 통합 (기존 `handlers.ts`에서 분할) |
| `src/test/mocks/handlers/auth.ts` | 신규 | 14a login/logout/changePassword 핸들러 이전 |
| `src/test/mocks/handlers/members.ts` | 신규 | `/admin/members` GET list/detail 핸들러 + fixture |
| `src/test/mocks/handlers/partyrooms.ts` | 신규 | `/admin/partyrooms` GET list/detail 핸들러 + fixture |
| `src/test/mocks/fixtures/members.ts` | 신규 | `AdminMemberSummary` / `AdminMemberDetail` 샘플 |
| `src/test/mocks/fixtures/partyrooms.ts` | 신규 | `AdminPartyroomListItem` / `AdminPartyroomDetail` 샘플 |
| `src/entities/member/model/types.ts` | 신규 | `AdminMemberSummary`, `AdminMemberDetail`, `UserAccountSummary`, `MemberProfileSummary`, `RecentActivityLogItem`, `ProviderType`, `AuthorityTier` |
| `src/entities/member/index.ts` | 신규 | barrel |
| `src/entities/partyroom/model/types.ts` | 신규 | `AdminPartyroomListItem`, `AdminPartyroomDetail` + sub records (`PlaybackSummary`, `CrewSummary`, `DjSummary`, `PenaltySummary`, `ReportSummary`, `AdminActionSummary`) + enums (`PartyroomStatus`, `StageType`) |
| `src/entities/partyroom/index.ts` | 신규 | barrel |
| `src/features/members/model/filter-schema.ts` | 신규 | `membersListQuerySchema` zod + `MembersListQuery` type |
| `src/features/members/model/__tests__/filter-schema.test.ts` | 신규 | unit |
| `src/features/members/api/members-api.ts` | 신규 | `listMembers(q)`, `getMemberDetail(id)` |
| `src/features/members/api/__tests__/members-api.test.ts` | 신규 | unit |
| `src/features/members/api/use-members-list.ts` | 신규 | `useQuery` hook |
| `src/features/members/api/use-member-detail.ts` | 신규 | `useQuery` hook |
| `src/features/members/ui/members-filter-form.tsx` | 신규 | text/dropdown/date range/sort form |
| `src/features/members/ui/members-table.tsx` | 신규 | 테이블 + row click → navigate |
| `src/features/members/ui/__tests__/members-filter-form.test.tsx` | 신규 | component |
| `src/features/members/ui/__tests__/members-table.test.tsx` | 신규 | component |
| `src/features/members/ui/member-detail-cards.tsx` | 신규 | 5 카드 (header/UserAccount/Profile/Tier/ActivityLog) — 별 카드 컴포넌트로 재export 가능하지만 14b는 단일 파일 묶음 |
| `src/features/members/ui/__tests__/member-detail-cards.test.tsx` | 신규 | component |
| `src/features/partyrooms/model/filter-schema.ts` | 신규 | `partyroomsListQuerySchema` + `PartyroomSortEnum` |
| `src/features/partyrooms/model/__tests__/filter-schema.test.ts` | 신규 | unit |
| `src/features/partyrooms/api/partyrooms-api.ts` | 신규 | `listPartyrooms(q)`, `getPartyroomDetail(id)` |
| `src/features/partyrooms/api/__tests__/partyrooms-api.test.ts` | 신규 | unit |
| `src/features/partyrooms/api/use-partyrooms-list.ts` | 신규 | `useQuery` |
| `src/features/partyrooms/api/use-partyroom-detail.ts` | 신규 | `useQuery` |
| `src/features/partyrooms/ui/partyrooms-filter-form.tsx` | 신규 | filter form |
| `src/features/partyrooms/ui/partyrooms-table.tsx` | 신규 | table |
| `src/features/partyrooms/ui/__tests__/partyrooms-filter-form.test.tsx` | 신규 | component |
| `src/features/partyrooms/ui/__tests__/partyrooms-table.test.tsx` | 신규 | component |
| `src/features/partyrooms/ui/partyroom-detail-cards.tsx` | 신규 | 8 카드 (header/meta/playback/crews/dj/penalties/reports/admin-actions) — 단일 파일 묶음 |
| `src/features/partyrooms/ui/__tests__/partyroom-detail-cards.test.tsx` | 신규 | component |
| `src/widgets/members-list.tsx` | 신규 | filter-form + table + pagination 합성 |
| `src/widgets/members-detail.tsx` | 신규 | header + 5 카드 합성 |
| `src/widgets/partyrooms-list.tsx` | 신규 | filter-form + table + pagination 합성 |
| `src/widgets/partyrooms-detail.tsx` | 신규 | header + 8 카드 합성 |
| `src/widgets/pagination.tsx` | 신규 | shared pagination 컴포넌트 (members/partyrooms 양쪽 사용) |
| `src/widgets/__tests__/pagination.test.tsx` | 신규 | component |
| `src/pages/members-page.tsx` | 신규 | URL parse + widget 호스트 |
| `src/pages/__tests__/members-page.test.tsx` | 신규 | integration |
| `src/pages/member-detail-page.tsx` | 신규 | URL params + widget |
| `src/pages/__tests__/member-detail-page.test.tsx` | 신규 | integration |
| `src/pages/partyrooms-page.tsx` | 신규 | URL parse + widget |
| `src/pages/__tests__/partyrooms-page.test.tsx` | 신규 | integration |
| `src/pages/partyroom-detail-page.tsx` | 신규 | URL params + widget |
| `src/pages/__tests__/partyroom-detail-page.test.tsx` | 신규 | integration |
| `src/app/__tests__/layout.test.tsx` | 신규 | sidebar nav (회원/파티룸 enabled, /users·/rooms 제거) — 단일 케이스 |

### 1.2 삭제 파일 (G1)

- `src/entities/user/` (디렉토리 통째)
- `src/entities/room/` (디렉토리 통째)
- `src/features/users/` (디렉토리 통째)
- `src/features/rooms/` (디렉토리 통째)
- `src/widgets/users/` (디렉토리 통째)
- `src/widgets/rooms/` (디렉토리 통째)
- `src/pages/users-page.tsx`
- `src/pages/rooms-page.tsx`
- `src/test/mocks/handlers.ts` (G1에서 `handlers/` 디렉토리로 분할 마이그레이션 후 삭제)
- `src/shared/lib/api-client.ts` (사용처 0이면 삭제, 잔존 시 보존 — G1에서 grep으로 확정)

### 1.3 수정 파일

| 경로 | 변경 |
|---|---|
| `src/main.tsx` | `QueryClientProvider` wrapping + react-query devtools (dev) |
| `src/App.tsx` | `/users`, `/rooms` 라우트 제거 + 신규 4 라우트 추가 |
| `src/app/layout.tsx` | nav 항목 변경 (가상 유저 → 회원, 파티룸 enable) |
| `src/test/setup.ts` | (변경 없음 — handlers import 경로만 자동 갱신될 수 있음) |
| `package.json` | dep 추가 |

### 1.4 FSD 슬라이스 경계

shared(인프라) → entities(도메인 데이터+상태) → features(유저 시나리오) → widgets(컴포넌트 합성) → pages(라우트 호스트). 상위 → 하위 import만 허용 (역방향 금지).

### 1.5 테스트 위치

모듈 옆 `__tests__/` co-located. `*.test.ts(x)` 패턴.

---

## Chunk 1: G1 — 인프라 + demo 정리 + 사이드바/라우트/타입

**목표:** 14b 본 작업(G2~G8) 기반이 되는 인프라 일괄 셋업. demo 슬라이스 삭제, react-query 설치, 라우트/사이드바 변경, `entities/{member,partyroom}` 타입, `shared/api/page.ts`, `shared/lib/url-state.ts`, `shared/lib/query-client.ts`, msw 핸들러 분할, `QueryClientProvider` wiring.

### Task 1.1: 의존성 verify + 누락 shadcn 설치

**Files:** Modify `package.json`, `pnpm-lock.yaml` (자동, 누락분 install 시).

> **Reality check (G0.3 polish 시 확인됨):** `@tanstack/react-query@5.62.12` + `date-fns@4.1.0`는 PR-14a에서 이미 설치됨. **추가 install 불필요.** `@tanstack/react-query-devtools`만 누락 가능 (확인 후 install). shadcn 미설치 컴포넌트 = `skeleton`, `popover`, `calendar` 3개.

- [ ] **Step 1: 디렉토리 + 기존 deps 확인**

```bash
grep -E "@tanstack/react-query|date-fns|react-query-devtools" package.json
```

Expected: `react-query` + `date-fns` 존재, `react-query-devtools`는 없을 가능성.

- [ ] **Step 2: react-query-devtools만 설치 (이미 있으면 skip)**

```bash
pnpm list @tanstack/react-query-devtools 2>/dev/null || pnpm add -D @tanstack/react-query-devtools
```

Expected: 미설치였으면 추가, 이미 있으면 skip.

- [ ] **Step 3: shadcn 미설치 컴포넌트 확인**

```bash
ls src/components/ui/
```

Expected: `table`, `select`, `input`, `button`, `card`, `badge`, `tabs`, `textarea`, `checkbox`, `label` 등 보임. **누락**: `skeleton`, `popover`, `calendar`.

- [ ] **Step 4: 누락 shadcn 컴포넌트 설치**

```bash
pnpm dlx shadcn@latest add skeleton popover calendar
```

Expected: 3 컴포넌트 추가. (table은 이미 있으므로 install 명령에서 제외.)

- [ ] **Step 5: 빌드 sanity**

```bash
pnpm exec tsc --noEmit
```

Expected: 0 error.

### Task 1.2: demo `api-client.ts` 사용처 확정 (G0.3 polish에서 grep 완료)

**Files:** Read only — Task 1.4 demo 삭제 범위 결정용.

> **Reality (G0.3 grep 결과):** `src/shared/lib/api-client.ts` 의 사용처 5건은 모두 demo subsystem 안 (`widgets/scenarios/`, `features/scenarios/` 2개 panel, `entities/room/model/store.ts`, `shared/lib/index.ts`). Task 1.4에서 demo subsystem 일괄 삭제 시 자연스럽게 사용처 0 — `api-client.ts`도 같이 삭제. **별 결정 분기 없음.**

- [ ] **Step 1: 재확인 grep (안전 점검)**

```bash
grep -rn "from \"@/shared/lib/api-client\"\|from \"../../shared/lib/api-client\"\|from \"../shared/lib/api-client\"\|from \"./api-client\"" src/
```

Expected: 5 사용처 모두 demo subsystem 안 — Task 1.4 삭제 범위에 포함됨.

### Task 1.3: msw 핸들러 디렉토리 분할

**Files:** Create `src/test/mocks/handlers/{auth,members,partyrooms,index}.ts`, `src/test/mocks/fixtures/{members,partyrooms}.ts`. Delete `src/test/mocks/handlers.ts`.

> **Ordering note:** 이 task의 fixtures (Step 3, 4)는 `@/entities/{member,partyroom}` 타입을 import한다. 따라서 **Task 1.6 (entities types) 을 먼저 실행 후 Task 1.3의 Step 3 이후로 진행**한다. 권장 실행 순서: Task 1.3 Step 1, 2 → Task 1.6 전체 → Task 1.3 Step 3~9. handlers/auth.ts 골격 (Step 2)은 entities 타입 의존 없음.

- [ ] **Step 1: 기존 `handlers.ts` 내용 확인**

```bash
cat src/test/mocks/handlers.ts
```

Expected: 14a auth 핸들러들 (login/logout/changePassword).

- [ ] **Step 2: `src/test/mocks/handlers/auth.ts` 생성 (기존 내용 이전)**

기존 `handlers.ts` 의 login/logout/changePassword 핸들러를 export 한다:

```ts
import { http, HttpResponse } from "msw"

export const authHandlers = [
  http.post("*/api/v1/auth/admin/login", async ({ request }) => {
    // 14a 기존 핸들러 본문 그대로 이전
    ...
  }),
  // logout, changePassword 동일
]
```

- [ ] **Step 3: `src/test/mocks/fixtures/members.ts` 생성**

```ts
import type { AdminMemberSummary, AdminMemberDetail } from "@/entities/member"

export const memberSummaryFixture: AdminMemberSummary = {
  memberId: 1,
  userAccountId: 100,
  email: "alice@example.com",
  providerType: "GOOGLE",
  nickname: "alice",
  authorityTier: "AM",
  lastLoginAt: "2026-04-28T10:00:00",
  createdAt: "2026-01-15T09:00:00",
  withdrawn: false,
  withdrawnAt: null,
}

export const memberSummaryWithdrawnFixture: AdminMemberSummary = {
  ...memberSummaryFixture,
  memberId: 2,
  email: "bob@example.com",
  nickname: "bob",
  withdrawn: true,
  withdrawnAt: "2026-04-20T12:00:00",
}

export const memberDetailFixture: AdminMemberDetail = {
  memberId: 1,
  userAccount: {
    userAccountId: 100,
    email: "alice@example.com",
    providerType: "GOOGLE",
    createdAt: "2026-01-15T09:00:00",
  },
  profile: {
    nickname: "alice",
    introduction: "안녕하세요",
  },
  authorityTier: "AM",
  createdAt: "2026-01-15T09:00:00",
  recentActivityLog: [
    { occurredAt: "2026-04-28T10:00:00", type: "LOGIN", summary: "로그인" },
  ],
}
```

(타입은 G1 Task 1.6에서 정의 — fixture는 타입 import만 두고 G1 task 순서는 types 먼저 → fixture 후. 본 step은 types 정의 후 작성.)

- [ ] **Step 4: `src/test/mocks/fixtures/partyrooms.ts` 생성 (동일 패턴)**

```ts
import type { AdminPartyroomListItem, AdminPartyroomDetail } from "@/entities/partyroom"

export const partyroomListItemFixture: AdminPartyroomListItem = {
  partyroomId: 1,
  title: "테스트 파티룸",
  stageType: "GENERAL",
  hostUserAccountId: 100,
  hostNickname: "alice",
  crewCount: 5,
  djCount: 2,
  playbackActivated: true,
  status: "ACTIVE",
  displayFlag: "PUBLIC",  // 실제 enum 값은 G7에서 확정
  createdAt: "2026-04-25T10:00:00",
  lastActivityAt: "2026-04-28T11:00:00",
}

export const partyroomDetailFixture: AdminPartyroomDetail = {
  partyroomId: 1,
  title: "테스트 파티룸",
  status: "ACTIVE",
  displayFlag: "PUBLIC",
  hostUserAccountId: 100,
  hostNickname: "alice",
  hostEmail: "alice@example.com",
  crewCount: 5,
  lastActivityAt: "2026-04-28T11:00:00",
  stageType: "GENERAL",
  playback: { activated: true, currentTrackName: null, currentDjCrewId: 10 },
  crews: [
    { crewId: 10, memberId: 1, gradeType: "DJ", nickname: "alice", enteredAt: "2026-04-25T10:00:00" },
  ],
  djQueue: [
    { djId: 5, crewId: 10, playlistName: null, orderNumber: 1 },
  ],
  recentPenalties: [],
  recentReports: [],
  recentAdminActions: [],
}
```

- [ ] **Step 5: `src/test/mocks/handlers/members.ts` 생성**

```ts
import { http, HttpResponse } from "msw"
import { memberSummaryFixture, memberSummaryWithdrawnFixture, memberDetailFixture } from "../fixtures/members"

const API = "*/api/v1/admin/members"

export const memberHandlers = [
  // list happy path
  http.get(API, ({ request }) => {
    const url = new URL(request.url)
    const tier = url.searchParams.get("tier")
    const content = tier === "FM" ? [] : [memberSummaryFixture, memberSummaryWithdrawnFixture]
    return HttpResponse.json({
      data: {
        content,
        totalElements: content.length,
        totalPages: content.length === 0 ? 0 : 1,
        number: 0,
        size: 50,
        first: true,
        last: true,
        empty: content.length === 0,
        numberOfElements: content.length,
      },
    })
  }),

  // detail happy path
  http.get(`${API}/:memberId`, ({ params }) => {
    if (params.memberId === "9999") {
      return HttpResponse.json(
        { status: 404, errorCode: "MEMBER_NOT_FOUND", message: "존재하지 않는 회원" },
        { status: 404 },
      )
    }
    return HttpResponse.json({ data: memberDetailFixture })
  }),
]
```

- [ ] **Step 6: `src/test/mocks/handlers/partyrooms.ts` 생성 (동일 패턴, raw page — wrap 없음)**

```ts
import { http, HttpResponse } from "msw"
import { partyroomListItemFixture, partyroomDetailFixture } from "../fixtures/partyrooms"

const API = "*/api/v1/admin/partyrooms"

export const partyroomHandlers = [
  http.get(API, ({ request }) => {
    // backend 400 (sort whitelist 위반) 분기는 zod (PartyroomSortEnum)에서 미리 차단되어 dead code — 추가 안 함.
    // backend 400 propagation 검증은 G5 Task 5.2 unit test (api 레벨 type-cast invalid sort)로 처리.
    const content = [partyroomListItemFixture]
    return HttpResponse.json({
      content,
      totalElements: content.length,
      totalPages: 1,
      number: 0,
      size: 50,
      first: true,
      last: true,
      empty: false,
      numberOfElements: content.length,
    })
  }),

  http.get(`${API}/:partyroomId`, ({ params }) => {
    if (params.partyroomId === "9999") {
      return HttpResponse.json(
        { status: 404, errorCode: "NOT_FOUND_ROOM", message: "존재하지 않는 룸" },
        { status: 404 },
      )
    }
    return HttpResponse.json(partyroomDetailFixture)
  }),
]
```

- [ ] **Step 7: `src/test/mocks/handlers/index.ts` 생성**

```ts
import { authHandlers } from "./auth"
import { memberHandlers } from "./members"
import { partyroomHandlers } from "./partyrooms"

export const handlers = [...authHandlers, ...memberHandlers, ...partyroomHandlers]
```

- [ ] **Step 8: 기존 `src/test/mocks/handlers.ts` 삭제 + `server.ts` import 경로 업데이트**

```bash
rm src/test/mocks/handlers.ts
```

`src/test/mocks/server.ts` 수정 (import 경로):

```ts
import { setupServer } from "msw/node"
import { handlers } from "./handlers"  // 기존 "./handlers" 경로가 디렉토리/index 자동 해결

export const server = setupServer(...handlers)
```

- [ ] **Step 9: 14a 회귀 테스트 통과 확인**

```bash
pnpm test:run
```

Expected: 14a 16/16 PASS 그대로.

### Task 1.4: demo subsystem 일괄 삭제 + 라우트/사이드바 정리

**Files (삭제):** `src/entities/{user,room,scenario,playlist,dj-queue}/`, `src/features/{users,rooms,scenarios}/`, `src/widgets/{users,rooms,scenarios}/`, `src/pages/{users,rooms,scenarios}-page.tsx`, `src/shared/lib/api-client.ts`. **Modify:** `src/App.tsx` (라우트 3개 제거 + 신규 4 placeholder 라우트는 Task 1.10에서). `src/app/layout.tsx` (Task 1.10).

> **Reality (G0.3 grep):** demo 18 파일 모두 demo subsystem 안에서만 import (외부 의존 없음). 즉 통째 삭제 시 신규 슬라이스(`entities/{member,partyroom}` Task 1.6)와 충돌 없음. **demo는 다시 필요해지면 backend endpoint 신설 + 새 slice 재구축으로 처리. 현재 코드 보존 ❌.**

- [ ] **Step 1: 디렉토리/파일 일괄 삭제**

```bash
rm -rf src/entities/user src/entities/room src/entities/scenario src/entities/playlist src/entities/dj-queue
rm -rf src/features/users src/features/rooms src/features/scenarios
rm -rf src/widgets/users src/widgets/rooms src/widgets/scenarios
rm src/pages/users-page.tsx src/pages/rooms-page.tsx src/pages/scenarios-page.tsx
rm src/shared/lib/api-client.ts
```

Expected: 11 디렉토리 + 4 파일 (3 pages + api-client) 제거.

- [ ] **Step 2: `src/shared/lib/index.ts` 정리 (api-client re-export 잔존 시)**

`src/shared/lib/index.ts` 안에서 `api-client` re-export 라인 제거. 비어 있으면 파일 자체도 삭제.

- [ ] **Step 3: `src/App.tsx` 수정 — `/users`, `/rooms`, `/scenarios` 라우트 + import 모두 제거**

기존 라우트 3개 (`<Route path="users" ...>`, `<Route path="rooms" ...>`, `<Route path="scenarios" ...>`) 제거. import 라인 (`UsersPage`, `RoomsPage`, `ScenariosPage`) 제거. (신규 `/members`, `/partyrooms` 라우트 + placeholder 페이지는 Task 1.10에서 추가.)

- [ ] **Step 4: tsc — 잔존 demo import 확인**

```bash
pnpm exec tsc --noEmit
```

Expected: 0 error. 만약 demo import 잔존 (예: `app/layout.tsx`가 demo asset import) 발견 시 모두 제거.

- [ ] **Step 5: 14a 회귀 테스트**

```bash
pnpm test:run
```

Expected: 14a 16/16 PASS (14a는 demo 슬라이스에 의존하는 테스트 없음).

### Task 1.5: `src/shared/api/page.ts` + 테스트

**Files:** Create `src/shared/api/page.ts`, `src/shared/api/__tests__/page.test.ts`.

- [ ] **Step 1: 테스트 작성 (RED)**

`src/shared/api/__tests__/page.test.ts`:

```ts
import { describe, it, expect } from "vitest"
import { unwrap } from "../page"
import type { ApiCommonResponse, Page } from "../page"

describe("unwrap", () => {
  it("ApiCommonResponse.data를 그대로 반환한다", () => {
    const res: ApiCommonResponse<{ value: number }> = { data: { value: 42 } }
    expect(unwrap(res)).toEqual({ value: 42 })
  })

  it("Page<T> 형태의 data를 그대로 반환한다", () => {
    const res: ApiCommonResponse<Page<string>> = {
      data: {
        content: ["a"], totalElements: 1, totalPages: 1, number: 0, size: 50,
        first: true, last: true, empty: false, numberOfElements: 1,
      },
    }
    expect(unwrap(res).content).toEqual(["a"])
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pnpm exec vitest run src/shared/api/__tests__/page.test.ts
```

Expected: FAIL (`page` 모듈 없음).

- [ ] **Step 3: 구현 (GREEN)**

`src/shared/api/page.ts`:

```ts
export interface Page<T> {
  content: T[]
  totalElements: number
  totalPages: number
  number: number      // 0-based page index
  size: number
  first: boolean
  last: boolean
  empty: boolean
  numberOfElements: number
}

export interface ApiCommonResponse<T> {
  data: T
}

export const unwrap = <T>(res: ApiCommonResponse<T>): T => res.data
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pnpm exec vitest run src/shared/api/__tests__/page.test.ts
```

Expected: 2/2 PASS.

### Task 1.6: `entities/member` + `entities/partyroom` 타입 정의

**Files:** Create `src/entities/member/{model/types.ts,index.ts}`, `src/entities/partyroom/{model/types.ts,index.ts}`.

- [ ] **Step 1: `src/entities/member/model/types.ts` 작성**

```ts
export type ProviderType = "GOOGLE" | "TWITTER" | "LOCAL"
export type AuthorityTier = "FM" | "AM" | "GT"
// FM = FULL_MEMBER (가입 + wallet 등록), AM = ASSOCIATE_MEMBER (가입 default),
// GT = GUEST (별 테이블이 정상 경로 — member 테이블에는 admin 강등 edge case에만 등장)

export interface AdminMemberSummary {
  memberId: number
  userAccountId: number
  email: string
  providerType: ProviderType
  nickname: string | null
  authorityTier: AuthorityTier
  lastLoginAt: string | null  // LocalDateTime ISO string
  createdAt: string
  withdrawn: boolean
  withdrawnAt: string | null
}

export interface UserAccountSummary {
  userAccountId: number
  email: string
  providerType: ProviderType
  createdAt: string
}

export interface MemberProfileSummary {
  nickname: string | null
  introduction: string | null
}

export interface RecentActivityLogItem {
  occurredAt: string
  type: string
  summary: string
}

export interface AdminMemberDetail {
  memberId: number
  userAccount: UserAccountSummary
  profile: MemberProfileSummary
  authorityTier: AuthorityTier
  createdAt: string
  recentActivityLog: RecentActivityLogItem[]
}
```

- [ ] **Step 2: `src/entities/member/index.ts` (barrel)**

```ts
export type {
  AdminMemberSummary,
  AdminMemberDetail,
  UserAccountSummary,
  MemberProfileSummary,
  RecentActivityLogItem,
  ProviderType,
  AuthorityTier,
} from "./model/types"
```

- [ ] **Step 3: `src/entities/partyroom/model/types.ts` 작성**

```ts
export type PartyroomStatus = "ACTIVE" | "SUSPENDED" | "TERMINATED"
export type StageType = "MAIN" | "GENERAL"

// 다음 enum은 14b detail 뱃지/section 라벨 용도. 실제 값은 G7/G8에서 backend 확정 후 union 좁히기 (§12 catch-up 항목 7).
// 안전을 위해 string으로 두고 렌더 단에서 unknown fallback ("미정의: XYZ").
export type DisplayFlag = string
export type GradeType = string
export type PenaltyType = string
export type PartyroomAdminActionType = string

export interface PlaybackSummary {
  activated: boolean
  currentTrackName: string | null
  currentDjCrewId: number | null
}

export interface CrewSummary {
  crewId: number
  memberId: number
  gradeType: GradeType
  nickname: string | null
  enteredAt: string
}

export interface DjSummary {
  djId: number
  crewId: number
  playlistName: string | null
  orderNumber: number
}

export interface PenaltySummary {
  id: number
  crewId: number
  penaltyType: PenaltyType
  punisherType: string
  reason: string
  date: string
}

export interface ReportSummary {
  id: number
  category: string
  status: string
  reporterUserAccountId: number
  createdAt: string
}

export interface AdminActionSummary {
  actionId: number
  actionType: PartyroomAdminActionType
  administratorId: number
  occurredAt: string
}

export interface AdminPartyroomListItem {
  partyroomId: number
  title: string
  stageType: StageType
  hostUserAccountId: number
  hostNickname: string | null
  crewCount: number
  djCount: number
  playbackActivated: boolean
  status: PartyroomStatus
  displayFlag: DisplayFlag
  createdAt: string
  lastActivityAt: string | null
}

export interface AdminPartyroomDetail {
  partyroomId: number
  title: string
  status: PartyroomStatus
  displayFlag: DisplayFlag
  hostUserAccountId: number
  hostNickname: string | null
  hostEmail: string | null
  crewCount: number
  lastActivityAt: string | null
  stageType: StageType
  playback: PlaybackSummary
  crews: CrewSummary[]
  djQueue: DjSummary[]
  recentPenalties: PenaltySummary[]
  recentReports: ReportSummary[]
  recentAdminActions: AdminActionSummary[]
}
```

- [ ] **Step 4: `src/entities/partyroom/index.ts` (barrel)**

```ts
export type {
  AdminPartyroomListItem,
  AdminPartyroomDetail,
  PlaybackSummary,
  CrewSummary,
  DjSummary,
  PenaltySummary,
  ReportSummary,
  AdminActionSummary,
  PartyroomStatus,
  StageType,
  DisplayFlag,
  GradeType,
  PenaltyType,
  PartyroomAdminActionType,
} from "./model/types"
```

- [ ] **Step 5: tsc**

```bash
pnpm exec tsc --noEmit
```

Expected: 0 error.

- [ ] **Step 6: Task 1.3 fixture 파일 (members/partyrooms) 컴파일 OK 확인**

```bash
pnpm exec vitest run src/test/mocks/fixtures
```

Expected: fixture 자체는 직접 테스트 안 하지만 import 시 컴파일 에러 없음.

### Task 1.7: `src/shared/lib/url-state.ts` + 테스트

**Files:** Create `src/shared/lib/url-state.ts`, `src/shared/lib/__tests__/url-state.test.ts`.

- [ ] **Step 1: 테스트 작성 (RED)**

```ts
import { describe, it, expect } from "vitest"
import { z } from "zod"
import { parseSearchParams, stripInvalidParams, serializeQuery } from "../url-state"

const schema = z.object({
  email: z.string().max(5).optional(),
  page: z.coerce.number().int().min(0).default(0),
  sort: z.enum(["asc", "desc"]).default("desc"),
})

describe("parseSearchParams", () => {
  it("URLSearchParams를 zod로 parse 한다 (default 적용)", () => {
    const r = parseSearchParams(schema, new URLSearchParams("email=a"))
    expect(r.success).toBe(true)
    if (r.success) {
      expect(r.data).toEqual({ email: "a", page: 0, sort: "desc" })
    }
  })

  it("invalid 값은 success=false", () => {
    const r = parseSearchParams(schema, new URLSearchParams("email=toolongstring&sort=foo"))
    expect(r.success).toBe(false)
  })
})

describe("stripInvalidParams", () => {
  it("invalid 필드만 drop한 새 URLSearchParams를 반환한다", () => {
    const params = new URLSearchParams("email=toolongstring&page=2&sort=foo")
    const r = parseSearchParams(schema, params)
    expect(r.success).toBe(false)
    if (!r.success) {
      const cleaned = stripInvalidParams(params, r.error)
      expect(cleaned.get("email")).toBeNull()
      expect(cleaned.get("page")).toBe("2")
      expect(cleaned.get("sort")).toBeNull()
    }
  })
})

describe("serializeQuery", () => {
  it("undefined/빈 문자열은 drop, 나머지는 string화", () => {
    const out = serializeQuery({ email: "a", tier: undefined, page: 0, sort: "desc" })
    expect(out.toString()).toBe("email=a&page=0&sort=desc")
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pnpm exec vitest run src/shared/lib/__tests__/url-state.test.ts
```

Expected: FAIL.

- [ ] **Step 3: 구현 (GREEN)**

```ts
import type { ZodError, ZodTypeAny } from "zod"

export function parseSearchParams<T extends ZodTypeAny>(
  schema: T,
  params: URLSearchParams,
): ReturnType<T["safeParse"]> {
  const obj = Object.fromEntries(params.entries())
  return schema.safeParse(obj) as ReturnType<T["safeParse"]>
}

export function stripInvalidParams(
  params: URLSearchParams,
  error: ZodError,
): URLSearchParams {
  const invalidKeys = new Set<string>()
  for (const issue of error.issues) {
    const key = issue.path[0]
    if (typeof key === "string") invalidKeys.add(key)
  }
  const cleaned = new URLSearchParams(params)
  for (const k of invalidKeys) cleaned.delete(k)
  return cleaned
}

export function serializeQuery(query: Record<string, unknown>): URLSearchParams {
  const out = new URLSearchParams()
  for (const [k, v] of Object.entries(query)) {
    if (v === undefined || v === null) continue
    const s = String(v)
    if (s === "") continue
    out.set(k, s)
  }
  return out
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pnpm exec vitest run src/shared/lib/__tests__/url-state.test.ts
```

Expected: 4/4 PASS.

### Task 1.8: `src/shared/lib/query-client.ts`

**Files:** Create `src/shared/lib/query-client.ts`.

- [ ] **Step 1: 작성**

```ts
import { QueryClient } from "@tanstack/react-query"
import { ApiError } from "@/shared/api/error"

export function createQueryClient(): QueryClient {
  return new QueryClient({
    defaultOptions: {
      queries: {
        refetchOnWindowFocus: false,
        staleTime: 30_000,
        retry: (failureCount, error) => {
          if (error instanceof ApiError) return false
          return failureCount < 1
        },
      },
    },
  })
}
```

- [ ] **Step 2: tsc**

```bash
pnpm exec tsc --noEmit
```

Expected: 0 error.

### Task 1.9: `src/main.tsx` — `QueryClientProvider` wrapping

**Files:** Modify `src/main.tsx`.

- [ ] **Step 1: 기존 main.tsx 확인**

```bash
cat src/main.tsx
```

> **Reality (G0.3 polish):** 기존 `src/main.tsx`는 이미 `QueryClientProvider`로 wrapping하고 `<Toaster position="top-right" richColors />`도 렌더한다. 인라인으로 정의된 `queryClient`만 `createQueryClient()` 팩토리로 교체하고 `ReactQueryDevtools`를 dev 환경에서만 추가하는 **targeted edit**으로 처리. **Toaster 누락 절대 금지** (sonner toast 회귀 위험).

- [ ] **Step 2: targeted edit — 인라인 queryClient → factory + devtools 추가**

기존:
```tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
...
const queryClient = new QueryClient({
  defaultOptions: { queries: { staleTime: 1000 * 60 * 5, refetchOnWindowFocus: false } },
})
```

다음으로 변경:
```tsx
import { QueryClientProvider } from "@tanstack/react-query"
import { ReactQueryDevtools } from "@tanstack/react-query-devtools"
import { createQueryClient } from "./shared/lib/query-client"
...
const queryClient = createQueryClient()
```

`<QueryClientProvider>` 안의 children block에 devtools 추가 (Toaster, BrowserRouter, App은 그대로):
```tsx
<QueryClientProvider client={queryClient}>
  <BrowserRouter>
    <App />
    <Toaster position="top-right" richColors />
  </BrowserRouter>
  {import.meta.env.DEV && <ReactQueryDevtools initialIsOpen={false} />}
</QueryClientProvider>
```

- [ ] **Step 3: 빌드 sanity + Toaster 살아있는지 확인**

```bash
pnpm build
grep -n "Toaster" src/main.tsx
```

Expected: 빌드 성공 + `<Toaster position="top-right" richColors />` 보존됨.

### Task 1.10: 사이드바 + App.tsx 라우트 변경

**Files:** Modify `src/app/layout.tsx`, `src/App.tsx`.

- [ ] **Step 1: `app/layout.tsx` nav 수정 (4 → 3 nav, 시나리오 메뉴 제거)**

기존 `navItems` 배열:
```tsx
const navItems = [
  { to: "/", icon: LayoutDashboard, label: "대시보드", disabled: false },
  { to: "/users", icon: Users, label: "가상 유저", disabled: true },
  { to: "/rooms", icon: DoorOpen, label: "파티룸", disabled: true },
  { to: "/scenarios", icon: PlaySquare, label: "시나리오", disabled: false },
]
```

다음으로 변경 (3 nav — 시나리오는 demo subsystem 삭제와 함께 제거):
```tsx
const navItems = [
  { to: "/", icon: LayoutDashboard, label: "대시보드", disabled: false },
  { to: "/members", icon: Users, label: "회원", disabled: false },
  { to: "/partyrooms", icon: DoorOpen, label: "파티룸", disabled: false },
]
```

`PlaySquare` lucide-react import도 사용처 0이면 제거.

- [ ] **Step 2: `App.tsx`에 신규 4 라우트 추가 — placeholder 페이지로 일관 완료**

**의사결정**: G1에서 placeholder 페이지 4개 (`MembersPage`, `MemberDetailPage`, `PartyroomsPage`, `PartyroomDetailPage`) 임시 작성 + 라우트 등록 + 사이드바 enable. **이유**: 사이드바 메뉴 클릭 시 404 방지. G3/G4/G6/G7에서 각 placeholder를 widget 호스트로 교체.

```tsx
// src/pages/members-page.tsx (placeholder)
export function MembersPage() {
  return <div className="p-6"><h2 className="text-2xl font-bold">회원 (구현 중)</h2></div>
}
```

(member-detail/partyrooms/partyroom-detail 동일 패턴)

`App.tsx` import + Routes 추가:

```tsx
import { MembersPage } from "@/pages/members-page"
import { MemberDetailPage } from "@/pages/member-detail-page"
import { PartyroomsPage } from "@/pages/partyrooms-page"
import { PartyroomDetailPage } from "@/pages/partyroom-detail-page"

// Routes 안에 (보호 라우트 그룹 내):
<Route path="/members" element={<MembersPage />} />
<Route path="/members/:memberId" element={<MemberDetailPage />} />
<Route path="/partyrooms" element={<PartyroomsPage />} />
<Route path="/partyrooms/:partyroomId" element={<PartyroomDetailPage />} />
```

- [ ] **Step 3: 사이드바 통합 테스트 (단일 케이스)**

`src/app/__tests__/layout.test.tsx`:

```tsx
import { describe, it, expect } from "vitest"
import { render, screen } from "@testing-library/react"
import { MemoryRouter, Routes, Route } from "react-router-dom"
import AppLayout from "../layout"

// useSessionStore + useLogout 모킹
vi.mock("@/entities/session", () => ({
  useSessionStore: () => ({ meta: { role: "ADMIN" } }),
}))
vi.mock("@/features/logout/api/use-logout", () => ({
  useLogout: () => ({ mutate: vi.fn(), isPending: false }),
}))

describe("AppLayout sidebar", () => {
  it("회원/파티룸 nav가 enabled로 노출된다", () => {
    render(
      <MemoryRouter>
        <Routes>
          <Route element={<AppLayout />}>
            <Route index element={<div>home</div>} />
          </Route>
        </Routes>
      </MemoryRouter>,
    )
    const member = screen.getByText("회원").closest("a")
    expect(member).toHaveAttribute("href", "/members")
    const partyroom = screen.getByText("파티룸").closest("a")
    expect(partyroom).toHaveAttribute("href", "/partyrooms")
    // /users, /rooms, /scenarios 메뉴 부재 (demo subsystem 삭제)
    expect(screen.queryByText("가상 유저")).toBeNull()
    expect(screen.queryByText("시나리오")).toBeNull()
  })
})
```

- [ ] **Step 4: 테스트 통과**

```bash
pnpm exec vitest run src/app/__tests__/layout.test.tsx
```

Expected: 1/1 PASS.

### Task 1.11: G1 commit

- [ ] **Step 1: 전체 회귀**

```bash
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```

Expected: 모든 테스트 PASS (14a 16 + page.test 2 + url-state.test 4 + layout.test 1 = 23), 0 type error, 빌드 성공.

- [ ] **Step 2: 변경 stage**

```bash
git add -A
git status
```

확인할 변경: package.json/lock, main.tsx, App.tsx, app/layout.tsx, shared/api/page.ts(+test), shared/lib/{url-state,query-client}.ts(+test), entities/{member,partyroom}/, test/mocks/{handlers,fixtures}/, pages/{members,member-detail,partyrooms,partyroom-detail}-page.tsx (placeholder), app/__tests__/layout.test.tsx, demo 파일 삭제 (D 표시).

- [ ] **Step 3: commit**

```bash
git commit -m "$(cat <<'EOF'
chore(infra): demo 정리 + react-query + entities/{member,partyroom} + page<T>/url-state/QueryClientProvider (PR 14b G1)

- demo subsystem 일괄 삭제 (18 파일):
  - entities/{user,room,scenario,playlist,dj-queue}
  - features/{users,rooms,scenarios}
  - widgets/{users,rooms,scenarios}
  - pages/{users,rooms,scenarios}-page.tsx
  - shared/lib/api-client.ts
- shadcn 누락 컴포넌트 (skeleton/popover/calendar) 설치 + react-query-devtools 설치 (react-query/date-fns는 14a에 이미 있음)
- main.tsx targeted edit: 인라인 QueryClient → createQueryClient() 팩토리 + ReactQueryDevtools dev only (Toaster 보존)
- shared/api/page.ts (Page<T>, ApiCommonResponse<T>, unwrap)
- shared/lib/url-state.ts (parseSearchParams + stripInvalidParams + serializeQuery)
- shared/lib/query-client.ts (defaults: refetchOnWindowFocus false, staleTime 30s, ApiError no-retry)
- entities/{member,partyroom} 타입 (backend DTO 정합 — ProviderType=GOOGLE/TWITTER/LOCAL, AuthorityTier FM/AM/GT, PartyroomStatus ACTIVE/SUSPENDED/TERMINATED, StageType MAIN/GENERAL)
- src/test/mocks/{handlers,fixtures}/ 분할 (auth/members/partyrooms) — 기존 handlers.ts 삭제
- 사이드바 nav 4→3: 가상 유저/시나리오 제거, 회원(/members enabled) + 파티룸(/partyrooms enabled) 추가
- App.tsx 라우트 정리: /users, /rooms, /scenarios 제거 + 신규 4 라우트(/members, /members/:memberId, /partyrooms, /partyrooms/:partyroomId — placeholder pages, G2~G8에서 widget 호스트로 교체)

테스트: 23/23 PASS (14a 16 + 신규 7), tsc 0 error, build OK.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Chunk 2: G2 — `features/members` list api + filter zod + react-query hook + msw

**목표:** 멤버 list query를 backend로부터 받아오는 호출 경로 완성. UI는 G3.

### Task 2.1: `features/members/model/filter-schema.ts` + 테스트 (RED)

**Files:** Create `src/features/members/model/filter-schema.ts`, `src/features/members/model/__tests__/filter-schema.test.ts`.

- [ ] **Step 1: 테스트 작성**

```ts
import { describe, it, expect } from "vitest"
import { membersListQuerySchema } from "../filter-schema"

describe("membersListQuerySchema", () => {
  it("빈 입력 → default (page=0, size=50, sort=created_at_desc)", () => {
    const r = membersListQuerySchema.safeParse({})
    expect(r.success).toBe(true)
    if (r.success) expect(r.data).toEqual({ page: 0, size: 50, sort: "created_at_desc" })
  })

  it("email max 255 위반 → fail", () => {
    const r = membersListQuerySchema.safeParse({ email: "a".repeat(256) })
    expect(r.success).toBe(false)
  })

  it("tier=GT 통과 (강등 edge case)", () => {
    const r = membersListQuerySchema.safeParse({ tier: "GT" })
    expect(r.success).toBe(true)
  })

  it("joined_from > joined_to → fail", () => {
    const r = membersListQuerySchema.safeParse({ joined_from: "2026-04-30", joined_to: "2026-04-01" })
    expect(r.success).toBe(false)
    if (!r.success) {
      expect(r.error.issues.some(i => i.path[0] === "joined_to")).toBe(true)
    }
  })
})
```

- [ ] **Step 2: 실패 확인**

```bash
pnpm exec vitest run src/features/members/model/__tests__/filter-schema.test.ts
```

Expected: FAIL.

### Task 2.2: filter-schema 구현 (GREEN)

- [ ] **Step 1: 작성**

```ts
import { z } from "zod"

export const TierEnum = z.enum(["FM", "AM", "GT"])
export type Tier = z.infer<typeof TierEnum>

export const MemberSortEnum = z.enum([
  "created_at_desc",
  "created_at_asc",
  "last_activity_desc",
])
export type MemberSort = z.infer<typeof MemberSortEnum>

export const membersListQuerySchema = z.object({
  email: z.string().max(255).optional(),
  tier: TierEnum.optional(),
  joined_from: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(),
  joined_to: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(),
  page: z.coerce.number().int().min(0).default(0),
  size: z.coerce.number().int().min(1).max(200).default(50),
  sort: MemberSortEnum.default("created_at_desc"),
}).superRefine((v, ctx) => {
  if (v.joined_from && v.joined_to && v.joined_from > v.joined_to) {
    ctx.addIssue({
      code: "custom",
      path: ["joined_to"],
      message: "가입일 종료가 시작보다 빨라요",
    })
  }
})

export type MembersListQuery = z.infer<typeof membersListQuerySchema>
```

- [ ] **Step 2: 테스트 통과**

Expected: 4/4 PASS.

### Task 2.3: `features/members/api/members-api.ts` + 테스트

**Files:** Create `src/features/members/api/members-api.ts`, `src/features/members/api/__tests__/members-api.test.ts`.

- [ ] **Step 1: 테스트 작성 (RED)**

```ts
import { describe, it, expect, beforeEach } from "vitest"
import { server } from "@/test/mocks/server"
import { http, HttpResponse } from "msw"
import { listMembers, getMemberDetail } from "../members-api"
import { memberSummaryFixture, memberDetailFixture } from "@/test/mocks/fixtures/members"

describe("members-api", () => {
  describe("listMembers", () => {
    it("ApiCommonResponse를 unwrap해서 Page<T>를 반환", async () => {
      const r = await listMembers({ page: 0, size: 50, sort: "created_at_desc" })
      expect(r.content[0].email).toEqual(memberSummaryFixture.email)
      expect(r.totalElements).toBeGreaterThanOrEqual(1)
    })

    it("filter를 query string으로 직렬화한다 (email/tier/joined_from/joined_to/sort/page/size)", async () => {
      let capturedUrl: URL | undefined
      server.use(
        http.get("*/api/v1/admin/members", ({ request }) => {
          capturedUrl = new URL(request.url)
          return HttpResponse.json({ data: { content: [], totalElements: 0, totalPages: 0, number: 0, size: 50, first: true, last: true, empty: true, numberOfElements: 0 } })
        }),
      )
      await listMembers({
        email: "alice", tier: "AM",
        joined_from: "2026-01-01", joined_to: "2026-04-29",
        page: 2, size: 25, sort: "last_activity_desc",
      })
      const params = capturedUrl!.searchParams
      expect(params.get("email")).toBe("alice")
      expect(params.get("tier")).toBe("AM")
      expect(params.get("joined_from")).toBe("2026-01-01")
      expect(params.get("joined_to")).toBe("2026-04-29")
      expect(params.get("page")).toBe("2")
      expect(params.get("size")).toBe("25")
      expect(params.get("sort")).toBe("last_activity_desc")
    })

    it("undefined 필터는 query string에서 drop", async () => {
      let capturedUrl: URL | undefined
      server.use(
        http.get("*/api/v1/admin/members", ({ request }) => {
          capturedUrl = new URL(request.url)
          return HttpResponse.json({ data: { content: [], totalElements: 0, totalPages: 0, number: 0, size: 50, first: true, last: true, empty: true, numberOfElements: 0 } })
        }),
      )
      await listMembers({ page: 0, size: 50, sort: "created_at_desc" })
      const params = capturedUrl!.searchParams
      expect(params.get("email")).toBeNull()
      expect(params.get("tier")).toBeNull()
    })
  })

  describe("getMemberDetail", () => {
    it("ApiCommonResponse를 unwrap한 detail을 반환", async () => {
      const r = await getMemberDetail(1)
      expect(r.memberId).toBe(memberDetailFixture.memberId)
    })

    it("404 → ApiError throw", async () => {
      await expect(getMemberDetail(9999)).rejects.toThrow()
    })
  })
})
```

- [ ] **Step 2: 실패 확인**

Expected: FAIL.

### Task 2.4: members-api 구현 (GREEN)

```ts
import { http } from "@/shared/api/http"
import { unwrap } from "@/shared/api/page"
import type { ApiCommonResponse, Page } from "@/shared/api/page"
import { serializeQuery } from "@/shared/lib/url-state"
import type { AdminMemberSummary, AdminMemberDetail } from "@/entities/member"
import type { MembersListQuery } from "../model/filter-schema"

export async function listMembers(query: MembersListQuery): Promise<Page<AdminMemberSummary>> {
  const qs = serializeQuery(query as Record<string, unknown>).toString()
  const res = await http<ApiCommonResponse<Page<AdminMemberSummary>>>(
    `/api/v1/admin/members${qs ? `?${qs}` : ""}`,
  )
  return unwrap(res)
}

export async function getMemberDetail(memberId: number): Promise<AdminMemberDetail> {
  const res = await http<ApiCommonResponse<AdminMemberDetail>>(
    `/api/v1/admin/members/${memberId}`,
  )
  return unwrap(res)
}
```

- [ ] **Step 1: 테스트 통과**

Expected: 5/5 PASS.

### Task 2.5: `use-members-list.ts` + `use-member-detail.ts`

**Files:** Create `src/features/members/api/use-members-list.ts`, `src/features/members/api/use-member-detail.ts`.

- [ ] **Step 1: `use-members-list.ts`**

```ts
import { useQuery, keepPreviousData } from "@tanstack/react-query"
import { listMembers } from "./members-api"
import type { MembersListQuery } from "../model/filter-schema"

export function useMembersList(query: MembersListQuery) {
  return useQuery({
    queryKey: ["members", "list", query],
    queryFn: () => listMembers(query),
    placeholderData: keepPreviousData,
  })
}
```

- [ ] **Step 2: `use-member-detail.ts`**

```ts
import { useQuery } from "@tanstack/react-query"
import { getMemberDetail } from "./members-api"

export function useMemberDetail(memberId: number) {
  return useQuery({
    queryKey: ["members", "detail", memberId],
    queryFn: () => getMemberDetail(memberId),
    enabled: Number.isFinite(memberId) && memberId > 0,
  })
}
```

- [ ] **Step 3: tsc**

Expected: 0 error.

(react-query hook 자체는 G3 widget 통합 테스트에서 실제 사용 검증 — 별 unit test 안 작성. queryKey/staleTime은 createQueryClient defaults가 책임.)

### Task 2.6: G2 commit

- [ ] **Step 1: 회귀**

```bash
pnpm test:run
```

Expected: 23 + 9 = 32 PASS (filter-schema 4 + members-api 5).

- [ ] **Step 2: commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(members): list/detail API + filter zod + react-query hooks + msw mock (PR 14b G2)

- features/members/model/filter-schema.ts (zod: email/tier/joined_*/page/size/sort + cross-field)
- features/members/api/members-api.ts (listMembers/getMemberDetail — ApiCommonResponse unwrap)
- features/members/api/use-{members-list,member-detail}.ts (react-query useQuery + keepPreviousData)
- msw handlers/members.ts + fixtures (G1에서 셋업, G2에서 사용 검증)

테스트: 9 신규 (filter zod 4 + api 5) — 32/32 PASS

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Chunk 3: G3 — `widgets/members-list` + `pages/members-page` + integration

**목표:** 멤버 list 화면 완성. URL ↔ form ↔ react-query 양방향 wiring.

### Task 3.1: 공용 `widgets/pagination.tsx` + 테스트

**Files:** Create `src/widgets/pagination.tsx`, `src/widgets/__tests__/pagination.test.tsx`.

- [ ] **Step 1: 컴포넌트 작성**

```tsx
import { Button } from "@/components/ui/button"
import { ChevronLeft, ChevronRight } from "lucide-react"

interface PaginationProps {
  page: number          // 0-based
  totalPages: number
  totalElements: number
  onChange: (page: number) => void
}

export function Pagination({ page, totalPages, totalElements, onChange }: PaginationProps) {
  if (totalElements === 0) return null
  const canPrev = page > 0
  const canNext = page < totalPages - 1
  return (
    <div className="flex items-center justify-between mt-4">
      <p className="text-sm text-muted-foreground">총 {totalElements}건</p>
      <div className="flex items-center gap-2">
        <Button variant="outline" size="sm" disabled={!canPrev} onClick={() => onChange(page - 1)}>
          <ChevronLeft className="h-4 w-4" />
        </Button>
        <span className="text-sm">{page + 1} / {totalPages}</span>
        <Button variant="outline" size="sm" disabled={!canNext} onClick={() => onChange(page + 1)}>
          <ChevronRight className="h-4 w-4" />
        </Button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 테스트 (총건수 0 → null, prev/next disabled, click 호출)**

```tsx
import { describe, it, expect, vi } from "vitest"
import { render, screen, fireEvent } from "@testing-library/react"
import { Pagination } from "../pagination"

describe("Pagination", () => {
  it("totalElements=0 → 렌더 안 함", () => {
    const { container } = render(<Pagination page={0} totalPages={0} totalElements={0} onChange={vi.fn()} />)
    expect(container.firstChild).toBeNull()
  })

  it("page=0이면 prev disabled, page=last이면 next disabled", () => {
    render(<Pagination page={0} totalPages={3} totalElements={150} onChange={vi.fn()} />)
    const buttons = screen.getAllByRole("button")
    expect(buttons[0]).toBeDisabled() // prev
    expect(buttons[1]).not.toBeDisabled() // next
  })

  it("next 클릭 시 onChange(page+1)", () => {
    const onChange = vi.fn()
    render(<Pagination page={1} totalPages={3} totalElements={150} onChange={onChange} />)
    const buttons = screen.getAllByRole("button")
    fireEvent.click(buttons[1])
    expect(onChange).toHaveBeenCalledWith(2)
  })
})
```

- [ ] **Step 3: 테스트 통과**

Expected: 3/3 PASS.

### Task 3.2: `features/members/ui/members-filter-form.tsx` + 테스트

**Files:** Create + test.

- [ ] **Step 1: 컴포넌트 작성**

```tsx
import { useEffect, useState } from "react"
import { Input } from "@/components/ui/input"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { Button } from "@/components/ui/button"
import { useDebounce } from "@/shared/lib/use-debounce"  // 별도 파일 — 다음 task에서 작성
import type { MembersListQuery, Tier, MemberSort } from "../model/filter-schema"

interface Props {
  query: MembersListQuery
  onChange: (next: Partial<MembersListQuery>) => void
  onReset: () => void
}

const TIER_OPTIONS: { value: Tier; label: string }[] = [
  { value: "FM", label: "FM (정회원)" },
  { value: "AM", label: "AM (준회원)" },
  { value: "GT", label: "GT (강등)" },
]

const SORT_OPTIONS: { value: MemberSort; label: string }[] = [
  { value: "created_at_desc", label: "가입일 ↓" },
  { value: "created_at_asc", label: "가입일 ↑" },
  { value: "last_activity_desc", label: "마지막 활동 ↓" },
]

export function MembersFilterForm({ query, onChange, onReset }: Props) {
  const [emailDraft, setEmailDraft] = useState(query.email ?? "")
  const debouncedEmail = useDebounce(emailDraft, 300)

  useEffect(() => {
    if (debouncedEmail !== (query.email ?? "")) {
      onChange({ email: debouncedEmail || undefined, page: 0 })
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [debouncedEmail])

  return (
    <div className="flex flex-wrap gap-2 items-end mb-4">
      <div>
        <label className="block text-xs font-medium mb-1">이메일</label>
        <Input
          value={emailDraft}
          onChange={(e) => setEmailDraft(e.target.value)}
          placeholder="부분일치"
          className="w-48"
        />
      </div>
      <div>
        <label className="block text-xs font-medium mb-1">권한</label>
        <Select
          value={query.tier ?? "ALL"}
          onValueChange={(v) => onChange({ tier: v === "ALL" ? undefined : (v as Tier), page: 0 })}
        >
          <SelectTrigger className="w-32"><SelectValue /></SelectTrigger>
          <SelectContent>
            <SelectItem value="ALL">전체</SelectItem>
            {TIER_OPTIONS.map(o => <SelectItem key={o.value} value={o.value}>{o.label}</SelectItem>)}
          </SelectContent>
        </Select>
      </div>
      <div>
        <label className="block text-xs font-medium mb-1">가입일 from</label>
        <Input
          type="date"
          value={query.joined_from ?? ""}
          onChange={(e) => onChange({ joined_from: e.target.value || undefined, page: 0 })}
          className="w-36"
        />
      </div>
      <div>
        <label className="block text-xs font-medium mb-1">가입일 to</label>
        <Input
          type="date"
          value={query.joined_to ?? ""}
          onChange={(e) => onChange({ joined_to: e.target.value || undefined, page: 0 })}
          className="w-36"
        />
      </div>
      <div>
        <label className="block text-xs font-medium mb-1">정렬</label>
        <Select
          value={query.sort}
          onValueChange={(v) => onChange({ sort: v as MemberSort, page: 0 })}
        >
          <SelectTrigger className="w-44"><SelectValue /></SelectTrigger>
          <SelectContent>
            {SORT_OPTIONS.map(o => <SelectItem key={o.value} value={o.value}>{o.label}</SelectItem>)}
          </SelectContent>
        </Select>
      </div>
      <Button variant="outline" onClick={onReset}>초기화</Button>
    </div>
  )
}
```

- [ ] **Step 2: `src/shared/lib/use-debounce.ts` 작성 (재사용)**

```ts
import { useEffect, useState } from "react"

export function useDebounce<T>(value: T, ms: number): T {
  const [debounced, setDebounced] = useState(value)
  useEffect(() => {
    const id = setTimeout(() => setDebounced(value), ms)
    return () => clearTimeout(id)
  }, [value, ms])
  return debounced
}
```

- [ ] **Step 3: 테스트 — debounce + 즉시 반영 + 초기화**

```tsx
import { describe, it, expect, vi } from "vitest"
import { render, screen, fireEvent, waitFor } from "@testing-library/react"
import { MembersFilterForm } from "../members-filter-form"
import type { MembersListQuery } from "../../model/filter-schema"

const baseQuery: MembersListQuery = { page: 0, size: 50, sort: "created_at_desc" }

describe("MembersFilterForm", () => {
  it("이메일 입력 → 300ms 후 onChange({ email, page: 0 })", async () => {
    vi.useFakeTimers()
    const onChange = vi.fn()
    render(<MembersFilterForm query={baseQuery} onChange={onChange} onReset={vi.fn()} />)
    fireEvent.change(screen.getByLabelText("이메일"), { target: { value: "alice" } })
    expect(onChange).not.toHaveBeenCalled()
    vi.advanceTimersByTime(300)
    await waitFor(() => expect(onChange).toHaveBeenCalledWith({ email: "alice", page: 0 }))
    vi.useRealTimers()
  })

  it("정렬 dropdown 변경 → 즉시 onChange({ sort, page: 0 })", async () => {
    // shadcn Select(radix)는 닫힘 상태에서 옵션이 DOM에 없음 — combobox role을 클릭해 열고 option role을 클릭
    const user = userEvent.setup()
    const onChange = vi.fn()
    render(<MembersFilterForm query={baseQuery} onChange={onChange} onReset={vi.fn()} />)
    await user.click(screen.getByRole("combobox", { name: /정렬/ }))
    await user.click(await screen.findByRole("option", { name: "가입일 ↑" }))
    expect(onChange).toHaveBeenCalledWith({ sort: "created_at_asc", page: 0 })
  })

  it("초기화 클릭 → onReset()", async () => {
    const user = userEvent.setup()
    const onReset = vi.fn()
    render(<MembersFilterForm query={baseQuery} onChange={vi.fn()} onReset={onReset} />)
    await user.click(screen.getByText("초기화"))
    expect(onReset).toHaveBeenCalled()
  })
})
```

> Note: 테스트 파일 상단에 `import userEvent from "@testing-library/user-event"` 추가. shadcn/radix Select는 closed 상태에서 옵션이 DOM에 없어 `fireEvent.click(getByText)` 로 못 잡음 — `userEvent.click(getByRole("combobox"))` → `findByRole("option", { name })` idiom 필수.

- [ ] **Step 4: 테스트 통과**

Expected: 3/3 PASS.

### Task 3.3: `features/members/ui/members-table.tsx` + 테스트

- [ ] **Step 1: 컴포넌트 작성**

```tsx
import { useNavigate } from "react-router-dom"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import { Badge } from "@/components/ui/badge"
import { Skeleton } from "@/components/ui/skeleton"
import type { AdminMemberSummary } from "@/entities/member"

interface Props {
  rows: AdminMemberSummary[]
  isLoading: boolean
  isEmpty: boolean
}

const TIER_LABEL: Record<string, string> = {
  FM: "FM", AM: "AM", GT: "GT (강등)",
}

function formatKst(iso: string | null): string {
  if (!iso) return "-"
  const d = new Date(iso)
  return d.toLocaleString("ko-KR", { hour12: false })
}

export function MembersTable({ rows, isLoading, isEmpty }: Props) {
  const navigate = useNavigate()
  if (isLoading) {
    return (
      <div className="space-y-2">
        {Array.from({ length: 5 }).map((_, i) => <Skeleton key={i} className="h-12 w-full" />)}
      </div>
    )
  }
  if (isEmpty) {
    return (
      <div className="text-center py-12 text-muted-foreground">
        조건에 맞는 회원이 없습니다
      </div>
    )
  }
  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>ID</TableHead>
          <TableHead>이메일</TableHead>
          <TableHead>provider</TableHead>
          <TableHead>닉네임</TableHead>
          <TableHead>권한</TableHead>
          <TableHead>마지막 로그인</TableHead>
          <TableHead>가입일</TableHead>
          <TableHead>상태</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {rows.map((row) => (
          <TableRow
            key={row.memberId}
            className="cursor-pointer hover:bg-accent/50"
            onClick={() => navigate(`/members/${row.memberId}`)}
          >
            <TableCell>{row.memberId}</TableCell>
            <TableCell>{row.email}</TableCell>
            <TableCell>{row.providerType}</TableCell>
            <TableCell>{row.nickname ?? "-"}</TableCell>
            <TableCell>
              <Badge variant={row.authorityTier === "GT" ? "destructive" : "default"}>
                {TIER_LABEL[row.authorityTier] ?? row.authorityTier}
              </Badge>
            </TableCell>
            <TableCell>{formatKst(row.lastLoginAt)}</TableCell>
            <TableCell>{formatKst(row.createdAt)}</TableCell>
            <TableCell>
              {row.withdrawn
                ? <Badge variant="secondary" title={row.withdrawnAt ?? ""}>탈퇴</Badge>
                : <Badge variant="outline">활동</Badge>}
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  )
}
```

- [ ] **Step 2: 테스트 — empty/loaded/row click**

```tsx
import { describe, it, expect, vi } from "vitest"
import { render, screen, fireEvent } from "@testing-library/react"
import { MemoryRouter, Routes, Route } from "react-router-dom"
import { MembersTable } from "../members-table"
import { memberSummaryFixture, memberSummaryWithdrawnFixture } from "@/test/mocks/fixtures/members"

function wrap(ui: React.ReactNode, capture: { last?: string }) {
  return render(
    <MemoryRouter initialEntries={["/members"]}>
      <Routes>
        <Route path="/members" element={ui} />
        <Route path="/members/:id" element={<div data-testid="dest">{capture.last = window.location.pathname}</div>} />
      </Routes>
    </MemoryRouter>,
  )
}

describe("MembersTable", () => {
  it("loaded — row 노출 + withdrawn badge", () => {
    render(
      <MemoryRouter>
        <MembersTable rows={[memberSummaryFixture, memberSummaryWithdrawnFixture]} isLoading={false} isEmpty={false} />
      </MemoryRouter>,
    )
    expect(screen.getByText(memberSummaryFixture.email)).toBeInTheDocument()
    expect(screen.getByText("탈퇴")).toBeInTheDocument()
  })

  it("empty 상태 메시지", () => {
    render(
      <MemoryRouter>
        <MembersTable rows={[]} isLoading={false} isEmpty={true} />
      </MemoryRouter>,
    )
    expect(screen.getByText(/회원이 없습니다/)).toBeInTheDocument()
  })

  it("row click → /members/:id 라우팅", () => {
    render(
      <MemoryRouter initialEntries={["/members"]}>
        <Routes>
          <Route path="/members" element={<MembersTable rows={[memberSummaryFixture]} isLoading={false} isEmpty={false} />} />
          <Route path="/members/:id" element={<div>detail</div>} />
        </Routes>
      </MemoryRouter>,
    )
    fireEvent.click(screen.getByText(memberSummaryFixture.email))
    expect(screen.getByText("detail")).toBeInTheDocument()
  })
})
```

- [ ] **Step 3: 테스트 통과**

Expected: 3/3 PASS.

### Task 3.4: `widgets/members-list.tsx` (합성)

```tsx
import { useSearchParams } from "react-router-dom"
import { toast } from "sonner"
import { membersListQuerySchema, type MembersListQuery } from "@/features/members/model/filter-schema"
import { useMembersList } from "@/features/members/api/use-members-list"
import { MembersFilterForm } from "@/features/members/ui/members-filter-form"
import { MembersTable } from "@/features/members/ui/members-table"
import { parseSearchParams, stripInvalidParams, serializeQuery } from "@/shared/lib/url-state"
import { Pagination } from "@/widgets/pagination"
import { ApiError } from "@/shared/api/error"

export function MembersListWidget() {
  const [params, setParams] = useSearchParams()
  const parsed = parseSearchParams(membersListQuerySchema, params)

  if (!parsed.success) {
    const cleaned = stripInvalidParams(params, parsed.error)
    setParams(cleaned, { replace: true })
    toast.error("필터 일부가 잘못돼 무시했어요")
    return null
  }

  const query: MembersListQuery = parsed.data
  const { data, isLoading, error } = useMembersList(query)

  const updateQuery = (next: Partial<MembersListQuery>) => {
    const merged = { ...query, ...next }
    setParams(serializeQuery(merged as Record<string, unknown>))
  }
  const reset = () => setParams(new URLSearchParams())
  const goToPage = (page: number) => updateQuery({ page })

  return (
    <div className="p-6 lg:p-8">
      <div className="mb-6 flex items-center justify-between">
        <h2 className="text-2xl font-bold">회원</h2>
        {data && <p className="text-sm text-muted-foreground">총 {data.totalElements}건</p>}
      </div>
      <MembersFilterForm query={query} onChange={updateQuery} onReset={reset} />
      {error instanceof ApiError && error.status === 403 && (
        <p className="text-destructive text-sm mb-2">이 화면을 볼 권한이 없습니다</p>
      )}
      <MembersTable
        rows={data?.content ?? []}
        isLoading={isLoading}
        isEmpty={!isLoading && (data?.empty ?? false)}
      />
      {data && (
        <Pagination
          page={data.number}
          totalPages={data.totalPages}
          totalElements={data.totalElements}
          onChange={goToPage}
        />
      )}
    </div>
  )
}
```

### Task 3.5: `pages/members-page.tsx` (placeholder 교체)

```tsx
import { MembersListWidget } from "@/widgets/members-list"

export function MembersPage() {
  return <MembersListWidget />
}
```

### Task 3.6: integration test — `/members` page

**Files:** Create `src/pages/__tests__/members-page.test.tsx`.

- [ ] **Step 1: 작성**

```tsx
import { describe, it, expect } from "vitest"
import { render, screen, fireEvent, waitFor } from "@testing-library/react"
import { MemoryRouter, Routes, Route } from "react-router-dom"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { Toaster } from "sonner"
import { MembersPage } from "../members-page"
import { memberSummaryFixture } from "@/test/mocks/fixtures/members"

function wrap(initial = "/members") {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  return render(
    <QueryClientProvider client={qc}>
      <MemoryRouter initialEntries={[initial]}>
        <Routes>
          <Route path="/members" element={<MembersPage />} />
          <Route path="/members/:memberId" element={<div data-testid="detail">detail</div>} />
        </Routes>
        <Toaster />
      </MemoryRouter>
    </QueryClientProvider>,
  )
}

describe("MembersPage integration", () => {
  it("happy: 마운트 → list 로드 → 회원 row 노출 → row click → /members/:id", async () => {
    wrap()
    await waitFor(() => expect(screen.getByText(memberSummaryFixture.email)).toBeInTheDocument())
    fireEvent.click(screen.getByText(memberSummaryFixture.email))
    await waitFor(() => expect(screen.getByTestId("detail")).toBeInTheDocument())
  })

  it("URL `/members?tier=FM` → backend로 전달 → 빈 결과 (msw 핸들러가 tier=FM에서 [] 반환)", async () => {
    wrap("/members?tier=FM")
    await waitFor(() => expect(screen.getByText(/회원이 없습니다/)).toBeInTheDocument())
  })

  it("URL `/members?email=` (255자 초과) → invalid drop + toast", async () => {
    const longEmail = "a".repeat(256)
    wrap(`/members?email=${longEmail}`)
    await waitFor(() => expect(screen.getByText(/잘못돼 무시했어요/)).toBeInTheDocument())
    // 정정 후 list 정상 로드 (default query)
    await waitFor(() => expect(screen.getByText(memberSummaryFixture.email)).toBeInTheDocument())
  })
})
```

- [ ] **Step 2: 테스트 통과**

Expected: 3/3 PASS.

### Task 3.7: G3 commit

- [ ] **Step 1: 회귀 + 빌드**

```bash
pnpm test:run && pnpm build
```

Expected: 32 + 12 = 44 PASS (pagination 3 + filter-form 3 + table 3 + integration 3), 빌드 OK.

- [ ] **Step 2: commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(members): list page (filter form + table + pagination + URL 동기화) (PR 14b G3)

- widgets/pagination.tsx (members/partyrooms 양쪽 사용 예정)
- features/members/ui/members-filter-form.tsx (email debounce 300ms + tier/sort/date dropdown 즉시)
- features/members/ui/members-table.tsx (skeleton/empty/row click)
- widgets/members-list.tsx (URL ↔ react-query ↔ form 합성, parseSearchParams + safeParse + invalid drop)
- pages/members-page.tsx (placeholder → widget 교체)
- shared/lib/use-debounce.ts (300ms debounce hook, partyrooms에서도 재사용)

테스트: 12 신규 (pagination 3 + filter form 3 + table 3 + integration 3) — 44/44 PASS

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Chunk 4: G4 — `features/members` detail + `widgets/members-detail` + `pages/member-detail-page` + integration

**목표:** 멤버 detail 페이지 완성 (5 카드).

### Task 4.1: `features/members/ui/member-detail-cards.tsx` + 테스트

```tsx
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import type { AdminMemberDetail } from "@/entities/member"

function formatKst(iso: string | null): string {
  return iso ? new Date(iso).toLocaleString("ko-KR", { hour12: false }) : "-"
}

interface Props {
  detail: AdminMemberDetail
  withdrawn?: boolean
  withdrawnAt?: string | null
}

export function MemberDetailCards({ detail, withdrawn, withdrawnAt }: Props) {
  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <h2 className="text-2xl font-bold">#{detail.memberId} {detail.profile.nickname ?? "-"}</h2>
        {withdrawn && <Badge variant="secondary" title={withdrawnAt ?? ""}>탈퇴 회원</Badge>}
      </div>

      <Card>
        <CardHeader><CardTitle>계정 정보</CardTitle></CardHeader>
        <CardContent className="grid grid-cols-2 gap-3 text-sm">
          <div><span className="text-muted-foreground">이메일</span><div>{detail.userAccount.email}</div></div>
          <div><span className="text-muted-foreground">provider</span><div>{detail.userAccount.providerType}</div></div>
          <div><span className="text-muted-foreground">user_account_id</span><div>{detail.userAccount.userAccountId}</div></div>
          <div><span className="text-muted-foreground">계정 생성일</span><div>{formatKst(detail.userAccount.createdAt)}</div></div>
        </CardContent>
      </Card>

      <Card>
        <CardHeader><CardTitle>프로필</CardTitle></CardHeader>
        <CardContent className="space-y-2 text-sm">
          <div><span className="text-muted-foreground">닉네임</span><div>{detail.profile.nickname ?? "-"}</div></div>
          <div><span className="text-muted-foreground">소개</span><div className="whitespace-pre-line">{detail.profile.introduction ?? "-"}</div></div>
        </CardContent>
      </Card>

      <Card>
        <CardHeader><CardTitle>권한 + 메타</CardTitle></CardHeader>
        <CardContent className="flex items-center gap-3 text-sm">
          <Badge variant={detail.authorityTier === "GT" ? "destructive" : "default"}>
            {detail.authorityTier}{detail.authorityTier === "GT" ? " (강등)" : ""}
          </Badge>
          <span className="text-muted-foreground">멤버 생성일</span>
          <span>{formatKst(detail.createdAt)}</span>
        </CardContent>
      </Card>

      <Card>
        <CardHeader><CardTitle>최근 활동 (top 30)</CardTitle></CardHeader>
        <CardContent>
          {detail.recentActivityLog.length === 0
            ? <p className="text-sm text-muted-foreground">최근 활동 없음</p>
            : (
              <Table>
                <TableHeader>
                  <TableRow>
                    <TableHead>시각</TableHead>
                    <TableHead>유형</TableHead>
                    <TableHead>요약</TableHead>
                  </TableRow>
                </TableHeader>
                <TableBody>
                  {detail.recentActivityLog.map((row, i) => (
                    <TableRow key={i}>
                      <TableCell>{formatKst(row.occurredAt)}</TableCell>
                      <TableCell>{row.type}</TableCell>
                      <TableCell>{row.summary}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            )}
        </CardContent>
      </Card>
    </div>
  )
}
```

- [ ] **Step 1: 테스트 — withdrawn badge / activity log empty**

```tsx
import { describe, it, expect } from "vitest"
import { render, screen } from "@testing-library/react"
import { MemberDetailCards } from "../member-detail-cards"
import { memberDetailFixture } from "@/test/mocks/fixtures/members"

describe("MemberDetailCards", () => {
  it("withdrawn=true → 탈퇴 badge", () => {
    render(<MemberDetailCards detail={memberDetailFixture} withdrawn={true} withdrawnAt="2026-04-20T10:00:00" />)
    expect(screen.getByText(/탈퇴 회원/)).toBeInTheDocument()
  })

  it("recentActivityLog 빈 배열 → '최근 활동 없음'", () => {
    render(
      <MemberDetailCards
        detail={{ ...memberDetailFixture, recentActivityLog: [] }}
      />,
    )
    expect(screen.getByText("최근 활동 없음")).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 테스트 통과**

Expected: 2/2 PASS.

### Task 4.2: `widgets/members-detail.tsx`

```tsx
import { useParams, Link } from "react-router-dom"
import { useMemberDetail } from "@/features/members/api/use-member-detail"
import { MemberDetailCards } from "@/features/members/ui/member-detail-cards"
import { Skeleton } from "@/components/ui/skeleton"
import { Button } from "@/components/ui/button"
import { ApiError } from "@/shared/api/error"

export function MembersDetailWidget() {
  const { memberId } = useParams<{ memberId: string }>()
  const id = Number(memberId)
  const { data, isLoading, error } = useMemberDetail(id)

  if (Number.isNaN(id) || id <= 0) {
    return <NotFoundView />
  }
  if (isLoading) {
    return (
      <div className="p-6 lg:p-8 space-y-4">
        <Skeleton className="h-8 w-48" />
        <Skeleton className="h-32 w-full" />
        <Skeleton className="h-32 w-full" />
      </div>
    )
  }
  if (error instanceof ApiError && error.status === 404) {
    return <NotFoundView />
  }
  if (!data) return null

  return (
    <div className="p-6 lg:p-8">
      <Link to="/members" className="text-sm text-muted-foreground mb-4 inline-block">← 목록으로</Link>
      <MemberDetailCards detail={data} />
    </div>
  )
}

function NotFoundView() {
  return (
    <div className="p-6 lg:p-8">
      <p className="text-muted-foreground mb-4">존재하지 않는 회원입니다</p>
      <Button asChild variant="outline"><Link to="/members">목록으로</Link></Button>
    </div>
  )
}
```

### Task 4.3: `pages/member-detail-page.tsx`

```tsx
import { MembersDetailWidget } from "@/widgets/members-detail"

export function MemberDetailPage() {
  return <MembersDetailWidget />
}
```

### Task 4.4: integration test — `/members/:memberId`

```tsx
import { describe, it, expect } from "vitest"
import { render, screen, waitFor } from "@testing-library/react"
import { MemoryRouter, Routes, Route } from "react-router-dom"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { MemberDetailPage } from "../member-detail-page"
import { memberDetailFixture } from "@/test/mocks/fixtures/members"

function wrap(initial: string) {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  return render(
    <QueryClientProvider client={qc}>
      <MemoryRouter initialEntries={[initial]}>
        <Routes>
          <Route path="/members/:memberId" element={<MemberDetailPage />} />
          <Route path="/members" element={<div data-testid="list">list</div>} />
        </Routes>
      </MemoryRouter>
    </QueryClientProvider>,
  )
}

describe("MemberDetailPage integration", () => {
  it("happy: /members/1 → detail 카드 + activity log", async () => {
    wrap("/members/1")
    await waitFor(() => expect(screen.getByText(`#${memberDetailFixture.memberId} ${memberDetailFixture.profile.nickname}`)).toBeInTheDocument())
    expect(screen.getByText(memberDetailFixture.userAccount.email)).toBeInTheDocument()
  })

  it("404: /members/9999 → '존재하지 않는 회원' + 목록으로 버튼", async () => {
    wrap("/members/9999")
    await waitFor(() => expect(screen.getByText(/존재하지 않는 회원/)).toBeInTheDocument())
    expect(screen.getByText("목록으로")).toBeInTheDocument()
  })
})
```

### Task 4.5: G4 commit

- [ ] **Step 1: 회귀**

```bash
pnpm test:run
```

Expected: 44 + 4 = 48 PASS (cards 2 + integration 2).

- [ ] **Step 2: commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(members): detail page (5 카드 — header/UserAccount/Profile/Tier/ActivityLog) (PR 14b G4)

- features/members/ui/member-detail-cards.tsx (5 카드 묶음, withdrawn badge, activity log empty)
- widgets/members-detail.tsx (useMemberDetail + skeleton + 404 NotFoundView)
- pages/member-detail-page.tsx (widget 교체)
- features/members/api/use-member-detail.ts (G2에서 작성됨, G4 통합 검증)

테스트: 4 신규 (cards 2 + integration 2) — 48/48 PASS

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Chunk 5: G5 — `features/partyrooms` list api + filter zod + react-query + msw

**목표:** 멤버 G2와 동일 구조. 파티룸 도메인 list/detail API + zod schema + hook.

### Task 5.1: `features/partyrooms/model/filter-schema.ts` + 테스트

- [ ] **Step 1: 테스트**

```ts
import { describe, it, expect } from "vitest"
import { partyroomsListQuerySchema, PartyroomSortEnum } from "../filter-schema"

describe("partyroomsListQuerySchema", () => {
  it("default: page=0, size=50, sort=createdAt,desc", () => {
    const r = partyroomsListQuerySchema.safeParse({})
    expect(r.success).toBe(true)
    if (r.success) expect(r.data).toEqual({ page: 0, size: 50, sort: "createdAt,desc" })
  })

  it("status enum 화이트리스트 (ACTIVE/SUSPENDED/TERMINATED)", () => {
    expect(partyroomsListQuerySchema.safeParse({ status: "ACTIVE" }).success).toBe(true)
    expect(partyroomsListQuerySchema.safeParse({ status: "ARCHIVED" }).success).toBe(false)
  })

  it("PartyroomSortEnum 5 columns × asc/desc = 10 옵션", () => {
    expect(PartyroomSortEnum.options).toHaveLength(10)
    expect(PartyroomSortEnum.options).toContain("hostNickname,asc")
    expect(PartyroomSortEnum.safeParse("invalid").success).toBe(false)
  })

  it("createdFrom > createdTo → fail", () => {
    const r = partyroomsListQuerySchema.safeParse({
      createdFrom: "2026-04-30T00:00:00.000Z",
      createdTo: "2026-04-01T00:00:00.000Z",
    })
    expect(r.success).toBe(false)
  })
})
```

- [ ] **Step 2: 실패 → 구현 → 통과**

```ts
import { z } from "zod"

export const PartyroomStatusEnum = z.enum(["ACTIVE", "SUSPENDED", "TERMINATED"])
export type PartyroomStatus = z.infer<typeof PartyroomStatusEnum>

export const StageTypeEnum = z.enum(["MAIN", "GENERAL"])
export type StageType = z.infer<typeof StageTypeEnum>

export const PartyroomSortEnum = z.enum([
  "createdAt,desc", "createdAt,asc",
  "lastActivityAt,desc", "lastActivityAt,asc",
  "crewCount,desc", "crewCount,asc",
  "title,desc", "title,asc",
  "hostNickname,desc", "hostNickname,asc",
])
export type PartyroomSort = z.infer<typeof PartyroomSortEnum>

export const partyroomsListQuerySchema = z.object({
  status: PartyroomStatusEnum.optional(),
  stageType: StageTypeEnum.optional(),
  createdFrom: z.string().datetime().optional(),
  createdTo: z.string().datetime().optional(),
  host: z.string().min(2).max(50).optional(),
  page: z.coerce.number().int().min(0).default(0),
  size: z.coerce.number().int().min(1).max(200).default(50),
  sort: PartyroomSortEnum.default("createdAt,desc"),
}).superRefine((v, ctx) => {
  if (v.createdFrom && v.createdTo && v.createdFrom > v.createdTo) {
    ctx.addIssue({
      code: "custom",
      path: ["createdTo"],
      message: "생성일 종료가 시작보다 빨라요",
    })
  }
})

export type PartyroomsListQuery = z.infer<typeof partyroomsListQuerySchema>
```

Expected: 4/4 PASS.

### Task 5.2: `features/partyrooms/api/partyrooms-api.ts` + 테스트

- [ ] **Step 1: 테스트 — raw page (no unwrap) + sort serialize + 400 propagation**

```ts
import { describe, it, expect } from "vitest"
import { server } from "@/test/mocks/server"
import { http, HttpResponse } from "msw"
import { listPartyrooms, getPartyroomDetail } from "../partyrooms-api"
import { partyroomListItemFixture } from "@/test/mocks/fixtures/partyrooms"

describe("partyrooms-api", () => {
  it("listPartyrooms: raw Page<T> 반환 (unwrap 안 함)", async () => {
    const r = await listPartyrooms({ page: 0, size: 50, sort: "createdAt,desc" })
    expect(r.content[0].partyroomId).toBe(partyroomListItemFixture.partyroomId)
  })

  it("filter/sort 직렬화 — status/stageType/createdFrom/createdTo/host/sort", async () => {
    let url: URL | undefined
    server.use(
      http.get("*/api/v1/admin/partyrooms", ({ request }) => {
        url = new URL(request.url)
        return HttpResponse.json({ content: [], totalElements: 0, totalPages: 0, number: 0, size: 50, first: true, last: true, empty: true, numberOfElements: 0 })
      }),
    )
    await listPartyrooms({
      status: "ACTIVE", stageType: "GENERAL", host: "alice",
      createdFrom: "2026-01-01T00:00:00.000Z", createdTo: "2026-04-29T00:00:00.000Z",
      page: 1, size: 25, sort: "hostNickname,asc",
    })
    const p = url!.searchParams
    expect(p.get("status")).toBe("ACTIVE")
    expect(p.get("stageType")).toBe("GENERAL")
    expect(p.get("host")).toBe("alice")
    expect(p.get("sort")).toBe("hostNickname,asc")
  })

  it("invalid sort → 400 ApiError", async () => {
    await expect(listPartyrooms({ page: 0, size: 50, sort: "invalid_xyz" as any })).rejects.toThrow()
  })

  it("getPartyroomDetail 404 → ApiError", async () => {
    await expect(getPartyroomDetail(9999)).rejects.toThrow()
  })

  it("getPartyroomDetail happy", async () => {
    const r = await getPartyroomDetail(1)
    expect(r.title).toBeTruthy()
  })
})
```

- [ ] **Step 2: 구현**

```ts
import { http } from "@/shared/api/http"
import type { Page } from "@/shared/api/page"
import { serializeQuery } from "@/shared/lib/url-state"
import type { AdminPartyroomListItem, AdminPartyroomDetail } from "@/entities/partyroom"
import type { PartyroomsListQuery } from "../model/filter-schema"

export async function listPartyrooms(query: PartyroomsListQuery): Promise<Page<AdminPartyroomListItem>> {
  const qs = serializeQuery(query as Record<string, unknown>).toString()
  return http<Page<AdminPartyroomListItem>>(
    `/api/v1/admin/partyrooms${qs ? `?${qs}` : ""}`,
  )
}

export async function getPartyroomDetail(partyroomId: number): Promise<AdminPartyroomDetail> {
  return http<AdminPartyroomDetail>(`/api/v1/admin/partyrooms/${partyroomId}`)
}
```

Expected: 5/5 PASS.

### Task 5.3: `use-partyrooms-list.ts` + `use-partyroom-detail.ts`

```ts
// use-partyrooms-list.ts
import { useQuery, keepPreviousData } from "@tanstack/react-query"
import { listPartyrooms } from "./partyrooms-api"
import type { PartyroomsListQuery } from "../model/filter-schema"

export function usePartyroomsList(query: PartyroomsListQuery) {
  return useQuery({
    queryKey: ["partyrooms", "list", query],
    queryFn: () => listPartyrooms(query),
    placeholderData: keepPreviousData,
  })
}

// use-partyroom-detail.ts
import { useQuery } from "@tanstack/react-query"
import { getPartyroomDetail } from "./partyrooms-api"

export function usePartyroomDetail(partyroomId: number) {
  return useQuery({
    queryKey: ["partyrooms", "detail", partyroomId],
    queryFn: () => getPartyroomDetail(partyroomId),
    enabled: Number.isFinite(partyroomId) && partyroomId > 0,
  })
}
```

### Task 5.4: G5 commit

- [ ] **Step 1: 회귀**

```bash
pnpm test:run
```

Expected: 48 + 9 = 57 PASS (filter zod 4 + api 5).

- [ ] **Step 2: commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(partyrooms): list/detail API + filter zod + sort 화이트리스트 + react-query (PR 14b G5)

- features/partyrooms/model/filter-schema.ts (PartyroomStatusEnum + StageTypeEnum + PartyroomSortEnum 10 옵션 + cross-field date range)
- features/partyrooms/api/partyrooms-api.ts (raw Page<T>, no unwrap — backend wrap 비대칭)
- features/partyrooms/api/use-{partyrooms-list,partyroom-detail}.ts

테스트: 9 신규 (zod 4 + api 5) — 57/57 PASS

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Chunk 6: G6 — `widgets/partyrooms-list` + `pages/partyrooms-page` + integration

### Task 6.1: `partyrooms-filter-form.tsx` + 테스트

```tsx
import { useEffect, useState } from "react"
import { Input } from "@/components/ui/input"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { Button } from "@/components/ui/button"
import { Info } from "lucide-react"
import { useDebounce } from "@/shared/lib/use-debounce"
import type { PartyroomsListQuery, PartyroomStatus, StageType, PartyroomSort } from "../model/filter-schema"

const STATUS_OPTIONS: { value: PartyroomStatus; label: string }[] = [
  { value: "ACTIVE", label: "활동 중" },
  { value: "SUSPENDED", label: "일시 중단" },
  { value: "TERMINATED", label: "종료" },
]

const STAGE_OPTIONS: { value: StageType; label: string }[] = [
  { value: "MAIN", label: "MAIN" },
  { value: "GENERAL", label: "GENERAL" },
]

const SORT_OPTIONS: { value: PartyroomSort; label: string }[] = [
  { value: "createdAt,desc", label: "생성일 ↓" },
  { value: "createdAt,asc", label: "생성일 ↑" },
  { value: "lastActivityAt,desc", label: "마지막 활동 ↓" },
  { value: "lastActivityAt,asc", label: "마지막 활동 ↑" },
  { value: "crewCount,desc", label: "크루수 ↓" },
  { value: "crewCount,asc", label: "크루수 ↑" },
  { value: "title,desc", label: "제목 ↓" },
  { value: "title,asc", label: "제목 ↑" },
  { value: "hostNickname,desc", label: "호스트 ↓" },
  { value: "hostNickname,asc", label: "호스트 ↑" },
]

interface Props {
  query: PartyroomsListQuery
  onChange: (next: Partial<PartyroomsListQuery>) => void
  onReset: () => void
}

export function PartyroomsFilterForm({ query, onChange, onReset }: Props) {
  const [hostDraft, setHostDraft] = useState(query.host ?? "")
  const debouncedHost = useDebounce(hostDraft, 300)

  useEffect(() => {
    if (debouncedHost !== (query.host ?? "")) {
      onChange({ host: debouncedHost && debouncedHost.length >= 2 ? debouncedHost : undefined, page: 0 })
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [debouncedHost])

  return (
    <div className="flex flex-wrap gap-2 items-end mb-4">
      <div>
        <label className="block text-xs font-medium mb-1 flex items-center gap-1">
          상태 <Info className="h-3 w-3 text-muted-foreground" aria-label="기본 보기는 종료 룸을 제외합니다" />
        </label>
        <Select
          value={query.status ?? "ALL"}
          onValueChange={(v) => onChange({ status: v === "ALL" ? undefined : (v as PartyroomStatus), page: 0 })}
        >
          <SelectTrigger className="w-32"><SelectValue /></SelectTrigger>
          <SelectContent>
            <SelectItem value="ALL">전체 (종료 제외)</SelectItem>
            {STATUS_OPTIONS.map(o => <SelectItem key={o.value} value={o.value}>{o.label}</SelectItem>)}
          </SelectContent>
        </Select>
      </div>
      <div>
        <label className="block text-xs font-medium mb-1">스테이지</label>
        <Select
          value={query.stageType ?? "ALL"}
          onValueChange={(v) => onChange({ stageType: v === "ALL" ? undefined : (v as StageType), page: 0 })}
        >
          <SelectTrigger className="w-28"><SelectValue /></SelectTrigger>
          <SelectContent>
            <SelectItem value="ALL">전체</SelectItem>
            {STAGE_OPTIONS.map(o => <SelectItem key={o.value} value={o.value}>{o.label}</SelectItem>)}
          </SelectContent>
        </Select>
      </div>
      <div>
        <label className="block text-xs font-medium mb-1">호스트 (이메일/닉네임)</label>
        <Input
          value={hostDraft}
          onChange={(e) => setHostDraft(e.target.value)}
          placeholder="2자 이상 부분일치"
          className="w-48"
        />
      </div>
      <div>
        <label className="block text-xs font-medium mb-1">정렬</label>
        <Select
          value={query.sort}
          onValueChange={(v) => onChange({ sort: v as PartyroomSort, page: 0 })}
        >
          <SelectTrigger className="w-44"><SelectValue /></SelectTrigger>
          <SelectContent>
            {SORT_OPTIONS.map(o => <SelectItem key={o.value} value={o.value}>{o.label}</SelectItem>)}
          </SelectContent>
        </Select>
      </div>
      <Button variant="outline" onClick={onReset}>초기화</Button>
    </div>
  )
}
```

- [ ] **Step 1: 테스트 — host debounce + status info + sort 즉시 + 초기화**

```tsx
import { describe, it, expect, vi } from "vitest"
import { render, screen, fireEvent, waitFor } from "@testing-library/react"
import { PartyroomsFilterForm } from "../partyrooms-filter-form"
import type { PartyroomsListQuery } from "../../model/filter-schema"

const baseQuery: PartyroomsListQuery = { page: 0, size: 50, sort: "createdAt,desc" }

describe("PartyroomsFilterForm", () => {
  it("호스트 입력 → 300ms 후 onChange (2자 이상)", async () => {
    vi.useFakeTimers()
    const onChange = vi.fn()
    render(<PartyroomsFilterForm query={baseQuery} onChange={onChange} onReset={vi.fn()} />)
    fireEvent.change(screen.getByPlaceholderText(/2자 이상/), { target: { value: "ali" } })
    vi.advanceTimersByTime(300)
    await waitFor(() => expect(onChange).toHaveBeenCalledWith({ host: "ali", page: 0 }))
    vi.useRealTimers()
  })

  it("정렬 dropdown → 즉시 onChange", async () => {
    // shadcn Select 패턴 — combobox 클릭으로 열고 option role로 선택 (G3 Task 3.2 동일 idiom)
    const user = userEvent.setup()
    const onChange = vi.fn()
    render(<PartyroomsFilterForm query={baseQuery} onChange={onChange} onReset={vi.fn()} />)
    await user.click(screen.getByRole("combobox", { name: /정렬/ }))
    await user.click(await screen.findByRole("option", { name: "크루수 ↓" }))
    expect(onChange).toHaveBeenCalledWith({ sort: "crewCount,desc", page: 0 })
  })

  it("초기화 → onReset", async () => {
    const user = userEvent.setup()
    const onReset = vi.fn()
    render(<PartyroomsFilterForm query={baseQuery} onChange={vi.fn()} onReset={onReset} />)
    await user.click(screen.getByText("초기화"))
    expect(onReset).toHaveBeenCalled()
  })
})
```

> Note: 테스트 파일 상단에 `import userEvent from "@testing-library/user-event"` 추가.

- [ ] **Step 2: 통과**

Expected: 3/3 PASS.

### Task 6.2: `partyrooms-table.tsx` + 테스트

```tsx
import { useNavigate } from "react-router-dom"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import { Badge } from "@/components/ui/badge"
import { Skeleton } from "@/components/ui/skeleton"
import type { AdminPartyroomListItem } from "@/entities/partyroom"

const STATUS_VARIANT: Record<string, "default" | "secondary" | "destructive" | "outline"> = {
  ACTIVE: "default", SUSPENDED: "secondary", TERMINATED: "destructive",
}

function formatKst(iso: string | null): string {
  return iso ? new Date(iso).toLocaleString("ko-KR", { hour12: false }) : "-"
}

interface Props {
  rows: AdminPartyroomListItem[]
  isLoading: boolean
  isEmpty: boolean
}

export function PartyroomsTable({ rows, isLoading, isEmpty }: Props) {
  const navigate = useNavigate()
  if (isLoading) {
    return <div className="space-y-2">{Array.from({ length: 5 }).map((_, i) => <Skeleton key={i} className="h-12 w-full" />)}</div>
  }
  if (isEmpty) {
    return <div className="text-center py-12 text-muted-foreground">조건에 맞는 파티룸이 없습니다</div>
  }
  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>ID</TableHead>
          <TableHead>제목</TableHead>
          <TableHead>스테이지</TableHead>
          <TableHead>호스트</TableHead>
          <TableHead>크루</TableHead>
          <TableHead>DJ</TableHead>
          <TableHead>재생</TableHead>
          <TableHead>상태</TableHead>
          <TableHead>표시</TableHead>
          <TableHead>생성일</TableHead>
          <TableHead>마지막 활동</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {rows.map((row) => (
          <TableRow
            key={row.partyroomId}
            className="cursor-pointer hover:bg-accent/50"
            onClick={() => navigate(`/partyrooms/${row.partyroomId}`)}
          >
            <TableCell>{row.partyroomId}</TableCell>
            <TableCell>{row.title}</TableCell>
            <TableCell>{row.stageType}</TableCell>
            <TableCell>{row.hostNickname ?? `#${row.hostUserAccountId}`}</TableCell>
            <TableCell>{row.crewCount}</TableCell>
            <TableCell>{row.djCount}</TableCell>
            <TableCell>{row.playbackActivated ? "ON" : "OFF"}</TableCell>
            <TableCell><Badge variant={STATUS_VARIANT[row.status] ?? "outline"}>{row.status}</Badge></TableCell>
            <TableCell>{row.displayFlag}</TableCell>
            <TableCell>{formatKst(row.createdAt)}</TableCell>
            <TableCell>{formatKst(row.lastActivityAt)}</TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  )
}
```

- [ ] **Step 1: 테스트 — empty/loaded/row click**

```tsx
import { describe, it, expect } from "vitest"
import { render, screen, fireEvent } from "@testing-library/react"
import { MemoryRouter, Routes, Route } from "react-router-dom"
import { PartyroomsTable } from "../partyrooms-table"
import { partyroomListItemFixture } from "@/test/mocks/fixtures/partyrooms"

describe("PartyroomsTable", () => {
  it("loaded — title + status badge", () => {
    render(<MemoryRouter><PartyroomsTable rows={[partyroomListItemFixture]} isLoading={false} isEmpty={false} /></MemoryRouter>)
    expect(screen.getByText(partyroomListItemFixture.title)).toBeInTheDocument()
    expect(screen.getByText("ACTIVE")).toBeInTheDocument()
  })
  it("empty 메시지", () => {
    render(<MemoryRouter><PartyroomsTable rows={[]} isLoading={false} isEmpty={true} /></MemoryRouter>)
    expect(screen.getByText(/파티룸이 없습니다/)).toBeInTheDocument()
  })
  it("row click → /partyrooms/:id", () => {
    render(
      <MemoryRouter initialEntries={["/partyrooms"]}>
        <Routes>
          <Route path="/partyrooms" element={<PartyroomsTable rows={[partyroomListItemFixture]} isLoading={false} isEmpty={false} />} />
          <Route path="/partyrooms/:id" element={<div>detail</div>} />
        </Routes>
      </MemoryRouter>,
    )
    fireEvent.click(screen.getByText(partyroomListItemFixture.title))
    expect(screen.getByText("detail")).toBeInTheDocument()
  })
})
```

Expected: 3/3 PASS.

### Task 6.3: `widgets/partyrooms-list.tsx`

(`widgets/members-list.tsx`와 동일 구조 — schema/hook/widget 이름만 교체)

```tsx
import { useSearchParams } from "react-router-dom"
import { toast } from "sonner"
import { partyroomsListQuerySchema, type PartyroomsListQuery } from "@/features/partyrooms/model/filter-schema"
import { usePartyroomsList } from "@/features/partyrooms/api/use-partyrooms-list"
import { PartyroomsFilterForm } from "@/features/partyrooms/ui/partyrooms-filter-form"
import { PartyroomsTable } from "@/features/partyrooms/ui/partyrooms-table"
import { parseSearchParams, stripInvalidParams, serializeQuery } from "@/shared/lib/url-state"
import { Pagination } from "@/widgets/pagination"
import { ApiError } from "@/shared/api/error"

export function PartyroomsListWidget() {
  const [params, setParams] = useSearchParams()
  const parsed = parseSearchParams(partyroomsListQuerySchema, params)

  if (!parsed.success) {
    setParams(stripInvalidParams(params, parsed.error), { replace: true })
    toast.error("필터 일부가 잘못돼 무시했어요")
    return null
  }

  const query: PartyroomsListQuery = parsed.data
  const { data, isLoading, error } = usePartyroomsList(query)

  const updateQuery = (next: Partial<PartyroomsListQuery>) => {
    setParams(serializeQuery({ ...query, ...next } as Record<string, unknown>))
  }
  const reset = () => setParams(new URLSearchParams())
  const goToPage = (page: number) => updateQuery({ page })

  return (
    <div className="p-6 lg:p-8">
      <div className="mb-6 flex items-center justify-between">
        <h2 className="text-2xl font-bold">파티룸</h2>
        {data && <p className="text-sm text-muted-foreground">총 {data.totalElements}건</p>}
      </div>
      <PartyroomsFilterForm query={query} onChange={updateQuery} onReset={reset} />
      {error instanceof ApiError && error.status === 403 && (
        <p className="text-destructive text-sm mb-2">이 화면을 볼 권한이 없습니다</p>
      )}
      {error instanceof ApiError && error.status === 400 && (
        <p className="text-destructive text-sm mb-2">{error.message}</p>
      )}
      <PartyroomsTable rows={data?.content ?? []} isLoading={isLoading} isEmpty={!isLoading && (data?.empty ?? false)} />
      {data && (
        <Pagination page={data.number} totalPages={data.totalPages} totalElements={data.totalElements} onChange={goToPage} />
      )}
    </div>
  )
}
```

### Task 6.4: `pages/partyrooms-page.tsx` (placeholder 교체)

```tsx
import { PartyroomsListWidget } from "@/widgets/partyrooms-list"

export function PartyroomsPage() {
  return <PartyroomsListWidget />
}
```

### Task 6.5: integration test — `/partyrooms`

```tsx
import { describe, it, expect } from "vitest"
import { render, screen, waitFor } from "@testing-library/react"
import { MemoryRouter, Routes, Route } from "react-router-dom"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { Toaster } from "sonner"
import { PartyroomsPage } from "../partyrooms-page"
import { partyroomListItemFixture } from "@/test/mocks/fixtures/partyrooms"

function wrap(initial = "/partyrooms") {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  return render(
    <QueryClientProvider client={qc}>
      <MemoryRouter initialEntries={[initial]}>
        <Routes>
          <Route path="/partyrooms" element={<PartyroomsPage />} />
          <Route path="/partyrooms/:partyroomId" element={<div data-testid="detail">detail</div>} />
        </Routes>
        <Toaster />
      </MemoryRouter>
    </QueryClientProvider>,
  )
}

describe("PartyroomsPage integration", () => {
  it("happy: list 로드 → row 노출", async () => {
    wrap()
    await waitFor(() => expect(screen.getByText(partyroomListItemFixture.title)).toBeInTheDocument())
  })

  it("URL `/partyrooms?sort=invalid_xyz` → zod 화이트리스트 차단 → invalid 필드 drop + toast", async () => {
    // 화이트리스트 위반은 PartyroomSortEnum (zod)에서 차단 → stripInvalidParams + toast.
    // backend 400 path는 G5 unit test (api 레벨)에서 검증함 — 여기선 zod 차단만 확인.
    wrap("/partyrooms?sort=invalid_xyz")
    await waitFor(() => expect(screen.getByText(/잘못돼 무시했어요/)).toBeInTheDocument())
  })
})
```

> **Reality 보정 (G0.3 polish)**: 이전 Plan에서는 msw 핸들러에 `sort=invalid_*` → 400 분기를 두었으나 zod가 먼저 차단해 dead code가 됨. **G1 Task 1.3 Step 6에서 msw 400 분기 제거** 권장. backend 400 propagation은 G5 Task 5.2 unit test ("invalid sort → 400 ApiError")로 충분.

### Task 6.6: G6 commit

- [ ] **Step 1: 회귀**

```bash
pnpm test:run
```

Expected: 57 + 8 = 65 PASS (filter form 3 + table 3 + integration 2).

- [ ] **Step 2: commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(partyrooms): list page (filter form + table + pagination + URL 동기화) (PR 14b G6)

- features/partyrooms/ui/partyrooms-filter-form.tsx (status 툴팁 + host 2자+debounce + sort 10옵션)
- features/partyrooms/ui/partyrooms-table.tsx (11 컬럼, status badge, 빈/로딩/로드)
- widgets/partyrooms-list.tsx
- pages/partyrooms-page.tsx (placeholder → widget)

테스트: 8 신규 — 65/65 PASS

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Chunk 7: G7 — `partyroom-detail-cards` (header/meta/playback/crews/dj) + `widgets/partyrooms-detail` + `pages/partyroom-detail-page` + integration

**목표:** 파티룸 detail 첫 5 카드 (총 8 카드 중 5). 나머지 3 카드는 G8.

### Task 7.1: `features/partyrooms/ui/partyroom-detail-cards.tsx` (5/8 카드)

```tsx
import { Link } from "react-router-dom"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import type { AdminPartyroomDetail } from "@/entities/partyroom"

function formatKst(iso: string | null): string {
  return iso ? new Date(iso).toLocaleString("ko-KR", { hour12: false }) : "-"
}

const STATUS_VARIANT: Record<string, "default" | "secondary" | "destructive" | "outline"> = {
  ACTIVE: "default", SUSPENDED: "secondary", TERMINATED: "destructive",
}

interface Props {
  detail: AdminPartyroomDetail
}

export function PartyroomDetailCards({ detail }: Props) {
  return (
    <div className="space-y-4">
      {/* 1. Header */}
      <div className="flex items-center gap-3">
        <Link to="/partyrooms" className="text-sm text-muted-foreground">← 목록으로</Link>
        <h2 className="text-2xl font-bold">#{detail.partyroomId} {detail.title}</h2>
        <Badge variant={STATUS_VARIANT[detail.status] ?? "outline"}>{detail.status}</Badge>
        <Badge variant="outline">{detail.displayFlag}</Badge>
      </div>

      {/* 2. Top-level meta */}
      <Card>
        <CardHeader><CardTitle>기본 정보</CardTitle></CardHeader>
        <CardContent className="grid grid-cols-2 gap-3 text-sm">
          <div><span className="text-muted-foreground">스테이지</span><div>{detail.stageType}</div></div>
          <div><span className="text-muted-foreground">호스트</span><div>{detail.hostNickname ?? "-"} ({detail.hostEmail ?? "-"}) #{detail.hostUserAccountId}</div></div>
          <div><span className="text-muted-foreground">크루 수</span><div>{detail.crewCount}</div></div>
          <div><span className="text-muted-foreground">마지막 활동</span><div>{formatKst(detail.lastActivityAt)}</div></div>
        </CardContent>
      </Card>

      {/* 3. Playback */}
      <Card>
        <CardHeader><CardTitle>재생 상태</CardTitle></CardHeader>
        <CardContent className="grid grid-cols-2 gap-3 text-sm">
          <div><span className="text-muted-foreground">활성</span><div>{detail.playback.activated ? "ON" : "OFF"}</div></div>
          <div><span className="text-muted-foreground">현재 트랙</span><div>{detail.playback.currentTrackName ?? "-"}</div></div>
          <div><span className="text-muted-foreground">현재 DJ crew</span><div>{detail.playback.currentDjCrewId ?? "-"}</div></div>
        </CardContent>
      </Card>

      {/* 4. Crews */}
      <Card>
        <CardHeader><CardTitle>크루 ({detail.crews.length})</CardTitle></CardHeader>
        <CardContent>
          {detail.crews.length === 0
            ? <p className="text-sm text-muted-foreground">크루 없음</p>
            : (
              <Table>
                <TableHeader>
                  <TableRow>
                    <TableHead>crewId</TableHead>
                    <TableHead>memberId</TableHead>
                    <TableHead>등급</TableHead>
                    <TableHead>닉네임</TableHead>
                    <TableHead>입장 시각</TableHead>
                  </TableRow>
                </TableHeader>
                <TableBody>
                  {detail.crews.map((c) => (
                    <TableRow key={c.crewId}>
                      <TableCell>{c.crewId}</TableCell>
                      <TableCell>{c.memberId}</TableCell>
                      <TableCell>{c.gradeType}</TableCell>
                      <TableCell>{c.nickname ?? "-"}</TableCell>
                      <TableCell>{formatKst(c.enteredAt)}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            )}
        </CardContent>
      </Card>

      {/* 5. DJ queue */}
      <Card>
        <CardHeader><CardTitle>DJ 큐 ({detail.djQueue.length})</CardTitle></CardHeader>
        <CardContent>
          {detail.djQueue.length === 0
            ? <p className="text-sm text-muted-foreground">DJ 없음</p>
            : (
              <Table>
                <TableHeader>
                  <TableRow>
                    <TableHead>djId</TableHead>
                    <TableHead>crewId</TableHead>
                    <TableHead>플레이리스트</TableHead>
                    <TableHead>순서</TableHead>
                  </TableRow>
                </TableHeader>
                <TableBody>
                  {detail.djQueue.map((d) => (
                    <TableRow key={d.djId}>
                      <TableCell>{d.djId}</TableCell>
                      <TableCell>{d.crewId}</TableCell>
                      <TableCell>{d.playlistName ?? "-"}</TableCell>
                      <TableCell>{d.orderNumber}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            )}
        </CardContent>
      </Card>

      {/* 6/7/8 카드 — G8에서 추가 */}
    </div>
  )
}
```

- [ ] **Step 1: 테스트 — playback null fallback / crew empty**

```tsx
import { describe, it, expect } from "vitest"
import { render, screen } from "@testing-library/react"
import { MemoryRouter } from "react-router-dom"
import { PartyroomDetailCards } from "../partyroom-detail-cards"
import { partyroomDetailFixture } from "@/test/mocks/fixtures/partyrooms"

describe("PartyroomDetailCards (G7 partial — 5/8 카드)", () => {
  it("currentTrackName=null → '-' fallback", () => {
    render(<MemoryRouter><PartyroomDetailCards detail={partyroomDetailFixture} /></MemoryRouter>)
    expect(screen.getByText("-")).toBeInTheDocument()
  })

  it("crews 빈 배열 → '크루 없음'", () => {
    render(
      <MemoryRouter>
        <PartyroomDetailCards detail={{ ...partyroomDetailFixture, crews: [] }} />
      </MemoryRouter>,
    )
    expect(screen.getByText("크루 없음")).toBeInTheDocument()
  })

  it("status=TERMINATED → destructive badge", () => {
    render(
      <MemoryRouter>
        <PartyroomDetailCards detail={{ ...partyroomDetailFixture, status: "TERMINATED" }} />
      </MemoryRouter>,
    )
    expect(screen.getByText("TERMINATED")).toBeInTheDocument()
  })
})
```

Expected: 3/3 PASS.

### Task 7.2: `widgets/partyrooms-detail.tsx`

(`widgets/members-detail.tsx`와 동일 패턴 — useParams + skeleton + 404 NotFoundView)

```tsx
import { useParams, Link } from "react-router-dom"
import { usePartyroomDetail } from "@/features/partyrooms/api/use-partyroom-detail"
import { PartyroomDetailCards } from "@/features/partyrooms/ui/partyroom-detail-cards"
import { Skeleton } from "@/components/ui/skeleton"
import { Button } from "@/components/ui/button"
import { ApiError } from "@/shared/api/error"

export function PartyroomsDetailWidget() {
  const { partyroomId } = useParams<{ partyroomId: string }>()
  const id = Number(partyroomId)
  const { data, isLoading, error } = usePartyroomDetail(id)

  if (Number.isNaN(id) || id <= 0) return <NotFoundView />
  if (isLoading) {
    return (
      <div className="p-6 lg:p-8 space-y-4">
        <Skeleton className="h-8 w-48" />
        <Skeleton className="h-32 w-full" />
        <Skeleton className="h-32 w-full" />
      </div>
    )
  }
  if (error instanceof ApiError && error.status === 404) return <NotFoundView />
  if (!data) return null

  return (
    <div className="p-6 lg:p-8">
      <PartyroomDetailCards detail={data} />
    </div>
  )
}

function NotFoundView() {
  return (
    <div className="p-6 lg:p-8">
      <p className="text-muted-foreground mb-4">존재하지 않는 파티룸입니다</p>
      <Button asChild variant="outline"><Link to="/partyrooms">목록으로</Link></Button>
    </div>
  )
}
```

### Task 7.3: `pages/partyroom-detail-page.tsx` (placeholder 교체)

```tsx
import { PartyroomsDetailWidget } from "@/widgets/partyrooms-detail"

export function PartyroomDetailPage() {
  return <PartyroomsDetailWidget />
}
```

### Task 7.4: integration test — `/partyrooms/:id`

```tsx
import { describe, it, expect } from "vitest"
import { render, screen, waitFor } from "@testing-library/react"
import { MemoryRouter, Routes, Route } from "react-router-dom"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { PartyroomDetailPage } from "../partyroom-detail-page"
import { partyroomDetailFixture } from "@/test/mocks/fixtures/partyrooms"

function wrap(initial: string) {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  return render(
    <QueryClientProvider client={qc}>
      <MemoryRouter initialEntries={[initial]}>
        <Routes>
          <Route path="/partyrooms/:partyroomId" element={<PartyroomDetailPage />} />
          <Route path="/partyrooms" element={<div>list</div>} />
        </Routes>
      </MemoryRouter>
    </QueryClientProvider>,
  )
}

describe("PartyroomDetailPage integration (G7)", () => {
  it("happy: /partyrooms/1 → 5 카드 노출", async () => {
    wrap("/partyrooms/1")
    await waitFor(() => expect(screen.getByText(`#${partyroomDetailFixture.partyroomId} ${partyroomDetailFixture.title}`)).toBeInTheDocument())
  })

  it("404: /partyrooms/9999 → '존재하지 않는 파티룸'", async () => {
    wrap("/partyrooms/9999")
    await waitFor(() => expect(screen.getByText(/존재하지 않는 파티룸/)).toBeInTheDocument())
  })
})
```

### Task 7.5: G7 commit

- [ ] **Step 1: 회귀**

Expected: 65 + 5 = 70 PASS (cards 3 + integration 2).

- [ ] **Step 2: commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(partyrooms): detail 5/8 카드 (header/meta/playback/crews/dj) + 404 view (PR 14b G7)

- features/partyrooms/ui/partyroom-detail-cards.tsx (header + meta + playback null fallback + crews/dj 빈 상태)
- widgets/partyrooms-detail.tsx
- pages/partyroom-detail-page.tsx (placeholder → widget)

penalties/reports/admin-actions 3 카드는 G8에서 추가.

테스트: 5 신규 — 70/70 PASS

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Chunk 8: G8 — partyroom detail 나머지 3 카드 (penalties / reports / admin actions) + 회귀 점검

### Task 8.1: 3 카드 추가

`partyroom-detail-cards.tsx` 끝에 추가:

```tsx
{/* 6. Recent penalties */}
<Card>
  <CardHeader><CardTitle>최근 페널티 (top 5)</CardTitle></CardHeader>
  <CardContent>
    {detail.recentPenalties.length === 0
      ? <p className="text-sm text-muted-foreground">최근 페널티 없음</p>
      : (
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead>id</TableHead>
              <TableHead>crewId</TableHead>
              <TableHead>유형</TableHead>
              <TableHead>가한 주체</TableHead>
              <TableHead>사유</TableHead>
              <TableHead>일시</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {detail.recentPenalties.map((p) => (
              <TableRow key={p.id}>
                <TableCell>{p.id}</TableCell>
                <TableCell>{p.crewId}</TableCell>
                <TableCell>{p.penaltyType}</TableCell>
                <TableCell>{p.punisherType}</TableCell>
                <TableCell className="max-w-xs truncate" title={p.reason}>{p.reason}</TableCell>
                <TableCell>{formatKst(p.date)}</TableCell>
              </TableRow>
            ))}
          </TableBody>
        </Table>
      )}
  </CardContent>
</Card>

{/* 7. Recent reports */}
<Card>
  <CardHeader><CardTitle>최근 신고</CardTitle></CardHeader>
  <CardContent>
    {detail.recentReports.length === 0
      ? <p className="text-sm text-muted-foreground">신고 내역 없음</p>
      : (
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead>id</TableHead>
              <TableHead>분류</TableHead>
              <TableHead>상태</TableHead>
              <TableHead>신고자</TableHead>
              <TableHead>일시</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {detail.recentReports.map((r) => (
              <TableRow key={r.id}>
                <TableCell>{r.id}</TableCell>
                <TableCell>{r.category}</TableCell>
                <TableCell>{r.status}</TableCell>
                <TableCell>#{r.reporterUserAccountId}</TableCell>
                <TableCell>{formatKst(r.createdAt)}</TableCell>
              </TableRow>
            ))}
          </TableBody>
        </Table>
      )}
  </CardContent>
</Card>

{/* 8. Recent admin actions */}
<Card>
  <CardHeader><CardTitle>최근 관리자 액션</CardTitle></CardHeader>
  <CardContent>
    {detail.recentAdminActions.length === 0
      ? <p className="text-sm text-muted-foreground">최근 관리자 액션 없음</p>
      : (
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead>actionId</TableHead>
              <TableHead>유형</TableHead>
              <TableHead>관리자</TableHead>
              <TableHead>일시</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {detail.recentAdminActions.map((a) => (
              <TableRow key={a.actionId}>
                <TableCell>{a.actionId}</TableCell>
                <TableCell>{a.actionType}</TableCell>
                <TableCell>#{a.administratorId}</TableCell>
                <TableCell>{formatKst(a.occurredAt)}</TableCell>
              </TableRow>
            ))}
          </TableBody>
        </Table>
      )}
  </CardContent>
</Card>
```

### Task 8.2: 테스트 추가 — 3 빈 상태 + 1 채워진 row

`partyroom-detail-cards.test.tsx`에 추가:

```tsx
it("recentPenalties / recentReports / recentAdminActions 모두 빈 → 각 빈 상태 메시지", () => {
  render(
    <MemoryRouter>
      <PartyroomDetailCards detail={{
        ...partyroomDetailFixture,
        recentPenalties: [], recentReports: [], recentAdminActions: [],
      }} />
    </MemoryRouter>,
  )
  expect(screen.getByText("최근 페널티 없음")).toBeInTheDocument()
  expect(screen.getByText("신고 내역 없음")).toBeInTheDocument()
  expect(screen.getByText("최근 관리자 액션 없음")).toBeInTheDocument()
})

it("recentReports 채움 → 행 노출", () => {
  render(
    <MemoryRouter>
      <PartyroomDetailCards detail={{
        ...partyroomDetailFixture,
        recentReports: [{ id: 1, category: "ABUSE", status: "PENDING", reporterUserAccountId: 99, createdAt: "2026-04-28T10:00:00" }],
      }} />
    </MemoryRouter>,
  )
  expect(screen.getByText("ABUSE")).toBeInTheDocument()
  expect(screen.getByText("#99")).toBeInTheDocument()
})
```

Expected: 추가 2 PASS (cards 누적 5 PASS).

### Task 8.3: detail integration 회귀 — 빈 reports + 채워진 reports

`partyroom-detail-page.test.tsx` 추가:

```tsx
it("빈 reports — '신고 내역 없음'", async () => {
  // fixture 기본이 빈 reports/penalties/adminActions
  wrap("/partyrooms/1")
  await waitFor(() => expect(screen.getByText("신고 내역 없음")).toBeInTheDocument())
})
```

(이미 G7 happy 테스트가 fixture 채워서 노출 검증함 — penalties/reports/adminActions 빈 상태 노출만 추가)

### Task 8.4: 전체 회귀 회로

- [ ] **Step 1: pnpm test:run + tsc + build**

```bash
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```

Expected: 70 + 3 = 73 PASS, 0 type error, 빌드 OK.

### Task 8.5: G8 commit

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(partyrooms): detail 6~8/8 카드 (penalties/reports/admin-actions) + 빈 상태 (PR 14b G8)

- features/partyrooms/ui/partyroom-detail-cards.tsx 3 카드 추가 (top 5 페널티 / 신고 / admin 액션)
- 모든 빈 상태 메시지 명시 ("최근 페널티 없음" / "신고 내역 없음" / "최근 관리자 액션 없음")

테스트: 3 신규 (cards 2 + integration 1) — 73/73 PASS

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Chunk 9: G9 — §14 Implementation Reality catch-up + §15 Future Polish backfill + 14a §13.2 forward-evolution

**목표:** spec ↔ 실제 코드 deviation을 §12에 backfill, 14a §13.2 demo 의존성 정리 SHA를 14a spec에 backfill, 14b §15 future polish 누적 정리.

### Task 9.1: §14 catch-up — chunk 진행 중 발견된 deviation 정리

각 chunk에서 spec과 실제 코드가 어긋난 항목을 SHA + 사유 + impact로 backfill. 다음은 예상 항목 (실제는 G1~G8 진행 중 정리):

- **[G1 SHA] msw mocks/handlers 디렉토리 분할 + fixtures 별 위치** — spec §3.1 `mocks/handlers/{members,partyrooms}.ts` 명시. 실제 추가로 `mocks/fixtures/{members,partyrooms}.ts`도 신규 — fixture 재사용성 위해 분리.
- **[G1 SHA] react-query devtools 추가** — spec §11에 명시했으나 dev only로 배포 영향 없음. main.tsx에 conditional render.
- **[G1 SHA] use-debounce.ts** — spec §3.1 미명시. members/partyrooms 양쪽 filter form에서 재사용하므로 `shared/lib/use-debounce.ts`로 추출.
- **[G1 SHA] api-client.ts grep 결과** — 사용처 (0 / 잔존)에 따라 결정. 잔존 시 §13 future polish로 미룸.
- **[G1 SHA] App.tsx 라우트 등록 timing** — spec §3.3은 모든 라우트 추가 명시. G1에서 placeholder 페이지 4개로 일괄 등록 (사이드바 메뉴 클릭 시 404 방지).
- **[G3 SHA] widgets/pagination.tsx 별 컴포넌트 추출** — spec 본문에 미명시. members + partyrooms 양쪽 사용으로 widget 레벨 단일화.
- **[G3 SHA] MembersListWidget invalid URL params 처리** — `return null` 후 `setParams` replace 흐름 (spec §7과 일치).
- **[G7 SHA] DisplayFlag/GradeType 등 enum** — backend 코드 grep 후 backfill (string으로 받되 unknown fallback 적용).
- (그 외 진행 중 발견 항목 채움)

`docs/specs/2026-04-29-admin-pr14b-design.md` §14에 backfill commit:

```bash
# §14 안의 placeholder 항목들을 실제 SHA + 사유로 채워 commit
```

### Task 9.2: 14a §13.2 forward-evolution backfill — demo 의존성 정리 SHA

`docs/specs/2026-04-28-admin-pr14a-design.md` §13.2에 14b G1 SHA 추가:

```markdown
- demo `entities/{user,room}` / `features/{users,rooms}` / `widgets/{users,rooms}` / `pages/{users,rooms}-page.tsx` 일괄 삭제 — **PR 14b G1 SHA <hash>**
- demo `src/shared/lib/api-client.ts` — (Task 1.2 grep 결과에 따라) 14b G1에서 함께 삭제 / 잔존 보존 후 14c 이후 별도 정리 — **PR 14b G1 SHA <hash>**
```

### Task 9.3: §15 Future Polish 누적

§15.2에 G1~G8 진행 중 발견된 신규 미해결 추가 (예: enum 좁히기 backfill, virtual scroll 재고려 등).

### Task 9.4: 사이드바 final wiring 검증

수동:
```bash
pnpm dev
```

브라우저에서:
- 로그인 → 대시보드
- 사이드바 "회원" 클릭 → /members 진입 (list 노출)
- row 클릭 → /members/:id (detail 노출)
- 뒤로 → list 유지 (URL 동기화)
- 사이드바 "파티룸" 클릭 → /partyrooms (list)
- row 클릭 → /partyrooms/:id (8 카드 detail)
- 로그아웃 → /login (returnTo 동작)

문제 발견 시 별 polish commit.

### Task 9.5: 회귀 회로 (자동)

```bash
pnpm test:run && pnpm exec tsc --noEmit && pnpm build
```

Expected: 73/73 PASS, 0 type error, 빌드 OK.

### Task 9.6: G9 commit

```bash
git add -A
git commit -m "$(cat <<'EOF'
docs(spec): PR 14b §14 catch-up + §15 future polish + 14a §13.2 forward-evolution backfill (G9)

§14 Implementation Reality:
- mocks/handlers + fixtures 분할 패턴 [G1 SHA <hash>]
- shared/lib/use-debounce.ts 신규 (members/partyrooms 양쪽 재사용)
- App.tsx placeholder 라우트 G1 일괄 등록 (사이드바 click 404 방지)
- widgets/pagination.tsx 추출 (양쪽 도메인 단일화)
- partyroom enum (DisplayFlag/GradeType/PenaltyType/PartyroomAdminActionType) 실제 값 [G7/G8 SHA]
- api-client.ts grep 결과 반영

§15 Future Polish — 신규 누적 (각 항목 사유 명시).

PR 14a §13.2 backfill — demo 의존성 정리 SHA 채움.

수동 회로 OK: login → /members → detail → /partyrooms → detail → 로그아웃.
회귀: 73/73 PASS.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## 최종 검증 체크리스트

- [ ] 73/73 테스트 PASS
- [ ] tsc 0 error
- [ ] pnpm build 성공
- [ ] 사이드바 "회원" / "파티룸" enabled, "/users", "/rooms" 라우트 부재
- [ ] /members, /members/:id, /partyrooms, /partyrooms/:id 모두 동작
- [ ] URL 직링크 새로고침 안전 (상세 페이지 진입)
- [ ] filter 변경 → URL 동기화 → 새 fetch
- [ ] 빈/탈퇴/404/403/400 상태 모두 명시 메시지
- [ ] demo 슬라이스 제거 완료 (또는 §14에 잔존 사유 backfill)
- [ ] spec §14 catch-up + §15 future polish 갱신
- [ ] 14a §13.2 forward-evolution backfill 완료

---

## 부록: 진행 방식

**Chunk 단위 사용자 confirm 흐름** (메모리 `feedback_pr_series_workflow.md` 패턴):

1. 각 chunk implementer subagent dispatch → spec compliance reviewer → code quality reviewer
2. 두 review pass 후 polish 결정 (사용자 confirm 또는 minor polish 자동 commit)
3. 다음 chunk 진행 전 사용자 confirm

**Atomic commit grouping**: 본 chunk 작업은 G<n>, code review polish는 같은 chunk 안 G<n>.1, G<n>.2 follow-up commit. Spec polish는 G0.x로 PR-14a 패턴 일관.

**브랜치**: `feature/admin-platform-frontend` — PR 14a~14d 통합 브랜치. 별 PR 없이 같은 브랜치에서 chunk 누적.
