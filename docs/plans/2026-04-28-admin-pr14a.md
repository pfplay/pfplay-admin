# PR 14a Implementation Plan — Admin Login + Protected Route + AuthStore + mustChangePassword

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Spec:** `docs/specs/2026-04-28-admin-pr14a-design.md`

**Goal:** pfplay-admin이 어드민 로그인을 받아 httpOnly 쿠키 기반 세션을 시작하고, 보호 라우트 안에서 빈 대시보드 + mustChangePassword 강제 변경 + 로그아웃까지 동작하는 첫 운영 가능 단위를 만든다.

**Architecture:** Feature-Sliced Design (shared → entities → features → widgets → pages). 인증 토큰은 백엔드 httpOnly 쿠키에 위임하고, 클라이언트는 비민감 메타(role, mustChangePassword)만 zustand persist + localStorage에 저장. 모든 변형 요청은 `shared/api/http.ts`가 `credentials: 'include'` + `XSRF-TOKEN` → `X-XSRF-TOKEN` echo + 401 → store.clear + hard redirect 로직을 단일 진입점으로 처리.

**Tech Stack:** React 18 + Vite 6 + TypeScript 5 (strict) + react-router-dom 7 + zustand 5 (persist) + @tanstack/react-query 5 + react-hook-form + zod + tailwindcss 4 + radix-ui + sonner. 신규: vitest 2 + @testing-library/react 16 + jsdom 25 + msw 2.

**Package Manager:** pnpm (lockfile `pnpm-lock.yaml`).

**Branch:** `feature/admin-platform-frontend` (장기 PR 14a~14d 통합 브랜치).

**Working dir:** `C:/Users/Eisen/Desktop/Labs/[projects] pfplay/pfplay-admin`.

---

## 0. Scope Check

PR 14a는 단일 sub-PR로 spec §1.1 목적만 다룸 (auth + 보호 라우트 + AuthStore + mustChangePassword). 분리 가능한 독립 sub-system 없음 — 단일 plan으로 진행. 14b/14c/14d는 별 plan에서 다룸.

## 1. File Structure

새로 생성하거나 수정할 파일 + 책임:

| 경로 | 종류 | 책임 |
|---|---|---|
| `package.json` | 수정 | devDependencies 추가, `test`/`test:ui` script 추가 |
| `vite.config.ts` | 수정 | `test` 블록 추가 (jsdom + setup files) |
| `src/test/setup.ts` | 신규 | RTL matchers + msw `server.listen()` lifecycle |
| `src/test/mocks/handlers.ts` | 신규 | msw 기본 핸들러 (login/logout/changePassword) |
| `src/test/mocks/server.ts` | 신규 | `setupServer(handlers)` 인스턴스 |
| `src/shared/config/env.ts` | 신규 | `VITE_API_BASE_URL` 단일 export |
| `src/shared/api/error.ts` | 신규 | `ApiError` 클래스 |
| `src/shared/api/csrf.ts` | 신규 | `getCsrfToken()` 쿠키 reader |
| `src/shared/api/http.ts` | 신규 | fetch wrapper (credentials, CSRF echo, 401 인터셉터) |
| `src/shared/api/__tests__/http.test.ts` | 신규 | unit test #4, #5 |
| `src/entities/session/model/types.ts` | 신규 | `AdminRole`, `SessionMeta`, `LoginCommand`, `AdminLoginResponseRaw` |
| `src/entities/session/model/store.ts` | 신규 | `useSessionStore` (zustand persist) |
| `src/entities/session/model/__tests__/store.test.ts` | 신규 | unit test #1, #2, #3 |
| `src/entities/session/api/session.ts` | 신규 | `login()`, `logout()`, `changePassword()` |
| `src/entities/session/index.ts` | 신규 | barrel export |
| `src/features/login/model/schema.ts` | 신규 | `loginSchema` zod |
| `src/features/login/api/use-login.ts` | 신규 | react-query mutation |
| `src/features/login/ui/login-form.tsx` | 신규 | RHF form |
| `src/features/login/__tests__/login-form.test.tsx` | 신규 | component test #6, #7 |
| `src/features/change-password/model/schema.ts` | 신규 | `changePasswordSchema` zod (10자+대소숫특) |
| `src/features/change-password/api/use-change-password.ts` | 신규 | mutation |
| `src/features/change-password/ui/change-password-form.tsx` | 신규 | RHF form |
| `src/features/change-password/__tests__/change-password-form.test.tsx` | 신규 | component test #8 |
| `src/features/logout/api/use-logout.ts` | 신규 | mutation + cleanup |
| `src/widgets/protected-route.tsx` | 신규 | gating 컴포넌트 |
| `src/pages/login-page.tsx` | 신규 | LoginForm wrapper, returnTo 처리 |
| `src/pages/login-page.test.tsx` | 신규 | integration test #9-12 |
| `src/pages/change-password-page.tsx` | 신규 | wrapper |
| `src/pages/change-password-page.test.tsx` | 신규 | integration test #15 |
| `src/pages/dashboard-page.tsx` | 신규 | placeholder |
| `src/widgets/__tests__/protected-route.test.tsx` | 신규 | integration test #13, #14 |
| `src/App.tsx` | 수정 | 라우트 등록 (login, password/change, /, scenarios/rooms/users 보호 wrapping) |
| `src/app/layout.tsx` | 수정 | 로그아웃 버튼 + 인증 사용자 표시 |

**FSD 슬라이스 경계**: shared(인프라) → entities(도메인 데이터+상태) → features(유저 시나리오) → widgets(컴포넌트 합성) → pages(라우트 호스트). 상위 레이어가 하위 레이어를 import (역방향 금지).

**테스트 위치**: 모듈 옆 `__tests__/` 또는 `*.test.ts(x)` co-located. vitest 기본 패턴.

---

## Chunk 1: G1 — shared 인프라 + 테스트 셋업

### Task 1.1: 의존성 추가

**Files:**
- Modify: `package.json` (devDependencies)
- Modify: `pnpm-lock.yaml` (자동)

- [ ] **Step 1: 디렉토리 확인**

```bash
ls "C:/Users/Eisen/Desktop/Labs/[projects] pfplay/pfplay-admin"
```
Expected: `package.json` 존재.

- [ ] **Step 2: 의존성 추가**

```bash
cd "C:/Users/Eisen/Desktop/Labs/[projects] pfplay/pfplay-admin" && \
pnpm add -D vitest@^2 @vitest/ui@^2 jsdom@^25 \
  @testing-library/react@^16 @testing-library/jest-dom@^6 @testing-library/user-event@^14 \
  msw@^2
```
Expected: `+ vitest@2.x.x`, `+ msw@2.x.x`, etc. lockfile 갱신.

- [ ] **Step 3: package.json scripts 추가**

`package.json`의 `"scripts"`에 추가:
```json
"test": "vitest",
"test:ui": "vitest --ui",
"test:run": "vitest run"
```

- [ ] **Step 4: 커밋 (chunk 1 시작 commit)**

```bash
git add package.json pnpm-lock.yaml
git commit -m "chore: add vitest + RTL + msw devDependencies (PR 14a G1)"
```

### Task 1.2: vite.config.ts에 test 블록 추가

**Files:**
- Modify: `vite.config.ts`

- [ ] **Step 1: 현재 파일 read**

`vite.config.ts`는 `defineConfig({ plugins, resolve, server })` 구조.

- [ ] **Step 2: test 블록 추가 + types 참조**

`defineConfig`의 import를 `vitest/config`에서 가져오도록 변경:
```ts
/// <reference types="vitest/config" />
import { defineConfig } from "vite"
import react from "@vitejs/plugin-react"
import tailwindcss from "@tailwindcss/vite"
import path from "path"

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: { "@": path.resolve(__dirname, "./src") },
  },
  server: { port: 3000 },
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: ["./src/test/setup.ts"],
    css: false,
  },
})
```

(triple-slash directive로 `vitest/config` 타입 인식. Vite 6 + Vitest 2 표준 패턴.)

### Task 1.3: src/test/setup.ts

**Files:**
- Create: `src/test/setup.ts`

- [ ] **Step 1: 디렉토리 생성 확인**

```bash
ls "C:/Users/Eisen/Desktop/Labs/[projects] pfplay/pfplay-admin/src"
```

- [ ] **Step 2: 작성**

```ts
import "@testing-library/jest-dom/vitest"
import { afterAll, afterEach, beforeAll } from "vitest"
import { server } from "./mocks/server"

beforeAll(() => server.listen({ onUnhandledRequest: "error" }))
afterEach(() => server.resetHandlers())
afterAll(() => server.close())
```

`onUnhandledRequest: "error"`로 누락 핸들러 잡기.

### Task 1.4: src/test/mocks/handlers.ts

**Files:**
- Create: `src/test/mocks/handlers.ts`

- [ ] **Step 1: 작성 (전체 핸들러 스켈레톤)**

```ts
import { http, HttpResponse } from "msw"
import { API_BASE_URL } from "../../shared/config/env"

const VALID_EMAIL = "admin@pfplay.xyz"
const VALID_PASSWORD = "ValidPass!1"

export const handlers = [
  http.post(`${API_BASE_URL}/api/v1/auth/admin/login`, async ({ request }) => {
    const body = (await request.json()) as { email: string; password: string }
    if (body.email !== VALID_EMAIL || body.password !== VALID_PASSWORD) {
      return HttpResponse.json(
        { status: 401, errorCode: "AUTH-INVALID", message: "invalid credentials" },
        { status: 401 },
      )
    }
    return HttpResponse.json(
      {
        data: {
          tokenType: "Cookie",
          expiresIn: 900,
          issuedAt: "2026-04-28T10:00:00",
          role: "ADMIN",
          mustChangePassword: false,
        },
      },
      { status: 200 },
    )
  }),

  http.post(`${API_BASE_URL}/api/v1/auth/admin/logout`, () => new HttpResponse(null, { status: 204 })),

  http.post(`${API_BASE_URL}/api/v1/admin/password/change`, () => new HttpResponse(null, { status: 204 })),
]
```

### Task 1.5: src/test/mocks/server.ts

**Files:**
- Create: `src/test/mocks/server.ts`

- [ ] **Step 1: 작성**

```ts
import { setupServer } from "msw/node"
import { handlers } from "./handlers"

export const server = setupServer(...handlers)
```

### Task 1.6: shared/config/env.ts

**Files:**
- Create: `src/shared/config/env.ts`

- [ ] **Step 1: 작성**

```ts
export const API_BASE_URL: string =
  import.meta.env.VITE_API_BASE_URL ?? "http://localhost:8080"
```

dev 기본값 명시. `.env.example`은 이미 `VITE_API_BASE_URL=https://api.pfplay.xyz` 보유.

### Task 1.7: shared/api/error.ts

**Files:**
- Create: `src/shared/api/error.ts`

- [ ] **Step 1: 작성**

```ts
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly errorCode: string,
    message: string,
  ) {
    super(message)
    this.name = "ApiError"
  }
}
```

### Task 1.8: shared/api/csrf.ts

**Files:**
- Create: `src/shared/api/csrf.ts`

- [ ] **Step 1: 작성**

```ts
export const CSRF_COOKIE_NAME = "XSRF-TOKEN"
export const CSRF_HEADER = "X-XSRF-TOKEN"

const COOKIE_REGEX = new RegExp(`(?:^|;\\s*)${CSRF_COOKIE_NAME}=([^;]+)`)

export function getCsrfToken(): string | null {
  if (typeof document === "undefined") return null
  const match = document.cookie.match(COOKIE_REGEX)
  return match ? decodeURIComponent(match[1]) : null
}
```

> **Chunk 순서 의존성**: `http.ts`는 `useSessionStore`를 import하므로 store 작성(Chunk 2) 이후에 만들어야 한다. 따라서 Chunk 1은 deps + 셋업 + store/http 의존 없는 shared 파일까지만 다루고, http.ts는 Chunk 3로 미룬다. 테스트도 마찬가지로 Chunk 3에서 작성.

### Task 1.9: Chunk 1 commit

- [ ] **Step 1: 변경 검증**

```bash
cd "C:/Users/Eisen/Desktop/Labs/[projects] pfplay/pfplay-admin" && pnpm test:run
```
Expected: "No test files found" 또는 0 tests run (정상 — 아직 test 미작성).

- [ ] **Step 2: 빌드 회귀 검증**

```bash
pnpm build
```
Expected: 기존 빌드 성공 그대로 유지.

- [ ] **Step 3: 커밋 (G1 atomic group)**

```bash
git add vite.config.ts src/test/setup.ts src/test/mocks/handlers.ts src/test/mocks/server.ts \
  src/shared/config/env.ts src/shared/api/error.ts src/shared/api/csrf.ts
git commit -m "feat(shared): vitest+msw 셋업 + shared/{config,api/{error,csrf}} (PR 14a G1)"
```

---

## Chunk 2: G2 — entities/session

### Task 2.1: types.ts

**Files:**
- Create: `src/entities/session/model/types.ts`

- [ ] **Step 1: 작성**

```ts
export type AdminRole = "SUPER_ADMIN" | "ADMIN"

export interface SessionMeta {
  role: AdminRole
  mustChangePassword: boolean
  issuedAt: string  // ISO
  expiresAt: string // ISO (issuedAt + expiresIn 초)
}

export interface LoginCommand {
  email: string
  password: string
}

export interface AdminLoginResponseRaw {
  tokenType: "Cookie"
  expiresIn: number          // 초
  issuedAt: string           // 백엔드 LocalDateTime — ISO 문자열로 직렬화 가정
  role: AdminRole
  mustChangePassword: boolean
}

export interface ChangePasswordCommand {
  currentPassword: string
  newPassword: string
}
```

### Task 2.2: store.test.ts (RED)

**Files:**
- Create: `src/entities/session/model/__tests__/store.test.ts`

- [ ] **Step 1: 테스트 작성 (RED)**

```ts
import { describe, it, expect, beforeEach } from "vitest"
import { useSessionStore } from "../store"
import type { SessionMeta } from "../types"

const META: SessionMeta = {
  role: "ADMIN",
  mustChangePassword: false,
  issuedAt: "2026-04-28T10:00:00.000Z",
  expiresAt: "2026-04-28T10:15:00.000Z",
}

describe("entities/session/store.ts", () => {
  beforeEach(() => {
    localStorage.clear()
    useSessionStore.getState().clear()
  })

  it("setSession → isAuthenticated=true + meta 저장 + localStorage persist", () => {
    useSessionStore.getState().setSession(META)
    expect(useSessionStore.getState().isAuthenticated).toBe(true)
    expect(useSessionStore.getState().meta).toEqual(META)
    expect(localStorage.getItem("pfplay-admin-session")).toContain("ADMIN")
  })

  it("clear → 초기화 + localStorage 비움 (state)", () => {
    useSessionStore.getState().setSession(META)
    useSessionStore.getState().clear()
    expect(useSessionStore.getState().isAuthenticated).toBe(false)
    expect(useSessionStore.getState().meta).toBeNull()
    const stored = localStorage.getItem("pfplay-admin-session")
    expect(stored ? JSON.parse(stored).state.isAuthenticated : false).toBe(false)
  })

  it("clearMustChangePassword → meta.mustChangePassword=false (다른 필드 보존)", () => {
    useSessionStore.getState().setSession({ ...META, mustChangePassword: true })
    useSessionStore.getState().clearMustChangePassword()
    expect(useSessionStore.getState().meta).toEqual({ ...META, mustChangePassword: false })
  })
})
```

- [ ] **Step 2: 실패 확인**

```bash
pnpm test:run src/entities/session
```
Expected: `Cannot find module '../store'` (RED).

### Task 2.3: store.ts 구현 (GREEN)

**Files:**
- Create: `src/entities/session/model/store.ts`

- [ ] **Step 1: 작성**

```ts
import { create } from "zustand"
import { persist, createJSONStorage } from "zustand/middleware"
import type { SessionMeta } from "./types"

interface SessionState {
  isAuthenticated: boolean
  meta: SessionMeta | null
  setSession: (meta: SessionMeta) => void
  clearMustChangePassword: () => void
  clear: () => void
}

export const useSessionStore = create<SessionState>()(
  persist(
    (set) => ({
      isAuthenticated: false,
      meta: null,
      setSession: (meta) => set({ isAuthenticated: true, meta }),
      clearMustChangePassword: () =>
        set((s) => (s.meta ? { meta: { ...s.meta, mustChangePassword: false } } : s)),
      clear: () => set({ isAuthenticated: false, meta: null }),
    }),
    {
      name: "pfplay-admin-session",
      storage: createJSONStorage(() => localStorage),
      version: 1,
    },
  ),
)
```

- [ ] **Step 2: GREEN 확인**

```bash
pnpm test:run src/entities/session
```
Expected: 3/3 PASS.

### Task 2.4: api/session.ts

**Files:**
- Create: `src/entities/session/api/session.ts`

> http.ts가 아직 없음. 이 파일은 http.ts 작성(Chunk 3) 후 함께 합치면 import error 발생 — **Task 2.4를 Chunk 3 직후로 미룸**. 여기서는 placeholder로 `// TODO: implement after http.ts` 주석만 (실제로는 빈 파일 생성 안 함).

→ **결정**: Task 2.4를 Chunk 3 Task 3.X로 이동.

### Task 2.5: index.ts (barrel)

**Files:**
- Create: `src/entities/session/index.ts`

- [ ] **Step 1: 작성** (api는 Chunk 3에서 추가)

```ts
export { useSessionStore } from "./model/store"
export type { AdminRole, SessionMeta, LoginCommand, ChangePasswordCommand } from "./model/types"
```

### Task 2.6: Chunk 2 commit

- [ ] **Step 1: 검증**

```bash
pnpm test:run
```
Expected: 3 tests PASS, 0 fail.

- [ ] **Step 2: 커밋**

```bash
git add src/entities/session/
git commit -m "feat(session): zustand persist store + types + barrel (PR 14a G2)"
```

---

## Chunk 3: G3a — shared/api/http.ts (with session import)

### Task 3.1: http.test.ts (RED)

**Files:**
- Create: `src/shared/api/__tests__/http.test.ts`

#### TDD Cycle: test #4 (401 인터셉터) + #5 (skip401Redirect 우회) + #16 (CSRF 헤더 echo)

- [ ] **Step 1: 작성**

```ts
// src/shared/api/__tests__/http.test.ts
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"
import { http as msw, HttpResponse } from "msw"
import { server } from "../../../test/mocks/server"
import { http } from "../http"
import { ApiError } from "../error"
import { API_BASE_URL } from "../../config/env"
import { useSessionStore } from "../../../entities/session/model/store"

const BASE_META = {
  role: "ADMIN" as const,
  mustChangePassword: false,
  issuedAt: "2026-04-28T10:00:00.000Z",
  expiresAt: "2026-04-28T10:15:00.000Z",
}

describe("shared/api/http.ts", () => {
  let originalLocation: Location
  beforeEach(() => {
    originalLocation = window.location
    Object.defineProperty(window, "location", {
      writable: true,
      value: { href: "", assign: vi.fn() } as unknown as Location,
    })
    document.cookie = "XSRF-TOKEN=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;"
    useSessionStore.getState().clear()
  })
  afterEach(() => {
    Object.defineProperty(window, "location", { writable: true, value: originalLocation })
  })

  it("401 응답 시 store.clear() + window.location.href = /login + ApiError throw", async () => {
    server.use(
      msw.get(`${API_BASE_URL}/api/v1/probe`, () =>
        HttpResponse.json({ status: 401, errorCode: "AUTH-EXPIRED", message: "expired" }, { status: 401 }),
      ),
    )
    useSessionStore.getState().setSession(BASE_META)

    await expect(http("/api/v1/probe")).rejects.toBeInstanceOf(ApiError)
    expect(useSessionStore.getState().isAuthenticated).toBe(false)
    expect(window.location.href).toBe("/login")
  })

  it("skip401Redirect: true 옵션 시 인터셉터 우회 — store 유지 + ApiError throw", async () => {
    server.use(
      msw.post(`${API_BASE_URL}/api/v1/auth/admin/login`, () =>
        HttpResponse.json({ status: 401, errorCode: "AUTH-INVALID", message: "invalid" }, { status: 401 }),
      ),
    )
    useSessionStore.getState().setSession(BASE_META)

    await expect(
      http("/api/v1/auth/admin/login", { method: "POST", body: { email: "x", password: "y" }, skip401Redirect: true }),
    ).rejects.toBeInstanceOf(ApiError)
    expect(useSessionStore.getState().isAuthenticated).toBe(true)
    expect(window.location.href).toBe("")
  })

  it("XSRF-TOKEN 쿠키 존재 시 변형 요청에 X-XSRF-TOKEN 헤더 echo", async () => {
    document.cookie = "XSRF-TOKEN=csrf-abc; path=/;"
    let receivedHeader: string | null = null
    server.use(
      msw.post(`${API_BASE_URL}/api/v1/admin/probe`, ({ request }) => {
        receivedHeader = request.headers.get("X-XSRF-TOKEN")
        return new HttpResponse(null, { status: 204 })
      }),
    )

    await http("/api/v1/admin/probe", { method: "POST", body: {} })
    expect(receivedHeader).toBe("csrf-abc")
  })
})
```

- [ ] **Step 2: 실패 확인 (RED)**

```bash
pnpm test:run src/shared/api
```
Expected: `Cannot find module '../http'` (RED).

### Task 3.2: http.ts 구현 (GREEN)

**Files:**
- Create: `src/shared/api/http.ts`

- [ ] **Step 1: 작성**

```ts
import { ApiError } from "./error"
import { CSRF_HEADER, getCsrfToken } from "./csrf"
import { API_BASE_URL } from "../config/env"
import { useSessionStore } from "../../entities/session/model/store"

export interface HttpOptions extends Omit<RequestInit, "body"> {
  body?: unknown
  /** Skip 401 → store.clear + redirect. Used by login (no session yet) */
  skip401Redirect?: boolean
}

const UNSAFE_METHODS = new Set(["POST", "PUT", "PATCH", "DELETE"])

export async function http<T = unknown>(path: string, opts: HttpOptions = {}): Promise<T> {
  const { body, skip401Redirect, headers, method = "GET", ...rest } = opts

  const finalHeaders = new Headers(headers)
  if (body !== undefined) {
    finalHeaders.set("Content-Type", "application/json")
  }

  // CSRF echo for unsafe methods
  if (UNSAFE_METHODS.has(method.toUpperCase())) {
    const token = getCsrfToken()
    if (token) finalHeaders.set(CSRF_HEADER, token)
  }

  const res = await fetch(`${API_BASE_URL}${path}`, {
    ...rest,
    method,
    headers: finalHeaders,
    credentials: "include",
    body: body !== undefined ? JSON.stringify(body) : undefined,
  })

  if (res.status === 401 && !skip401Redirect) {
    useSessionStore.getState().clear()
    window.location.href = "/login"
  }

  if (!res.ok) {
    const errBody = await res.json().catch(() => ({
      status: res.status,
      errorCode: "UNKNOWN",
      message: res.statusText,
    }))
    throw new ApiError(
      errBody.status ?? res.status,
      errBody.errorCode ?? "UNKNOWN",
      errBody.message ?? "Request failed",
    )
  }

  if (res.status === 204) return undefined as T
  return (await res.json()) as T
}
```

- [ ] **Step 2: GREEN 확인**

```bash
pnpm test:run src/shared/api
```
Expected: 2/2 PASS.

### Task 3.3: entities/session/api/session.ts

**Files:**
- Create: `src/entities/session/api/session.ts`
- Modify: `src/entities/session/index.ts`

- [ ] **Step 1: api/session.ts 작성**

```ts
import { http } from "../../../shared/api/http"
import type {
  AdminLoginResponseRaw,
  ChangePasswordCommand,
  LoginCommand,
  SessionMeta,
} from "../model/types"

interface ApiCommonResponse<T> {
  data: T
}

function toSessionMeta(raw: AdminLoginResponseRaw): SessionMeta {
  const issued = new Date(raw.issuedAt)
  const expires = new Date(issued.getTime() + raw.expiresIn * 1000)
  return {
    role: raw.role,
    mustChangePassword: raw.mustChangePassword,
    issuedAt: issued.toISOString(),
    expiresAt: expires.toISOString(),
  }
}

export async function login(cmd: LoginCommand): Promise<SessionMeta> {
  const res = await http<ApiCommonResponse<AdminLoginResponseRaw>>(
    "/api/v1/auth/admin/login",
    { method: "POST", body: cmd, skip401Redirect: true },
  )
  return toSessionMeta(res.data)
}

export async function logout(): Promise<void> {
  await http<void>("/api/v1/auth/admin/logout", { method: "POST" })
}

export async function changePassword(cmd: ChangePasswordCommand): Promise<void> {
  await http<void>("/api/v1/admin/password/change", { method: "POST", body: cmd })
}
```

- [ ] **Step 2: index.ts 보완**

```ts
export { useSessionStore } from "./model/store"
export type {
  AdminRole,
  SessionMeta,
  LoginCommand,
  ChangePasswordCommand,
} from "./model/types"
export * as sessionApi from "./api/session"
```

- [ ] **Step 3: 타입체크**

```bash
pnpm exec tsc --noEmit
```
Expected: errors 0.

### Task 3.4: Chunk 3 commit

- [ ] **Step 1: 회귀 검증**

```bash
pnpm test:run
```
Expected: 6/6 PASS (store 3 + http 3).

- [ ] **Step 2: 커밋**

```bash
git add src/shared/api/http.ts src/shared/api/__tests__/ \
  src/entities/session/api/session.ts src/entities/session/index.ts
git commit -m "feat(shared,session): http.ts (credentials/CSRF/401) + session API (PR 14a G3a)"
```

---

## Chunk 4: G3b — features/login + LoginPage

### Task 4.0: @hookform/resolvers 추가 (선행)

`react-hook-form` + `zod` 통합용 resolver 패키지. 기존 미설치.

- [ ] **Step 1**

```bash
cd "C:/Users/Eisen/Desktop/Labs/[projects] pfplay/pfplay-admin" && \
pnpm add @hookform/resolvers
```
Expected: lockfile 갱신, 새 의존성 추가 완료.

### Task 4.1: features/login/model/schema.ts

**Files:**
- Create: `src/features/login/model/schema.ts`

- [ ] **Step 1: 작성**

```ts
import { z } from "zod"

export const loginSchema = z.object({
  email: z.string().email("이메일 형식이 올바르지 않습니다").max(255),
  password: z.string().min(8, "8자 이상 입력해주세요").max(128),
})

export type LoginFormValues = z.infer<typeof loginSchema>
```

### Task 4.2: features/login/api/use-login.ts

**Files:**
- Create: `src/features/login/api/use-login.ts`

- [ ] **Step 1: 작성**

```ts
import { useMutation } from "@tanstack/react-query"
import { sessionApi, useSessionStore, type SessionMeta } from "@/entities/session"
import type { LoginCommand } from "@/entities/session"

export function useLogin(onSuccess?: (meta: SessionMeta) => void) {
  return useMutation({
    mutationFn: (cmd: LoginCommand) => sessionApi.login(cmd),
    onSuccess: (meta) => {
      useSessionStore.getState().setSession(meta)
      onSuccess?.(meta)
    },
  })
}
```

### Task 4.3: login-form 컴포넌트 테스트 작성 (RED)

**Files:**
- Create: `src/features/login/__tests__/login-form.test.tsx`

- [ ] **Step 1: 테스트 작성 (test #6, #7)**

```tsx
import { describe, it, expect, vi, beforeEach } from "vitest"
import { render, screen } from "@testing-library/react"
import userEvent from "@testing-library/user-event"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { LoginForm } from "../ui/login-form"

function renderForm() {
  const qc = new QueryClient({ defaultOptions: { mutations: { retry: false }, queries: { retry: false } } })
  const onSuccess = vi.fn()
  render(
    <QueryClientProvider client={qc}>
      <LoginForm onSuccess={onSuccess} />
    </QueryClientProvider>,
  )
  return { onSuccess }
}

describe("LoginForm 검증", () => {
  beforeEach(() => localStorage.clear())

  it("invalid email format → 이메일 형식 에러", async () => {
    const user = userEvent.setup()
    renderForm()
    await user.type(screen.getByLabelText("이메일"), "not-an-email")
    await user.type(screen.getByLabelText("비밀번호"), "ValidPass!1")
    await user.click(screen.getByRole("button", { name: "로그인" }))
    expect(await screen.findByText("이메일 형식이 올바르지 않습니다")).toBeInTheDocument()
  })

  it("short password → 8자 이상 에러", async () => {
    const user = userEvent.setup()
    renderForm()
    await user.type(screen.getByLabelText("이메일"), "admin@pfplay.xyz")
    await user.type(screen.getByLabelText("비밀번호"), "short")
    await user.click(screen.getByRole("button", { name: "로그인" }))
    expect(await screen.findByText("8자 이상 입력해주세요")).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 실패 확인 (RED)**

```bash
pnpm test:run src/features/login
```
Expected: `Cannot find module '../ui/login-form'` (RED).

### Task 4.4: features/login/ui/login-form.tsx 구현 (GREEN)

**Files:**
- Create: `src/features/login/ui/login-form.tsx`

- [ ] **Step 1: 작성** (RHF + zod + sonner + ApiError 분기)

```tsx
import { zodResolver } from "@hookform/resolvers/zod"
import { useForm } from "react-hook-form"
import { toast } from "sonner"
import { ApiError } from "@/shared/api/error"
import { useLogin } from "../api/use-login"
import { loginSchema, type LoginFormValues } from "../model/schema"

interface Props {
  onSuccess: (meta: { mustChangePassword: boolean }) => void
}

export function LoginForm({ onSuccess }: Props) {
  const {
    register,
    handleSubmit,
    setError,
    formState: { errors, isSubmitting },
  } = useForm<LoginFormValues>({
    resolver: zodResolver(loginSchema),
  })

  const login = useLogin((meta) => onSuccess(meta))

  const submit = handleSubmit((values) => {
    login.mutate(values, {
      onError: (err) => {
        if (err instanceof ApiError && err.status === 401) {
          setError("root", { message: "이메일 또는 비밀번호가 올바르지 않습니다" })
        } else if (err instanceof ApiError && err.status === 429) {
          setError("root", { message: "잠시 후 다시 시도해주세요" })
        } else {
          toast.error("서버 오류")
          setError("root", { message: "잠시 후 다시 시도해주세요" })
        }
      },
    })
  })

  return (
    <form onSubmit={submit} className="space-y-4" noValidate>
      <div>
        <label htmlFor="email" className="block text-sm font-medium">이메일</label>
        <input
          id="email"
          type="email"
          autoComplete="email"
          {...register("email")}
          className="mt-1 w-full rounded border px-3 py-2"
        />
        {errors.email && <p role="alert" className="mt-1 text-sm text-destructive">{errors.email.message}</p>}
      </div>
      <div>
        <label htmlFor="password" className="block text-sm font-medium">비밀번호</label>
        <input
          id="password"
          type="password"
          autoComplete="current-password"
          {...register("password")}
          className="mt-1 w-full rounded border px-3 py-2"
        />
        {errors.password && <p role="alert" className="mt-1 text-sm text-destructive">{errors.password.message}</p>}
      </div>
      {errors.root && <p role="alert" className="text-sm text-destructive">{errors.root.message}</p>}
      <button
        type="submit"
        disabled={isSubmitting || login.isPending}
        className="w-full rounded bg-primary px-4 py-2 text-primary-foreground disabled:opacity-50"
      >
        로그인
      </button>
    </form>
  )
}
```

- [ ] **Step 2: 테스트 통과 확인 (GREEN)**

```bash
pnpm test:run src/features/login
```
Expected: 2/2 PASS.

### Task 4.5: pages/login-page.tsx

**Files:**
- Create: `src/pages/login-page.tsx`

- [ ] **Step 1: 작성**

```tsx
import { Navigate, useLocation, useNavigate } from "react-router-dom"
import { useSessionStore } from "@/entities/session"
import { LoginForm } from "@/features/login/ui/login-form"

interface LocationState {
  returnTo?: string
}

export function LoginPage() {
  const navigate = useNavigate()
  const location = useLocation()
  const { isAuthenticated, meta } = useSessionStore()
  const returnTo = (location.state as LocationState | null)?.returnTo

  if (isAuthenticated && !meta?.mustChangePassword) {
    return <Navigate to={returnTo ?? "/"} replace />
  }

  return (
    <main className="flex min-h-screen items-center justify-center bg-background">
      <div className="w-full max-w-sm space-y-6 rounded-lg border border-border bg-card p-8 shadow-sm">
        <h1 className="text-center text-2xl font-bold">PFPlay Admin 로그인</h1>
        <LoginForm
          onSuccess={(meta) => {
            const next = meta.mustChangePassword ? "/password/change" : (returnTo ?? "/")
            navigate(next, { replace: true })
          }}
        />
      </div>
    </main>
  )
}
```

### Task 4.6: App.tsx에 /login 라우트 등록

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: 수정**

```tsx
import { Routes, Route, Navigate } from "react-router-dom"
import AppLayout from "@/app/layout"
import { ScenariosPage } from "@/pages/scenarios-page"
import { LoginPage } from "@/pages/login-page"

export function App() {
  return (
    <Routes>
      <Route path="/login" element={<LoginPage />} />
      <Route element={<AppLayout />}>
        <Route path="/" element={<Navigate to="/scenarios" replace />} />
        <Route path="/scenarios" element={<ScenariosPage />} />
      </Route>
    </Routes>
  )
}
```

(rooms/users 라우트는 Chunk 5에서 ProtectedRoute와 함께 wiring.)

### Task 4.7: integration test — login-page

**Files:**
- Create: `src/pages/login-page.test.tsx`

- [ ] **Step 1: 테스트 작성 (4 cases)**

```tsx
import { describe, it, expect, beforeEach } from "vitest"
import { http as msw, HttpResponse } from "msw"
import { render, screen } from "@testing-library/react"
import userEvent from "@testing-library/user-event"
import { MemoryRouter, Route, Routes } from "react-router-dom"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { server } from "@/test/mocks/server"
import { API_BASE_URL } from "@/shared/config/env"
import { useSessionStore } from "@/entities/session"
import { LoginPage } from "./login-page"

function renderAt(initialEntry: { pathname: string; state?: unknown } | string) {
  const qc = new QueryClient({ defaultOptions: { mutations: { retry: false }, queries: { retry: false } } })
  return render(
    <QueryClientProvider client={qc}>
      <MemoryRouter initialEntries={[initialEntry]}>
        <Routes>
          <Route path="/login" element={<LoginPage />} />
          <Route path="/" element={<div>Dashboard</div>} />
          <Route path="/password/change" element={<div>ChangePasswordPage</div>} />
          <Route path="/rooms" element={<div>Rooms</div>} />
        </Routes>
      </MemoryRouter>
    </QueryClientProvider>,
  )
}

describe("LoginPage integration", () => {
  beforeEach(() => {
    localStorage.clear()
    useSessionStore.getState().clear()
  })

  it("happy path → / 로 navigate + sessionStore 갱신", async () => {
    const user = userEvent.setup()
    renderAt("/login")
    await user.type(screen.getByLabelText("이메일"), "admin@pfplay.xyz")
    await user.type(screen.getByLabelText("비밀번호"), "ValidPass!1")
    await user.click(screen.getByRole("button", { name: "로그인" }))
    expect(await screen.findByText("Dashboard")).toBeInTheDocument()
    expect(useSessionStore.getState().isAuthenticated).toBe(true)
    expect(useSessionStore.getState().meta?.role).toBe("ADMIN")
  })

  it("401 응답 → form-level 에러 표시", async () => {
    const user = userEvent.setup()
    renderAt("/login")
    await user.type(screen.getByLabelText("이메일"), "wrong@pfplay.xyz")
    await user.type(screen.getByLabelText("비밀번호"), "WrongPass!1")
    await user.click(screen.getByRole("button", { name: "로그인" }))
    expect(await screen.findByText("이메일 또는 비밀번호가 올바르지 않습니다")).toBeInTheDocument()
    expect(useSessionStore.getState().isAuthenticated).toBe(false)
  })

  it("429 응답 → 잠시 후 다시 시도해주세요", async () => {
    server.use(
      msw.post(`${API_BASE_URL}/api/v1/auth/admin/login`, () =>
        HttpResponse.json({ status: 429, errorCode: "RATE-LIMITED", message: "rate limited" }, { status: 429 }),
      ),
    )
    const user = userEvent.setup()
    renderAt("/login")
    await user.type(screen.getByLabelText("이메일"), "admin@pfplay.xyz")
    await user.type(screen.getByLabelText("비밀번호"), "ValidPass!1")
    await user.click(screen.getByRole("button", { name: "로그인" }))
    expect(await screen.findByText("잠시 후 다시 시도해주세요")).toBeInTheDocument()
  })

  it("mustChangePassword=true → /password/change navigate", async () => {
    server.use(
      msw.post(`${API_BASE_URL}/api/v1/auth/admin/login`, () =>
        HttpResponse.json({
          data: {
            tokenType: "Cookie",
            expiresIn: 900,
            issuedAt: "2026-04-28T10:00:00",
            role: "ADMIN",
            mustChangePassword: true,
          },
        }),
      ),
    )
    const user = userEvent.setup()
    renderAt("/login")
    await user.type(screen.getByLabelText("이메일"), "admin@pfplay.xyz")
    await user.type(screen.getByLabelText("비밀번호"), "ValidPass!1")
    await user.click(screen.getByRole("button", { name: "로그인" }))
    expect(await screen.findByText("ChangePasswordPage")).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 실행**

```bash
pnpm test:run src/pages
```
Expected: 4/4 PASS.

### Task 4.8: Chunk 4 commit

- [ ] **Step 1: 전체 회귀**

```bash
pnpm test:run
```
Expected: 12/12 PASS (store 3 + http 3 + login-form 2 + login-page 4).

- [ ] **Step 2: 커밋**

```bash
git add src/features/login/ src/pages/login-page.tsx src/pages/login-page.test.tsx \
  src/App.tsx package.json pnpm-lock.yaml
git commit -m "feat(login): LoginForm + LoginPage + /login route + integration tests (PR 14a G3b)"
```

---

## Chunk 5: G4 — ProtectedRoute + features/change-password + dashboard

### Task 5.1: pages/dashboard-page.tsx (placeholder)

**Files:**
- Create: `src/pages/dashboard-page.tsx`

- [ ] **Step 1: 작성**

```tsx
export function DashboardPage() {
  return (
    <div className="p-8">
      <h2 className="text-2xl font-bold">PFPlay Admin</h2>
      <p className="mt-2 text-muted-foreground">대시보드 — PR 14b 이후 구현 예정</p>
    </div>
  )
}
```

### Task 5.2: features/change-password/model/schema.ts

**Files:**
- Create: `src/features/change-password/model/schema.ts`

- [ ] **Step 1: 작성**

```ts
import { z } from "zod"

const PASSWORD_REGEX = /^(?=.*[A-Z])(?=.*[a-z])(?=.*\d)(?=.*[!@#$%^&*]).{10,128}$/

export const changePasswordSchema = z
  .object({
    // 정책 변경 이전 발급된 임시 비밀번호 호환을 위해 min(8) 유지 (백엔드 ChangeAdminPasswordRequest는 @NotBlank만 요구)
    currentPassword: z.string().min(8, "8자 이상").max(128),
    newPassword: z
      .string()
      .regex(PASSWORD_REGEX, "10자 이상, 대/소문자, 숫자, 특수문자(!@#$%^&*) 각 1개 이상"),
    newPasswordConfirm: z.string(),
  })
  .refine((d) => d.newPassword === d.newPasswordConfirm, {
    path: ["newPasswordConfirm"],
    message: "새 비밀번호와 일치하지 않습니다",
  })
  .refine((d) => d.newPassword !== d.currentPassword, {
    path: ["newPassword"],
    message: "현재 비밀번호와 달라야 합니다",
  })

export type ChangePasswordFormValues = z.infer<typeof changePasswordSchema>
```

### Task 5.3: features/change-password/api/use-change-password.ts

**Files:**
- Create: `src/features/change-password/api/use-change-password.ts`

- [ ] **Step 1: 작성**

```ts
import { useMutation } from "@tanstack/react-query"
import { sessionApi, useSessionStore } from "@/entities/session"
import type { ChangePasswordCommand } from "@/entities/session"

export function useChangePassword(onSuccess?: () => void) {
  return useMutation({
    mutationFn: (cmd: ChangePasswordCommand) => sessionApi.changePassword(cmd),
    onSuccess: () => {
      useSessionStore.getState().clearMustChangePassword()
      onSuccess?.()
    },
  })
}
```

### Task 5.4: change-password-form 컴포넌트 테스트 작성 (RED)

**Files:**
- Create: `src/features/change-password/__tests__/change-password-form.test.tsx`

- [ ] **Step 1: 테스트 작성 (test #8)**

```tsx
import { describe, it, expect } from "vitest"
import { render, screen } from "@testing-library/react"
import userEvent from "@testing-library/user-event"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { ChangePasswordForm } from "../ui/change-password-form"

function renderForm() {
  const qc = new QueryClient({ defaultOptions: { mutations: { retry: false } } })
  render(
    <QueryClientProvider client={qc}>
      <ChangePasswordForm onSuccess={() => {}} />
    </QueryClientProvider>,
  )
}

describe("ChangePasswordForm", () => {
  it("newPassword === currentPassword → 현재 비밀번호와 달라야 합니다", async () => {
    const user = userEvent.setup()
    renderForm()
    await user.type(screen.getByLabelText("현재 비밀번호"), "Password!1AB")
    await user.type(screen.getByLabelText("새 비밀번호"), "Password!1AB")
    await user.type(screen.getByLabelText("새 비밀번호 확인"), "Password!1AB")
    await user.click(screen.getByRole("button", { name: "변경" }))
    expect(await screen.findByText("현재 비밀번호와 달라야 합니다")).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 실패 확인 (RED)**

```bash
pnpm test:run src/features/change-password
```
Expected: `Cannot find module '../ui/change-password-form'` (RED).

### Task 5.5: features/change-password/ui/change-password-form.tsx 구현 (GREEN)

**Files:**
- Create: `src/features/change-password/ui/change-password-form.tsx`

- [ ] **Step 1: 작성**

```tsx
import { zodResolver } from "@hookform/resolvers/zod"
import { useForm } from "react-hook-form"
import { toast } from "sonner"
import { ApiError } from "@/shared/api/error"
import { useChangePassword } from "../api/use-change-password"
import { changePasswordSchema, type ChangePasswordFormValues } from "../model/schema"

interface Props {
  onSuccess: () => void
}

export function ChangePasswordForm({ onSuccess }: Props) {
  const { register, handleSubmit, setError, formState: { errors, isSubmitting } } =
    useForm<ChangePasswordFormValues>({ resolver: zodResolver(changePasswordSchema) })

  const change = useChangePassword(() => {
    toast.success("비밀번호가 변경되었습니다")
    onSuccess()
  })

  const submit = handleSubmit((values) => {
    change.mutate(
      { currentPassword: values.currentPassword, newPassword: values.newPassword },
      {
        onError: (err) => {
          if (err instanceof ApiError && err.errorCode === "ADMIN-INVALID-CURRENT-PASSWORD") {
            setError("currentPassword", { message: "현재 비밀번호가 올바르지 않습니다" })
          } else if (err instanceof ApiError && err.errorCode === "ADMIN-INVALID-NEW-PASSWORD") {
            setError("newPassword", { message: "비밀번호 정책을 만족하지 않습니다" })
          } else if (err instanceof ApiError && err.status === 400) {
            setError("root", { message: err.message })
          } else {
            toast.error("서버 오류")
            setError("root", { message: "잠시 후 다시 시도해주세요" })
          }
        },
      },
    )
  })

  return (
    <form onSubmit={submit} className="space-y-4" noValidate>
      <div>
        <label htmlFor="currentPassword" className="block text-sm font-medium">현재 비밀번호</label>
        <input id="currentPassword" type="password" autoComplete="current-password"
          {...register("currentPassword")} className="mt-1 w-full rounded border px-3 py-2" />
        {errors.currentPassword && <p role="alert" className="mt-1 text-sm text-destructive">{errors.currentPassword.message}</p>}
      </div>
      <div>
        <label htmlFor="newPassword" className="block text-sm font-medium">새 비밀번호</label>
        <input id="newPassword" type="password" autoComplete="new-password"
          {...register("newPassword")} className="mt-1 w-full rounded border px-3 py-2" />
        {errors.newPassword && <p role="alert" className="mt-1 text-sm text-destructive">{errors.newPassword.message}</p>}
      </div>
      <div>
        <label htmlFor="newPasswordConfirm" className="block text-sm font-medium">새 비밀번호 확인</label>
        <input id="newPasswordConfirm" type="password" autoComplete="new-password"
          {...register("newPasswordConfirm")} className="mt-1 w-full rounded border px-3 py-2" />
        {errors.newPasswordConfirm && <p role="alert" className="mt-1 text-sm text-destructive">{errors.newPasswordConfirm.message}</p>}
      </div>
      {errors.root && <p role="alert" className="text-sm text-destructive">{errors.root.message}</p>}
      <button type="submit" disabled={isSubmitting || change.isPending}
        className="w-full rounded bg-primary px-4 py-2 text-primary-foreground disabled:opacity-50">
        변경
      </button>
    </form>
  )
}
```

- [ ] **Step 2: 테스트 통과 확인 (GREEN)**

```bash
pnpm test:run src/features/change-password
```
Expected: 1/1 PASS.

### Task 5.6: pages/change-password-page.tsx

**Files:**
- Create: `src/pages/change-password-page.tsx`

- [ ] **Step 1: 작성**

```tsx
import { useNavigate } from "react-router-dom"
import { ChangePasswordForm } from "@/features/change-password/ui/change-password-form"

export function ChangePasswordPage() {
  const navigate = useNavigate()
  return (
    <main className="flex min-h-screen items-center justify-center bg-background">
      <div className="w-full max-w-sm space-y-6 rounded-lg border border-border bg-card p-8 shadow-sm">
        <h1 className="text-center text-xl font-bold">비밀번호 변경</h1>
        <p className="text-sm text-muted-foreground">최초 로그인 후 비밀번호를 변경해야 합니다.</p>
        <ChangePasswordForm onSuccess={() => navigate("/", { replace: true })} />
      </div>
    </main>
  )
}
```

### Task 5.7: change-password-page integration test (#15)

**Files:**
- Create: `src/pages/change-password-page.test.tsx`

- [ ] **Step 1: 테스트**

```tsx
import { describe, it, expect, beforeEach } from "vitest"
import { render, screen } from "@testing-library/react"
import userEvent from "@testing-library/user-event"
import { MemoryRouter, Route, Routes } from "react-router-dom"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { useSessionStore } from "@/entities/session"
import { ChangePasswordPage } from "./change-password-page"

describe("ChangePasswordPage integration", () => {
  beforeEach(() => {
    localStorage.clear()
    useSessionStore.getState().setSession({
      role: "ADMIN", mustChangePassword: true,
      issuedAt: "2026-04-28T10:00:00.000Z", expiresAt: "2026-04-28T10:15:00.000Z",
    })
  })

  it("성공(204) → clearMustChangePassword + / 로 navigate", async () => {
    const qc = new QueryClient({ defaultOptions: { mutations: { retry: false } } })
    const user = userEvent.setup()
    render(
      <QueryClientProvider client={qc}>
        <MemoryRouter initialEntries={["/password/change"]}>
          <Routes>
            <Route path="/password/change" element={<ChangePasswordPage />} />
            <Route path="/" element={<div>Dashboard</div>} />
          </Routes>
        </MemoryRouter>
      </QueryClientProvider>,
    )
    await user.type(screen.getByLabelText("현재 비밀번호"), "OldPass!1ab")
    await user.type(screen.getByLabelText("새 비밀번호"), "NewPass!1AB")
    await user.type(screen.getByLabelText("새 비밀번호 확인"), "NewPass!1AB")
    await user.click(screen.getByRole("button", { name: "변경" }))
    expect(await screen.findByText("Dashboard")).toBeInTheDocument()
    expect(useSessionStore.getState().meta?.mustChangePassword).toBe(false)
  })
})
```

- [ ] **Step 2**

```bash
pnpm test:run src/pages/change-password-page
```
Expected: 1/1 PASS.

### Task 5.8: widgets/protected-route.tsx

**Files:**
- Create: `src/widgets/protected-route.tsx`

- [ ] **Step 1: 작성**

```tsx
import type { ReactNode } from "react"
import { Navigate, useLocation } from "react-router-dom"
import { useSessionStore } from "@/entities/session"

interface Props { children: ReactNode }

export function ProtectedRoute({ children }: Props) {
  const { isAuthenticated, meta } = useSessionStore()
  const location = useLocation()

  if (!isAuthenticated) {
    return <Navigate to="/login" state={{ returnTo: location.pathname + location.search }} replace />
  }
  if (meta?.mustChangePassword && location.pathname !== "/password/change") {
    return <Navigate to="/password/change" replace />
  }
  return <>{children}</>
}
```

### Task 5.9: protected-route integration test (#13, #14)

**Files:**
- Create: `src/widgets/__tests__/protected-route.test.tsx`

- [ ] **Step 1: 테스트**

```tsx
import { describe, it, expect, beforeEach } from "vitest"
import { render, screen } from "@testing-library/react"
import { MemoryRouter, Route, Routes, useLocation } from "react-router-dom"
import { useSessionStore } from "@/entities/session"
import { ProtectedRoute } from "../protected-route"

function ShowReturnTo() {
  const location = useLocation()
  const state = location.state as { returnTo?: string } | null
  return <div>login + returnTo={state?.returnTo ?? "(none)"}</div>
}

function renderAt(initial: string) {
  return render(
    <MemoryRouter initialEntries={[initial]}>
      <Routes>
        <Route path="/login" element={<ShowReturnTo />} />
        <Route path="/password/change" element={<div>ChangePassword</div>} />
        <Route path="/rooms" element={<ProtectedRoute><div>Rooms</div></ProtectedRoute>} />
      </Routes>
    </MemoryRouter>,
  )
}

describe("ProtectedRoute", () => {
  beforeEach(() => {
    localStorage.clear()
    useSessionStore.getState().clear()
  })

  it("미인증 + /rooms → /login + state.returnTo='/rooms'", () => {
    renderAt("/rooms")
    expect(screen.getByText("login + returnTo=/rooms")).toBeInTheDocument()
  })

  it("인증 + mustChangePassword=true + /rooms → /password/change", () => {
    useSessionStore.getState().setSession({
      role: "ADMIN", mustChangePassword: true,
      issuedAt: "2026-04-28T10:00:00.000Z", expiresAt: "2026-04-28T10:15:00.000Z",
    })
    renderAt("/rooms")
    expect(screen.getByText("ChangePassword")).toBeInTheDocument()
  })
})
```

- [ ] **Step 2**

```bash
pnpm test:run src/widgets
```
Expected: 2/2 PASS.

### Task 5.10: App.tsx 보호 라우트 wiring

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: 수정** (모든 인증 라우트 ProtectedRoute로 감싸기. spec §3.2의 `/scenarios`, `/rooms`, `/users` 모두 보호 뒤로 이동.)

```tsx
import { Routes, Route, Navigate } from "react-router-dom"
import AppLayout from "@/app/layout"
import { ScenariosPage } from "@/pages/scenarios-page"
import { RoomsPage } from "@/pages/rooms-page"
import { UsersPage } from "@/pages/users-page"
import { LoginPage } from "@/pages/login-page"
import { ChangePasswordPage } from "@/pages/change-password-page"
import { DashboardPage } from "@/pages/dashboard-page"
import { ProtectedRoute } from "@/widgets/protected-route"

export function App() {
  return (
    <Routes>
      <Route path="/login" element={<LoginPage />} />
      <Route path="/password/change" element={<ProtectedRoute><ChangePasswordPage /></ProtectedRoute>} />
      <Route element={<ProtectedRoute><AppLayout /></ProtectedRoute>}>
        <Route path="/" element={<DashboardPage />} />
        <Route path="/scenarios" element={<ScenariosPage />} />
        <Route path="/rooms" element={<RoomsPage />} />
        <Route path="/users" element={<UsersPage />} />
      </Route>
      <Route path="*" element={<Navigate to="/" replace />} />
    </Routes>
  )
}
```

기존 `pages/{rooms,users,scenarios}-page.tsx` 파일은 그대로. 라우트만 등록하면 demo 화면 표시됨. **사이드바 disabled 표시는 layout.tsx에 그대로 유지** (Task 6.2 참조) — 직접 URL 입력 시 진입 가능, 사이드바 클릭으로는 진입 불가 (14b/14c에서 사이드바 활성화 + 실제 backend 연결).

기존 page export 형태 확인 필요. 명명 export(`export const RoomsPage`)가 아닌 default export면 import 구문을 맞춰 조정. Task 5.10 Step 0으로 1회 read.

- [ ] **Step 0: rooms-page / users-page export 형태 확인 (Step 1 직전)**

```bash
grep -n "^export" src/pages/rooms-page.tsx src/pages/users-page.tsx
```
Expected: `export function RoomsPage` / `export function UsersPage` 등 — 명명 export면 위 코드 그대로. default export면 `import RoomsPage from ...`로 변경.

- [ ] **Step 2: 빌드 회귀**

```bash
pnpm exec tsc --noEmit && pnpm build
```
Expected: 컴파일 + 빌드 성공.

### Task 5.11: Chunk 5 commit

- [ ] **Step 1: 전체 회귀**

```bash
pnpm test:run
```
Expected: 16/16 PASS (누계: store 3 + http 3 + login-form 2 + login-page 4 + change-password-form 1 + change-password-page 1 + protected-route 2 = 16).

- [ ] **Step 2: 커밋**

```bash
git add src/features/change-password/ src/widgets/protected-route.tsx src/widgets/__tests__/ \
  src/pages/change-password-page.tsx src/pages/change-password-page.test.tsx \
  src/pages/dashboard-page.tsx src/App.tsx
git commit -m "feat(change-password,widgets): ProtectedRoute + ChangePassword + dashboard placeholder + 보호 라우트 wiring (PR 14a G4)"
```

---

## Chunk 6: G5 — logout + 운영 수동 검증 + spec catch-up (G6)

### Task 6.1: features/logout/api/use-logout.ts

**Files:**
- Create: `src/features/logout/api/use-logout.ts`

- [ ] **Step 1: 작성**

```ts
import { useMutation } from "@tanstack/react-query"
import { useNavigate } from "react-router-dom"
import { sessionApi, useSessionStore } from "@/entities/session"

export function useLogout() {
  const navigate = useNavigate()
  return useMutation({
    mutationFn: () => sessionApi.logout(),
    // 백엔드 응답 성공/실패와 무관하게 클라 cleanup
    onSettled: () => {
      useSessionStore.getState().clear()
      navigate("/login", { replace: true })
    },
  })
}
```

### Task 6.2: app/layout.tsx — 로그아웃 + 사용자 표시

**Files:**
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: 수정**

```tsx
import { NavLink, Outlet } from "react-router-dom"
import { Users, DoorOpen, PlaySquare, LogOut, LayoutDashboard } from "lucide-react"
import { cn } from "@/shared/lib/utils"
import { useSessionStore } from "@/entities/session"
import { useLogout } from "@/features/logout/api/use-logout"
import "@/globals.css"

export default function AppLayout() {
  const { meta } = useSessionStore()
  const logout = useLogout()

  const navItems = [
    { to: "/", icon: LayoutDashboard, label: "대시보드", disabled: false },
    { to: "/users", icon: Users, label: "가상 유저", disabled: true },
    { to: "/rooms", icon: DoorOpen, label: "파티룸", disabled: true },
    { to: "/scenarios", icon: PlaySquare, label: "시나리오", disabled: false },
  ]

  return (
    <div className="flex h-screen bg-background">
      <aside className="flex w-64 flex-col border-r border-border bg-card">
        <div className="flex h-16 items-center border-b border-border px-6">
          <h1 className="text-xl font-bold text-foreground">PFPlay Admin</h1>
        </div>
        <nav className="flex-1 space-y-1 p-4">
          {navItems.map((item) =>
            item.disabled ? (
              <div key={item.to}
                className="flex items-center gap-3 rounded-lg px-4 py-3 text-sm font-medium text-muted-foreground/50 cursor-not-allowed">
                <item.icon className="h-5 w-5" />{item.label}
              </div>
            ) : (
              <NavLink key={item.to} to={item.to}
                className={({ isActive }) => cn(
                  "flex items-center gap-3 rounded-lg px-4 py-3 text-sm font-medium transition-colors",
                  isActive ? "bg-primary text-primary-foreground" : "text-muted-foreground hover:bg-accent hover:text-accent-foreground",
                )} end={item.to === "/"}>
                <item.icon className="h-5 w-5" />{item.label}
              </NavLink>
            )
          )}
        </nav>
        <div className="border-t border-border p-4 space-y-2">
          {meta && (
            <p className="text-xs text-muted-foreground">권한: {meta.role}</p>
          )}
          <button
            type="button"
            onClick={() => logout.mutate()}
            disabled={logout.isPending}
            className="flex w-full items-center gap-2 rounded-lg px-4 py-2 text-sm font-medium text-muted-foreground hover:bg-accent disabled:opacity-50"
          >
            <LogOut className="h-4 w-4" />로그아웃
          </button>
        </div>
      </aside>
      <main className="flex-1 overflow-auto">
        <Outlet />
      </main>
    </div>
  )
}
```

- [ ] **Step 2: 타입체크 + 빌드**

```bash
pnpm exec tsc --noEmit && pnpm build
```
Expected: 성공.

### Task 6.3: 수동 검증 5 시나리오 (G5 핵심)

> **사전 조건**: 로컬 백엔드(`pfplay-platform`) `:app:bootRun` 실행 중. `JAVA_HOME="C:/Users/Eisen/.jdks/ms-21.0.7"` (사용자 메모). `.env.local`에 `VITE_API_BASE_URL=http://localhost:8080` 설정. 또는 dev 서버 띄울 때 vite proxy 사용.

- [ ] **Step 1: dev 서버 띄우기**

```bash
cd "C:/Users/Eisen/Desktop/Labs/[projects] pfplay/pfplay-admin" && pnpm dev
```
Expected: `Local: http://localhost:3000/`.

- [ ] **Step 2: 시나리오 ① happy login**
  - http://localhost:3000/ → /login redirect (returnTo="/")
  - 백엔드 seed 어드민 계정 (또는 `AdministratorManagementController`로 생성한 계정)으로 로그인
  - 성공 후 / (대시보드) 표시. 사이드바에 "권한: ADMIN" + 로그아웃 버튼.
  - `useSessionStore` localStorage(`pfplay-admin-session`)에 메타 저장 확인 (DevTools).

- [ ] **Step 3: 시나리오 ② expired cookie 401 → returnTo 보존**
  - 새 탭에서 /scenarios 진입 → 정상 표시.
  - DevTools → Application → Cookies → `AdminAccessToken` 삭제.
  - /scenarios에서 새로고침 또는 어떤 변형 호출 시도 → 401 → /login + returnTo="/scenarios" 표시 (URL 또는 location.state 확인).
  - 재로그인 후 /scenarios로 자동 navigate 확인.

- [ ] **Step 4: 시나리오 ③ mustChangePassword 흐름**
  - 백엔드에서 어드민 계정의 `must_change_password=true`로 update (DB 또는 별 endpoint).
  - 로그아웃 → 다시 로그인 → /password/change 강제 진입 확인.
  - 어떤 보호 라우트(/scenarios) 진입 시도 → /password/change로 redirect 확인.
  - 비밀번호 변경 → / 로 navigate + sidebar 정상 navigation 가능.

- [ ] **Step 5: 시나리오 ④ logout**
  - 사이드바 "로그아웃" 클릭.
  - 쿠키 클리어 + localStorage clear + /login 표시.
  - 다시 /scenarios 시도 → /login + returnTo 확인.

- [ ] **Step 6: 시나리오 ⑤ CSRF 토큰 발급 확인 (R4 검증)**
  - 로그인 직후 DevTools → Application → Cookies → `XSRF-TOKEN` 존재 여부 확인.
  - 부재 시 → `R4 미해소`로 §12에 기록 + 14b 진입 전 follow-up commit 필요 (예: 로그인 직후 가벼운 GET 호출로 발급 유도).
  - 있으면 → 로그아웃 호출 시 Network 탭에서 `X-XSRF-TOKEN` 헤더 echo 확인.

- [ ] **Step 7: 발견사항 메모**

각 시나리오에서 발견된 deviations(예: 백엔드 errorCode 다름, 응답 envelope 차이, AdminLoginResponse.issuedAt 타임존 등)를 §12에 backfill 준비.

### Task 6.4: G5 commit

- [ ] **Step 1: 전체 회귀**

```bash
pnpm test:run
```
Expected: 16/16 PASS.

(로그아웃 흐름은 Task 6.3 시나리오 ④에서 수동 검증. 자동 unit test는 별도로 추가하지 않음 — `useLogout`은 `sessionApi.logout` + `store.clear` + `navigate` 합성에 가까운 얇은 wrapper이므로.)

- [ ] **Step 2: 커밋**

```bash
git add src/features/logout/ src/app/layout.tsx
git commit -m "feat(logout): logout mutation + sidebar wire + 권한 표시 (PR 14a G5)"
```

### Task 6.5: G6 — spec §12 / §13 catch-up

**Files:**
- Modify: `docs/specs/2026-04-28-admin-pr14a-design.md` (§12, §13)

- [ ] **Step 1: §12 작성** (수동 검증 발견 사항 + ground-truth 정정 + chunk SHA)

§12 본문 양식:
```
## 12. Open Items / Implementation Reality (post-build catch-up)

### 12.1 Chunk SHA backfill
- G1 commit `<sha>` — chore: vitest+RTL+msw deps
- G1 commit `<sha>` — feat(shared): http/error/csrf 셋업
- G2 commit `<sha>` — feat(session): zustand persist store
- G3a commit `<sha>` — feat(shared,session): http.ts + session API
- G3b commit `<sha>` — feat(login): LoginForm + LoginPage
- G4 commit `<sha>` — feat(change-password,widgets): ProtectedRoute + ChangePassword
- G5 commit `<sha>` — feat(logout): logout + sidebar wire

### 12.2 Backend ground-truth deviations (관찰 결과 vs spec §2)
- (각 deviation 1줄 — 예: AdminLoginResponse.issuedAt 타임존 — Z 미포함 → toSessionMeta에서 KST 가정)
- ...

### 12.3 R1 (CORS/SameSite) 검증 결과
- staging 검증: ⬜ 미수행 / ✅ 통과 / ❌ 실패 (실패 시 사유 + 백엔드 follow-up PR 결정)

### 12.4 R4 (CSRF 토큰 발급) 검증 결과
- 로그인 직후 XSRF-TOKEN 존재 여부: ⬜ / ✅ / ❌
- (❌인 경우 follow-up: 로그인 직후 GET 호출로 발급 유도, commit `<sha>`)

### 12.5 R7 (env 정합성) 검증 결과
- dev/staging/prod 환경 변수 매트릭스 점검 결과

### 12.6 R8 (Safari ITP) 결과
- 운영 모니터링 항목으로 기록 — 별 측정 없음

### 12.7 G5.1 polish follow-up (있다면)
- (cheap polish commit SHA + 사유)
```

- [ ] **Step 2: §13 backfill**

§13 future polish 누계 (§11.2 + 신규 발견):
```
## 13. Future Polish (§11.2 + 신규 발견 누적)

- (§11.2 8개 항목 그대로 옮김)
- (신규 발견 추가 — 예: AdminLoginResponse.issuedAt 타임존 명시 백엔드 PR)
```

- [ ] **Step 3: 커밋 (G6)**

```bash
git add docs/specs/2026-04-28-admin-pr14a-design.md
git commit -m "docs(spec): PR 14a §12 catch-up + §13 future polish backfill (G6)"
```

---

## 2. 완료 기준 (Definition of Done)

- [ ] **테스트 16/16 PASS** (`pnpm test:run`)
- [ ] **타입 0 에러** (`pnpm exec tsc --noEmit`)
- [ ] **빌드 성공** (`pnpm build`)
- [ ] **dev 서버 5 시나리오 수동 검증** (Task 6.3 ①~⑤ 모두 통과 또는 §12에 미통과 항목 follow-up 명시)
- [ ] **spec §12, §13 backfill 완료** (G6 commit 존재)
- [ ] **커밋 체인**: G1 → G2 → G3a → G3b → G4 → G5 → G6 (+필요 시 G<n>.1 polish)
- [ ] **회귀 0**: 기존 ScenariosPage 동작 그대로 (보호 라우트 안 진입 가능, 사이드바 표시 정상)
- [ ] **PR description 초안 준비**: `feature/admin-platform-frontend` 브랜치에서 PR 14a 단위로 PR 생성 가능 상태 (chunk SHA + features 요약 + §12 deviations 링크)

## 3. 위험 trigger 시 행동

- **R1 (cross-subdomain CORS) 실패**: §12.3에 ❌ 기록 + plan 진행 일시 정지 + 사용자 confirm으로 백엔드 별 PR(메모 "백엔드 작업 0" 위배) 진행 여부 결정.
- **R4 (CSRF 발급 부재)**: G5.1로 follow-up commit 작성 — 로그인 직후 가벼운 GET (예: GET `/api/v1/admin/me` 추가 시 그것, 부재 시 임시로 GET `/api/v1/admin/members?page=0&size=1`) 1회 호출하여 XSRF-TOKEN 발급 유도. §12.4에 sha 기록.
- **백엔드 errorCode 가정 불일치** (예: `ADMIN-INVALID-CURRENT-PASSWORD` 실제는 다른 prefix): G5.1 polish로 form errorCode 매핑 정정 + §12.2에 deviation 기록.

## 4. Skills/References

- @superpowers:test-driven-development — 모든 chunk Red/Green/Refactor 사이클
- @superpowers:executing-plans — 본 plan 실행 시
- @superpowers:subagent-driven-development — chunk 단위 fresh subagent dispatch (메모 "PR 시리즈 워크플로우" 패턴)
- @superpowers:verification-before-completion — 각 chunk commit 전 `pnpm test:run` + `tsc --noEmit` + `pnpm build` 회귀 검증
- 메모 `feedback_pr_series_workflow.md` — chunk 단위 confirm + atomic group + polish follow-up + spec §12 catch-up 패턴

## 5. 변경 카운트 추정

- 신규 파일: ~25개
- 수정 파일: 4개 (`package.json`, `vite.config.ts`, `src/App.tsx`, `src/app/layout.tsx`)
- LoC: ~1100 (코드 ~600 + 테스트 ~500)
- 테스트: 16 cases (unit 6 + component 3 + integration 7)
- Commits: G1, G2, G3a, G3b, G4, G5, G6 = 7 atomic + 0~3 polish (G<n>.1)
