# PR 14g Plan — §13.2 묶음 α

**선행**: 14g spec G0 (`d97c182`). 14f HEAD `f8875b1` baseline 287/287 PASS.
**목표 commits**: ~25-30. 신규 tests ~29, 누적 ~316.

---

## Chunk G1: 5 shared hook 추출 (TDD)

### G1.1: `useDialogResetEffect` (가장 단순)
- Create `src/shared/lib/use-dialog-reset-effect.ts` + test (3 tests)
- Commit: `feat(shared): add useDialogResetEffect (open=false → onReset) (G1.1)`

### G1.2: `useUrlQueryState` (single-param, 14b/14c/14d 패턴)
- Create + test (4 tests)
- Commit: `feat(shared): add useUrlQueryState<T> (zod schema + invalid drop + setQuery/reset) (G1.2)`

### G1.3: `useUrlMultiParamState` (multi-param, 14e 패턴)
- Create + test (3 tests)
- Commit: `feat(shared): add useUrlMultiParamState<T> (multi getAll/append) (G1.3)`

### G1.4: `useSelectionState` (14d partyrooms-list 패턴)
- Create + test (5 tests)
- Commit: `feat(shared): add useSelectionState<T> (Set<T> + deps reset + toggleAll) (G1.4)`

### G1.5: `useRefineError` (RHF v7 errors[""])
- Create + test (2 tests)
- Commit: `feat(shared): add useRefineError helper (RHF v7 top-level refine 추출) (G1.5)`

---

## Chunk G2: refactor 적용 (회귀 0)

### G2.1: useDialogResetEffect 적용 — 8 dialog
- ChangeTierDialog (14c) — 추가 적용 (현재 미적용, R11 폴리시 미준수)
- WithdrawDialog (14c)
- TerminateDialog / SuspendDialog / RestoreDialog (14c)
- UpdateMetaDialog (14c)
- DisplayFlagDialog (14c)
- BulkActionDialog (14d)
- BulkActionResultDialog (14d) — reset 미사용 (read-only) → skip
- TransitionStatusDialog (14e)
- PublishAvatarDialog / RetireAvatarDialog (14f)
- Commit: `refactor(dialogs): apply useDialogResetEffect to 8 dialogs + ChangeTierDialog R11 폴리시 일관 (G2.1)`

### G2.2: useUrlQueryState 적용 — 3 widget
- members-list / partyrooms-list / partyroom 외 다른 list page
- Commit: `refactor(widgets): apply useUrlQueryState to members/partyrooms list (G2.2)`

### G2.3: useUrlMultiParamState 적용 — reports widget
- reports-list
- Commit: `refactor(widgets): apply useUrlMultiParamState to reports-list (G2.3)`

### G2.4: useSelectionState 적용 — partyrooms-list
- Commit: `refactor(widgets): apply useSelectionState to partyrooms-list (G2.4)`

### G2.5: useRefineError 적용 — UpdateMetaDialog
- Commit: `refactor(dialogs): apply useRefineError to UpdateMetaDialog (G2.5)`

### G2.6: chunk sanity
- 287 + 17 (G1) = 304 PASS 검증

---

## Chunk G3: Backend DTO 확장 (cross-repo)

### G3.1: backend grep + spec mirror
- `user_account.withdrawn` 컬럼 존재 확인
- `PartyroomData.getIntroduction()` / `getPlaybackTimeLimit()` 메소드 확인 (14c update-meta로 검증됨)
- AdminMemberQueryService projection 변경 분량 파악

### G3.2: backend AdminMemberDetail 확장
- `AdminMemberDetailResponse` record에 `withdrawn: boolean` + `withdrawnAt: LocalDateTime` 추가
- AdminMemberQueryRepositoryImpl projection 변경
- backend test 보강 (DTO shape assertion)
- pfplay-platform commit (cross-repo)

### G3.3: backend AdminPartyroomDetail 확장
- `AdminPartyroomDetail` record에 `introduction: String` + `playbackTimeLimit: Integer` 추가
- AdminPartyroomQueryRepositoryImpl projection
- backend test 보강
- pfplay-platform commit

---

## Chunk G4: Frontend type mirror + UI 활성화

### G4.1: types.ts mirror (member + partyroom)
- `AdminMemberDetail` types에 `withdrawn` / `withdrawnAt` 추가
- `AdminPartyroomDetail` types에 `introduction` / `playbackTimeLimit` 추가
- Commit

### G4.2: members-actions-dropdown — withdraw menuitem disabled
- `disabled={member.withdrawn}` + tooltip "이미 탈퇴됨"
- test 보강 (3 tests)
- Commit

### G4.3: member-detail-cards — withdrawn 뱃지
- header에 회색 "탈퇴됨" Badge + tooltip(formatKst(withdrawnAt))
- test 보강
- Commit

### G4.4: partyroom-detail-cards — introduction / playbackTimeLimit 행
- meta 카드에 "소개" + "재생 시간 제한" 행 추가
- UpdateMetaDialog placeholder 채움
- test 보강
- Commit

---

## Chunk G5: Small enhancement

### G5.1: role-based nav
- layout.tsx — navItems에 `role` 필드 추가, filter
- test 보강 (3 tests)
- Commit

### G5.2: alreadyWithdrawn 토스트 차별화
- useWithdrawMember onSuccess 분기
- test 보강 (2 tests)
- Commit

### G5.3: status badge 색상 토큰
- globals.css에 변수 추가 + table/cards에 적용
- Commit

### G5.4: audit-log 카드 highlight (14c §13.2 마이크로 UX)
- partyroom-detail-cards `recentAdminActions` 첫 row useEffect timeout 1초 highlight
- test 보강 (1 test, className 검증)
- Commit

---

## Chunk G6: spec catch-up + 전체 sanity

### G6.1: 전체 sanity
- typecheck + tests + build
- 약 316/316 PASS

### G6.2: spec §12 Implementation Reality
- G1~G5 발견 항목 정리
- 14f §13.2 backfill (5 hook 추출 / DTO 확장 closed)
- Commit

---

## 참고

- 14g spec: `docs/specs/2026-04-29-admin-pr14g-design.md` (HEAD `d97c182`)
- 14f memory: `project_pr14ef_completed.md`
- 시간 추정 ~20h (refactor 11h + DTO 6h + small 3h)
