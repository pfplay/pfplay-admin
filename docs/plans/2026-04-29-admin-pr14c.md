# PR 14c Implementation Plan — 회원/파티룸 단건 mutation (헤더 dropdown + 모달)

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Spec:** `docs/specs/2026-04-29-admin-pr14c-design.md` (HEAD `22d0b80`)

**Goal:** 14b read-only detail page 위에 운영자 mutation 7개(회원 tier 변경 / 비식별화 탈퇴 + 파티룸 강제 종료 / 일시정지 / 재개 / 메타 수정 / display-flag 변경)를 헤더 우측 `Actions ▾` dropdown + 모달 패턴으로 통합한다. 14b의 read-only 카드 본문은 무수정. backend 변경 0.

**Architecture:** Approach 1 (per-mutation 파일, 14b FSD 컨벤션 그대로 확장). 모든 mutation은 동일한 react-query `useMutation` 골격 + invalidate 키 매트릭스 + `mutation-toast` 공통 헬퍼를 따른다. dropdown은 도메인별 1개 (`MembersActionsDropdown` / `PartyroomsActionsDropdown`)이며 활성 dialog id를 state로 보유. status-aware disabled 매트릭스로 파티룸 mutation 유효성 1차 차단.

**Tech Stack:** React 18 + TypeScript 5 + react-router-dom 7 + zustand 5 + @tanstack/react-query 5 + react-hook-form 7 + zod 3 + sonner 2 + radix-ui (shadcn). 테스트: vitest 2 + @testing-library/react 16 + jsdom 25 + msw 2. **신규 npm 의존**: `@radix-ui/react-dropdown-menu`, `@radix-ui/react-tooltip`, `@radix-ui/react-dialog` (모두 14b까지 미설치 상태, G1에서 추가).

**Package Manager:** pnpm (lockfile `pnpm-lock.yaml`).

**Branch:** `feature/admin-platform-frontend` (PR 14a~14d 장기 통합 브랜치, 14c 시작 HEAD `22d0b80`).

**Working dir:** `C:/Users/Eisen/Desktop/Labs/[projects] pfplay/pfplay-admin`.

**JDK 환경**: 백엔드는 다른 레포(`pfplay-platform`). 본 PR은 frontend only — JDK 무관.

---

## 0. Scope Check

PR 14c는 단일 sub-PR로 spec §1.1 목적만 다룸 (단건 mutation 7개). 분리 가능한 독립 sub-system 없음 — 모든 mutation은 14b detail page 위에 dropdown + dialog 패턴으로 응집. bulk-action(B-8)은 14d (spec §1.2 명시 제외), penalty/avatar는 14d+ (백엔드 endpoint 부재). 단일 plan으로 진행.

---

## 1. File Structure

### 1.1 신규 파일

| 경로 | 종류 | 책임 |
|---|---|---|
| `package.json` | 수정 | `@radix-ui/react-dropdown-menu`, `@radix-ui/react-tooltip`, `@radix-ui/react-dialog` 추가 |
| `src/components/ui/dropdown-menu.tsx` | 신규 | shadcn DropdownMenu wrapper |
| `src/components/ui/tooltip.tsx` | 신규 | shadcn Tooltip wrapper |
| `src/components/ui/dialog.tsx` | 신규 | shadcn Dialog wrapper |
| `src/shared/lib/mutation-toast.ts` | 신규 | `mutationSuccessToast(msg)` + `mutationErrorToast(err)` (ApiError → errorCode 표시) |
| `src/shared/lib/__tests__/mutation-toast.test.ts` | 신규 | unit (success / ApiError / generic 분기) |
| `src/entities/member/model/types.ts` | 수정 | `AdminMemberTierChangeResponse`, `AdminMemberWithdrawResponse` 인터페이스 추가 |
| `src/features/members/api/members-api.ts` | 수정 | `changeMemberTier(id, body)`, `withdrawMember(id)` 추가 |
| `src/features/members/api/__tests__/members-api.test.ts` | 수정 | mutation 테스트 4 케이스 추가 |
| `src/features/members/api/use-change-member-tier.ts` | 신규 | useMutation hook |
| `src/features/members/api/use-withdraw-member.ts` | 신규 | useMutation hook |
| `src/features/members/api/__tests__/use-change-member-tier.test.ts` | 신규 | hook 테스트 |
| `src/features/members/api/__tests__/use-withdraw-member.test.ts` | 신규 | hook 테스트 (alreadyWithdrawn 분기) |
| `src/features/members/model/mutation-schema.ts` | 신규 | zod (TierChangeRequest) |
| `src/features/members/model/__tests__/mutation-schema.test.ts` | 신규 | zod unit |
| `src/features/members/ui/members-actions-dropdown.tsx` | 신규 | 헤더 dropdown (활성 dialog id state) |
| `src/features/members/ui/__tests__/members-actions-dropdown.test.tsx` | 신규 | component |
| `src/features/members/ui/mutation-dialogs/change-tier-dialog.tsx` | 신규 | form 모달 (FM/AM/GT select) |
| `src/features/members/ui/mutation-dialogs/withdraw-dialog.tsx` | 신규 | confirm 모달 (idempotent 분기) |
| `src/features/members/ui/mutation-dialogs/__tests__/change-tier-dialog.test.tsx` | 신규 | component |
| `src/features/members/ui/mutation-dialogs/__tests__/withdraw-dialog.test.tsx` | 신규 | component |
| `src/features/partyrooms/api/partyrooms-api.ts` | 수정 | 5 mutation fn 추가 |
| `src/features/partyrooms/api/__tests__/partyrooms-api.test.ts` | 수정 | mutation 테스트 7 케이스 추가 |
| `src/features/partyrooms/api/use-terminate-partyroom.ts` | 신규 | useMutation |
| `src/features/partyrooms/api/use-suspend-partyroom.ts` | 신규 | useMutation |
| `src/features/partyrooms/api/use-restore-partyroom.ts` | 신규 | useMutation |
| `src/features/partyrooms/api/use-update-partyroom-meta.ts` | 신규 | useMutation |
| `src/features/partyrooms/api/use-update-partyroom-display-flag.ts` | 신규 | useMutation |
| `src/features/partyrooms/api/__tests__/use-terminate-partyroom.test.ts` | 신규 | hook |
| `src/features/partyrooms/api/__tests__/use-suspend-partyroom.test.ts` | 신규 | hook |
| `src/features/partyrooms/api/__tests__/use-restore-partyroom.test.ts` | 신규 | hook |
| `src/features/partyrooms/api/__tests__/use-update-partyroom-meta.test.ts` | 신규 | hook |
| `src/features/partyrooms/api/__tests__/use-update-partyroom-display-flag.test.ts` | 신규 | hook |
| `src/features/partyrooms/model/mutation-schema.ts` | 신규 | zod (Terminate/Suspend reason, UpdateMeta refine, DisplayFlag enum) |
| `src/features/partyrooms/model/__tests__/mutation-schema.test.ts` | 신규 | zod unit |
| `src/features/partyrooms/ui/partyrooms-actions-dropdown.tsx` | 신규 | 헤더 dropdown (status-aware disabled) |
| `src/features/partyrooms/ui/__tests__/partyrooms-actions-dropdown.test.tsx` | 신규 | component (disable 매트릭스 5 케이스) |
| `src/features/partyrooms/ui/mutation-dialogs/terminate-dialog.tsx` | 신규 | form (reason) |
| `src/features/partyrooms/ui/mutation-dialogs/suspend-dialog.tsx` | 신규 | form (reason) |
| `src/features/partyrooms/ui/mutation-dialogs/restore-dialog.tsx` | 신규 | confirm |
| `src/features/partyrooms/ui/mutation-dialogs/update-meta-dialog.tsx` | 신규 | form (title/intro/limit + refine) |
| `src/features/partyrooms/ui/mutation-dialogs/display-flag-dialog.tsx` | 신규 | form (NORMAL/FEATURED/HIDDEN) |
| `src/features/partyrooms/ui/mutation-dialogs/__tests__/(5×.test.tsx)` | 신규 | component |
| `src/test/mocks/handlers/members.ts` | 수정 | tier/withdraw mutation handlers + idempotent withdraw |
| `src/test/mocks/handlers/partyrooms.ts` | 수정 | 5 mutation handlers + 204 응답 |
| `src/test/mocks/fixtures/members.ts` | 수정 | tier/withdraw response fixture 추가 |
| `src/test/mocks/fixtures/partyrooms.ts` | 수정 | error response fixture (ALREADY_TERMINATED, ILLEGAL_STATE_TRANSITION 등) |

### 1.2 수정 widget

| 경로 | 변경 |
|---|---|
| `src/widgets/members-detail.tsx` | 헤더 우측에 `<MembersActionsDropdown memberId={...} currentTier={...} displayName={...} />` 부착 |
| `src/widgets/partyrooms-detail.tsx` | 헤더 우측에 `<PartyroomsActionsDropdown partyroom={detail} />` 부착 |

---

## 2. TDD 컨벤션 (모든 chunk 공통)

1. **RED → GREEN → REFACTOR**: 매 task는 실패하는 테스트부터. 테스트 통과 코드는 minimal.
2. **테스트 도구**: vitest run (`pnpm exec vitest run <path>` 또는 `pnpm test:run`). UI 모드 사용 안 함.
3. **type check**: chunk 끝마다 `pnpm exec tsc --noEmit` 0 error 확인.
4. **빌드 sanity**: 마지막 chunk(G6) 끝에 `pnpm build` 1회.
5. **commit 단위**: task 단위 commit 권장. chunk 끝에 sanity commit 추가 (linter/typecheck/포매팅 정리).
6. **shadcn Select / Dropdown 테스트 idiom (14b 계승, §14 entry 8/9)**:
   - `userEvent.setup({ pointerEventsCheck: 0, delay: null })`
   - `getByRole("combobox")` / `getByRole("button", { name: /actions/i })`
   - 옵션 노출은 `await user.click(trigger)` 후 `findByRole("option", { name })`
   - jsdom polyfill은 14b에서 이미 `src/test/setup.ts`에 박혀 있음 (`hasPointerCapture` 등 4 메소드). dropdown-menu/dialog 추가에 polyfill 추가 필요 시 G1에서 확장.
7. **react-query test 골격** (모든 mutation hook 테스트 공통):
   ```ts
   import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
   import { renderHook, waitFor } from "@testing-library/react"

   function makeWrapper() {
     const qc = new QueryClient({ defaultOptions: { mutations: { retry: false }, queries: { retry: false } } })
     const wrapper = ({ children }: { children: React.ReactNode }) => (
       <QueryClientProvider client={qc}>{children}</QueryClientProvider>
     )
     return { qc, wrapper }
   }
   ```
8. **toast spy 패턴**: `vi.spyOn(toast, "success")` / `vi.spyOn(toast, "error")` (14b §14 entry 10).
9. **invalidate 스파이**: `const spy = vi.spyOn(qc, "invalidateQueries")` 후 `expect(spy).toHaveBeenCalledWith({ queryKey: ["members"] })`.

---

## Chunk G1: Shared Mutation Infra

**목표**: mutation-toast 헬퍼 + shadcn Dialog/DropdownMenu/Tooltip 설치. 도메인 mutation 0개. G2~G5의 토대.

### Task G1.1: shadcn 의존 추가

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 의존 패키지 설치**

```bash
JAVA_HOME="C:/Users/Eisen/.jdks/ms-21.0.7" pnpm add @radix-ui/react-dropdown-menu @radix-ui/react-tooltip @radix-ui/react-dialog
```

(JDK env prefix는 user 메모리 — pfplay-platform용이므로 frontend pnpm엔 무관하지만 일관 prefix 유지. 필요 없으면 떼도 무방.)

- [ ] **Step 2: 설치 확인**

Run: `pnpm list @radix-ui/react-dropdown-menu @radix-ui/react-tooltip @radix-ui/react-dialog`
Expected: 세 패키지 모두 lock에 등록.

- [ ] **Step 3: type check sanity**

Run: `pnpm exec tsc --noEmit`
Expected: 0 error (의존 추가만으로는 영향 없어야 함).

- [ ] **Step 4: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "chore(deps): add radix-ui dropdown-menu/tooltip/dialog for PR 14c (G1.1)"
```

### Task G1.2: shadcn `dropdown-menu.tsx` 컴포넌트

**Files:**
- Create: `src/components/ui/dropdown-menu.tsx`

- [ ] **Step 1: shadcn 공식 source를 wrapper로 작성**

```tsx
"use client"
import * as DropdownMenuPrimitive from "@radix-ui/react-dropdown-menu"
import * as React from "react"
import { cn } from "@/shared/lib/utils"
import { Check, ChevronRight, Circle } from "lucide-react"

const DropdownMenu = DropdownMenuPrimitive.Root
const DropdownMenuTrigger = DropdownMenuPrimitive.Trigger
const DropdownMenuGroup = DropdownMenuPrimitive.Group
const DropdownMenuPortal = DropdownMenuPrimitive.Portal
const DropdownMenuSub = DropdownMenuPrimitive.Sub
const DropdownMenuRadioGroup = DropdownMenuPrimitive.RadioGroup

const DropdownMenuContent = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Content>
>(({ className, sideOffset = 4, ...props }, ref) => (
  <DropdownMenuPrimitive.Portal>
    <DropdownMenuPrimitive.Content
      ref={ref}
      sideOffset={sideOffset}
      className={cn(
        "z-50 min-w-[8rem] overflow-hidden rounded-md border bg-popover p-1 text-popover-foreground shadow-md",
        "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0",
        className,
      )}
      {...props}
    />
  </DropdownMenuPrimitive.Portal>
))
DropdownMenuContent.displayName = DropdownMenuPrimitive.Content.displayName

const DropdownMenuItem = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Item>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Item> & { inset?: boolean }
>(({ className, inset, ...props }, ref) => (
  <DropdownMenuPrimitive.Item
    ref={ref}
    className={cn(
      "relative flex cursor-default select-none items-center rounded-sm px-2 py-1.5 text-sm outline-none transition-colors",
      "focus:bg-accent focus:text-accent-foreground",
      "data-[disabled]:pointer-events-none data-[disabled]:opacity-50",
      inset && "pl-8",
      className,
    )}
    {...props}
  />
))
DropdownMenuItem.displayName = DropdownMenuPrimitive.Item.displayName

const DropdownMenuLabel = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Label>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Label>
>(({ className, ...props }, ref) => (
  <DropdownMenuPrimitive.Label
    ref={ref}
    className={cn("px-2 py-1.5 text-sm font-semibold", className)}
    {...props}
  />
))
DropdownMenuLabel.displayName = DropdownMenuPrimitive.Label.displayName

const DropdownMenuSeparator = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Separator>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Separator>
>(({ className, ...props }, ref) => (
  <DropdownMenuPrimitive.Separator
    ref={ref}
    className={cn("-mx-1 my-1 h-px bg-muted", className)}
    {...props}
  />
))
DropdownMenuSeparator.displayName = DropdownMenuPrimitive.Separator.displayName

export {
  DropdownMenu,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuGroup,
  DropdownMenuPortal,
  DropdownMenuSub,
  DropdownMenuRadioGroup,
}
```

- [ ] **Step 2: type check**

Run: `pnpm exec tsc --noEmit`
Expected: 0 error.

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/dropdown-menu.tsx
git commit -m "feat(ui): add shadcn DropdownMenu component (G1.2)"
```

### Task G1.3: shadcn `tooltip.tsx`, `dialog.tsx`

**Files:**
- Create: `src/components/ui/tooltip.tsx`, `src/components/ui/dialog.tsx`

- [ ] **Step 1: tooltip.tsx 작성**

```tsx
"use client"
import * as TooltipPrimitive from "@radix-ui/react-tooltip"
import * as React from "react"
import { cn } from "@/shared/lib/utils"

const TooltipProvider = TooltipPrimitive.Provider
const Tooltip = TooltipPrimitive.Root
const TooltipTrigger = TooltipPrimitive.Trigger

const TooltipContent = React.forwardRef<
  React.ElementRef<typeof TooltipPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof TooltipPrimitive.Content>
>(({ className, sideOffset = 4, ...props }, ref) => (
  <TooltipPrimitive.Content
    ref={ref}
    sideOffset={sideOffset}
    className={cn(
      "z-50 overflow-hidden rounded-md border bg-popover px-3 py-1.5 text-sm text-popover-foreground shadow-md",
      "data-[state=delayed-open]:animate-in data-[state=closed]:animate-out",
      className,
    )}
    {...props}
  />
))
TooltipContent.displayName = TooltipPrimitive.Content.displayName

export { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider }
```

- [ ] **Step 2: dialog.tsx 작성**

```tsx
"use client"
import * as DialogPrimitive from "@radix-ui/react-dialog"
import { X } from "lucide-react"
import * as React from "react"
import { cn } from "@/shared/lib/utils"

const Dialog = DialogPrimitive.Root
const DialogTrigger = DialogPrimitive.Trigger
const DialogPortal = DialogPrimitive.Portal
const DialogClose = DialogPrimitive.Close

const DialogOverlay = React.forwardRef<
  React.ElementRef<typeof DialogPrimitive.Overlay>,
  React.ComponentPropsWithoutRef<typeof DialogPrimitive.Overlay>
>(({ className, ...props }, ref) => (
  <DialogPrimitive.Overlay
    ref={ref}
    className={cn(
      "fixed inset-0 z-50 bg-black/80",
      "data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0",
      className,
    )}
    {...props}
  />
))
DialogOverlay.displayName = DialogPrimitive.Overlay.displayName

const DialogContent = React.forwardRef<
  React.ElementRef<typeof DialogPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof DialogPrimitive.Content>
>(({ className, children, ...props }, ref) => (
  <DialogPortal>
    <DialogOverlay />
    <DialogPrimitive.Content
      ref={ref}
      className={cn(
        "fixed left-[50%] top-[50%] z-50 grid w-full max-w-lg translate-x-[-50%] translate-y-[-50%] gap-4 border bg-background p-6 shadow-lg sm:rounded-lg",
        "data-[state=open]:animate-in data-[state=closed]:animate-out",
        className,
      )}
      {...props}
    >
      {children}
      <DialogPrimitive.Close className="absolute right-4 top-4 rounded-sm opacity-70 ring-offset-background transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 disabled:pointer-events-none">
        <X className="h-4 w-4" />
        <span className="sr-only">Close</span>
      </DialogPrimitive.Close>
    </DialogPrimitive.Content>
  </DialogPortal>
))
DialogContent.displayName = DialogPrimitive.Content.displayName

const DialogHeader = ({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) => (
  <div className={cn("flex flex-col space-y-1.5 text-center sm:text-left", className)} {...props} />
)
DialogHeader.displayName = "DialogHeader"

const DialogFooter = ({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) => (
  <div className={cn("flex flex-col-reverse sm:flex-row sm:justify-end sm:space-x-2", className)} {...props} />
)
DialogFooter.displayName = "DialogFooter"

const DialogTitle = React.forwardRef<
  React.ElementRef<typeof DialogPrimitive.Title>,
  React.ComponentPropsWithoutRef<typeof DialogPrimitive.Title>
>(({ className, ...props }, ref) => (
  <DialogPrimitive.Title ref={ref} className={cn("text-lg font-semibold leading-none tracking-tight", className)} {...props} />
))
DialogTitle.displayName = DialogPrimitive.Title.displayName

const DialogDescription = React.forwardRef<
  React.ElementRef<typeof DialogPrimitive.Description>,
  React.ComponentPropsWithoutRef<typeof DialogPrimitive.Description>
>(({ className, ...props }, ref) => (
  <DialogPrimitive.Description ref={ref} className={cn("text-sm text-muted-foreground", className)} {...props} />
))
DialogDescription.displayName = DialogPrimitive.Description.displayName

export {
  Dialog,
  DialogPortal,
  DialogOverlay,
  DialogClose,
  DialogTrigger,
  DialogContent,
  DialogHeader,
  DialogFooter,
  DialogTitle,
  DialogDescription,
}
```

- [ ] **Step 3: type check**

Run: `pnpm exec tsc --noEmit`
Expected: 0 error.

- [ ] **Step 4: Commit**

```bash
git add src/components/ui/tooltip.tsx src/components/ui/dialog.tsx
git commit -m "feat(ui): add shadcn Tooltip + Dialog components (G1.3)"
```

### Task G1.4: `mutation-toast.ts` 헬퍼 (TDD)

**Files:**
- Create: `src/shared/lib/mutation-toast.ts`
- Test: `src/shared/lib/__tests__/mutation-toast.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

```ts
// src/shared/lib/__tests__/mutation-toast.test.ts
import { describe, expect, it, vi, beforeEach } from "vitest"
import { toast } from "sonner"
import { ApiError } from "@/shared/api/error"
import { mutationSuccessToast, mutationErrorToast } from "@/shared/lib/mutation-toast"

describe("mutation-toast", () => {
  beforeEach(() => {
    vi.spyOn(toast, "success").mockImplementation(() => "")
    vi.spyOn(toast, "error").mockImplementation(() => "")
  })

  it("mutationSuccessToast calls toast.success with message", () => {
    mutationSuccessToast("등급 변경 완료")
    expect(toast.success).toHaveBeenCalledWith("등급 변경 완료")
  })

  it("mutationErrorToast surfaces ApiError errorCode + status", () => {
    const err = new ApiError(403, "ALREADY_TERMINATED", "이미 종료됨")
    mutationErrorToast(err)
    expect(toast.error).toHaveBeenCalledWith("이미 종료됨", {
      description: "code: ALREADY_TERMINATED (status: 403)",
    })
  })

  it("mutationErrorToast falls back to generic for non-ApiError", () => {
    mutationErrorToast(new Error("network down"))
    expect(toast.error).toHaveBeenCalledWith("요청 실패")
  })
})
```

- [ ] **Step 2: 테스트 실행 (FAIL 확인)**

Run: `pnpm exec vitest run src/shared/lib/__tests__/mutation-toast.test.ts`
Expected: FAIL with `mutationSuccessToast is not exported` 등.

- [ ] **Step 3: 최소 구현**

```ts
// src/shared/lib/mutation-toast.ts
import { toast } from "sonner"
import { ApiError } from "@/shared/api/error"

export function mutationSuccessToast(message: string): void {
  toast.success(message)
}

export function mutationErrorToast(err: unknown): void {
  if (err instanceof ApiError) {
    toast.error(err.message ?? "요청 실패", {
      description: `code: ${err.errorCode ?? "UNKNOWN"} (status: ${err.status})`,
    })
  } else {
    toast.error("요청 실패")
  }
}
```

- [ ] **Step 4: 테스트 실행 (PASS 확인)**

Run: `pnpm exec vitest run src/shared/lib/__tests__/mutation-toast.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add src/shared/lib/mutation-toast.ts src/shared/lib/__tests__/mutation-toast.test.ts
git commit -m "feat(shared): add mutation-toast helper (success / ApiError 분기 / generic fallback) (G1.4)"
```

### Task G1.5: chunk sanity

- [ ] **Step 1: 전체 테스트 실행**

Run: `pnpm test:run`
Expected: 14b 77/77 + G1.4 신규 3 = 80/80 PASS.

- [ ] **Step 2: typecheck**

Run: `pnpm exec tsc --noEmit`
Expected: 0 error.

- [ ] **Step 3: chunk closing commit (있다면)**

(없으면 skip — task별 commit으로 충분)

---

## Chunk G2: 회원 도메인 mutation (changeTier + withdraw + dropdown)

**목표**: member detail page에 dropdown 통합 + 2 mutation 완결.

### Task G2.1: 타입 추가 (TDD 무관, 인터페이스 정의)

**Files:**
- Modify: `src/entities/member/model/types.ts`

- [ ] **Step 1: 응답 타입 추가**

기존 파일 끝에 append:

```ts
export interface AdminMemberTierChangeResponse {
  memberId: number
  oldTier: AuthorityTier
  newTier: AuthorityTier
}

export interface AdminMemberWithdrawResponse {
  memberId: number
  userAccountId: number
  withdrawnAt: string  // LocalDateTime ISO (KST)
  alreadyWithdrawn: boolean
}
```

- [ ] **Step 2: type check**

Run: `pnpm exec tsc --noEmit`
Expected: 0 error.

- [ ] **Step 3: Commit**

```bash
git add src/entities/member/model/types.ts
git commit -m "feat(entities): add AdminMemberTierChange/WithdrawResponse types (G2.1)"
```

### Task G2.2: zod mutation-schema (TDD)

**Files:**
- Create: `src/features/members/model/mutation-schema.ts`
- Test: `src/features/members/model/__tests__/mutation-schema.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
import { describe, expect, it } from "vitest"
import { changeMemberTierRequestSchema } from "@/features/members/model/mutation-schema"

describe("changeMemberTierRequestSchema", () => {
  it("accepts FM/AM/GT", () => {
    expect(changeMemberTierRequestSchema.parse({ tier: "FM" })).toEqual({ tier: "FM" })
    expect(changeMemberTierRequestSchema.parse({ tier: "AM" })).toEqual({ tier: "AM" })
    expect(changeMemberTierRequestSchema.parse({ tier: "GT" })).toEqual({ tier: "GT" })
  })

  it("rejects unknown tier", () => {
    expect(() => changeMemberTierRequestSchema.parse({ tier: "XYZ" })).toThrow()
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

```ts
// src/features/members/model/mutation-schema.ts
import { z } from "zod"
import { TierEnum } from "./filter-schema"  // 14b에서 정의된 z.enum(["FM","AM","GT"]) 재사용

export const changeMemberTierRequestSchema = z.object({
  tier: TierEnum,
})

export type ChangeMemberTierRequest = z.infer<typeof changeMemberTierRequestSchema>
```

(14b의 `TierEnum` 위치 확인 필요 — `features/members/model/filter-schema.ts`에 export되어 있음. 없으면 신규 정의.)

- [ ] **Step 3: 테스트 PASS 확인**

Run: `pnpm exec vitest run src/features/members/model/__tests__/mutation-schema.test.ts`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/features/members/model/mutation-schema.ts src/features/members/model/__tests__/mutation-schema.test.ts
git commit -m "feat(members): add changeMemberTierRequest zod schema (G2.2)"
```

### Task G2.3: members-api.ts 확장 + 테스트

**Files:**
- Modify: `src/features/members/api/members-api.ts`
- Modify: `src/features/members/api/__tests__/members-api.test.ts`

- [ ] **Step 1: 실패 테스트 추가**

기존 파일 끝에 append:

```ts
describe("changeMemberTier", () => {
  it("PATCH /admin/members/:id/tier with body and unwraps response", async () => {
    server.use(
      http.patch("*/api/v1/admin/members/1/tier", async ({ request }) => {
        const body = await request.json()
        expect(body).toEqual({ tier: "FM" })
        return HttpResponse.json({ data: { memberId: 1, oldTier: "AM", newTier: "FM" } })
      }),
    )
    const res = await changeMemberTier(1, { tier: "FM" })
    expect(res).toEqual({ memberId: 1, oldTier: "AM", newTier: "FM" })
  })

  it("propagates ApiError on 400 TIER_UNCHANGED", async () => {
    server.use(
      http.patch("*/api/v1/admin/members/1/tier", () =>
        HttpResponse.json({ status: 400, errorCode: "TIER_UNCHANGED", message: "동일 등급" }, { status: 400 }),
      ),
    )
    await expect(changeMemberTier(1, { tier: "FM" })).rejects.toMatchObject({
      status: 400,
      errorCode: "TIER_UNCHANGED",
    })
  })
})

describe("withdrawMember", () => {
  it("POST /admin/members/:id/withdraw and unwraps", async () => {
    server.use(
      http.post("*/api/v1/admin/members/1/withdraw", () =>
        HttpResponse.json({
          data: { memberId: 1, userAccountId: 100, withdrawnAt: "2026-04-29T10:00:00", alreadyWithdrawn: false },
        }),
      ),
    )
    const res = await withdrawMember(1)
    expect(res.alreadyWithdrawn).toBe(false)
    expect(res.withdrawnAt).toBe("2026-04-29T10:00:00")
  })

  it("returns alreadyWithdrawn=true on idempotent re-call", async () => {
    server.use(
      http.post("*/api/v1/admin/members/1/withdraw", () =>
        HttpResponse.json({
          data: { memberId: 1, userAccountId: 100, withdrawnAt: "2026-04-20T12:00:00", alreadyWithdrawn: true },
        }),
      ),
    )
    const res = await withdrawMember(1)
    expect(res.alreadyWithdrawn).toBe(true)
  })
})
```

(`changeMemberTier`, `withdrawMember` import도 파일 상단에 추가.)

Run: FAIL with import errors.

- [ ] **Step 2: 구현**

`src/features/members/api/members-api.ts` 끝에 append:

```ts
import type { ChangeMemberTierRequest } from "../model/mutation-schema"
import type { AdminMemberTierChangeResponse, AdminMemberWithdrawResponse } from "@/entities/member/model/types"

export async function changeMemberTier(
  memberId: number,
  body: ChangeMemberTierRequest,
): Promise<AdminMemberTierChangeResponse> {
  const res = await http<ApiCommonResponse<AdminMemberTierChangeResponse>>(
    `/api/v1/admin/members/${memberId}/tier`,
    { method: "PATCH", body },
  )
  return unwrap(res)
}

export async function withdrawMember(memberId: number): Promise<AdminMemberWithdrawResponse> {
  const res = await http<ApiCommonResponse<AdminMemberWithdrawResponse>>(
    `/api/v1/admin/members/${memberId}/withdraw`,
    { method: "POST" },
  )
  return unwrap(res)
}
```

(import 경로는 14b 기존 import 컨벤션 따름. `http`, `ApiCommonResponse`, `unwrap`은 이미 import되어 있을 것.)

- [ ] **Step 3: 테스트 PASS 확인**

Run: `pnpm exec vitest run src/features/members/api/__tests__/members-api.test.ts`
Expected: PASS (4 신규 추가).

- [ ] **Step 4: Commit**

```bash
git add src/features/members/api/members-api.ts src/features/members/api/__tests__/members-api.test.ts
git commit -m "feat(members): add changeMemberTier + withdrawMember API fns (G2.3)"
```

### Task G2.4: useChangeMemberTier hook (TDD)

**Files:**
- Create: `src/features/members/api/use-change-member-tier.ts`
- Test: `src/features/members/api/__tests__/use-change-member-tier.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
import { describe, expect, it, vi } from "vitest"
import { renderHook, waitFor } from "@testing-library/react"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { http, HttpResponse } from "msw"
import { server } from "@/test/mocks/server"
import { toast } from "sonner"
import { useChangeMemberTier } from "@/features/members/api/use-change-member-tier"

function makeWrapper() {
  const qc = new QueryClient({
    defaultOptions: { mutations: { retry: false }, queries: { retry: false } },
  })
  const wrapper = ({ children }: { children: React.ReactNode }) => (
    <QueryClientProvider client={qc}>{children}</QueryClientProvider>
  )
  return { qc, wrapper }
}

describe("useChangeMemberTier", () => {
  it("on success: invalidates ['members'] prefix, toast.success", async () => {
    const { qc, wrapper } = makeWrapper()
    const invalidateSpy = vi.spyOn(qc, "invalidateQueries")
    const successSpy = vi.spyOn(toast, "success").mockImplementation(() => "")
    server.use(
      http.patch("*/api/v1/admin/members/1/tier", () =>
        HttpResponse.json({ data: { memberId: 1, oldTier: "AM", newTier: "FM" } }),
      ),
    )

    const { result } = renderHook(() => useChangeMemberTier(), { wrapper })
    result.current.mutate({ memberId: 1, tier: "FM" })
    await waitFor(() => expect(result.current.isSuccess).toBe(true))

    expect(invalidateSpy).toHaveBeenCalledWith({ queryKey: ["members"] })
    expect(successSpy).toHaveBeenCalled()
  })

  it("on ApiError: mutationErrorToast called", async () => {
    const { wrapper } = makeWrapper()
    const errorSpy = vi.spyOn(toast, "error").mockImplementation(() => "")
    server.use(
      http.patch("*/api/v1/admin/members/1/tier", () =>
        HttpResponse.json({ status: 400, errorCode: "TIER_UNCHANGED", message: "동일 등급" }, { status: 400 }),
      ),
    )

    const { result } = renderHook(() => useChangeMemberTier(), { wrapper })
    result.current.mutate({ memberId: 1, tier: "FM" })
    await waitFor(() => expect(result.current.isError).toBe(true))

    expect(errorSpy).toHaveBeenCalledWith("동일 등급", {
      description: "code: TIER_UNCHANGED (status: 400)",
    })
  })
})
```

Run: FAIL (hook 미존재).

- [ ] **Step 2: 구현**

```ts
// src/features/members/api/use-change-member-tier.ts
import { useMutation, useQueryClient } from "@tanstack/react-query"
import { changeMemberTier } from "./members-api"
import { mutationSuccessToast, mutationErrorToast } from "@/shared/lib/mutation-toast"
import type { AuthorityTier } from "@/entities/member/model/types"

export function useChangeMemberTier() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (vars: { memberId: number; tier: AuthorityTier }) =>
      changeMemberTier(vars.memberId, { tier: vars.tier }),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["members"] })  // prefix — 14b list+detail 모두 stale
      mutationSuccessToast("등급 변경 완료")
    },
    onError: mutationErrorToast,
  })
}
```

- [ ] **Step 3: 테스트 PASS**

Run: `pnpm exec vitest run src/features/members/api/__tests__/use-change-member-tier.test.ts`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/features/members/api/use-change-member-tier.ts src/features/members/api/__tests__/use-change-member-tier.test.ts
git commit -m "feat(members): add useChangeMemberTier mutation hook (G2.4)"
```

### Task G2.5: useWithdrawMember hook (TDD, alreadyWithdrawn 분기 포함)

**Files:**
- Create: `src/features/members/api/use-withdraw-member.ts`
- Test: `src/features/members/api/__tests__/use-withdraw-member.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
import { describe, expect, it, vi } from "vitest"
import { renderHook, waitFor } from "@testing-library/react"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { http, HttpResponse } from "msw"
import { server } from "@/test/mocks/server"
import { toast } from "sonner"
import { useWithdrawMember } from "@/features/members/api/use-withdraw-member"

function makeWrapper() {
  const qc = new QueryClient({
    defaultOptions: { mutations: { retry: false }, queries: { retry: false } },
  })
  const wrapper = ({ children }: { children: React.ReactNode }) => (
    <QueryClientProvider client={qc}>{children}</QueryClientProvider>
  )
  return { qc, wrapper }
}

describe("useWithdrawMember", () => {
  it("on first withdrawal (alreadyWithdrawn=false): toast '탈퇴 처리 완료'", async () => {
    const { wrapper } = makeWrapper()
    const successSpy = vi.spyOn(toast, "success").mockImplementation(() => "")
    server.use(
      http.post("*/api/v1/admin/members/1/withdraw", () =>
        HttpResponse.json({
          data: { memberId: 1, userAccountId: 100, withdrawnAt: "2026-04-29T10:00:00", alreadyWithdrawn: false },
        }),
      ),
    )
    const { result } = renderHook(() => useWithdrawMember(), { wrapper })
    result.current.mutate({ memberId: 1 })
    await waitFor(() => expect(result.current.isSuccess).toBe(true))
    expect(successSpy).toHaveBeenCalledWith("탈퇴 처리 완료")
  })

  it("on idempotent re-call (alreadyWithdrawn=true): toast '이미 탈퇴된 회원입니다'", async () => {
    const { wrapper } = makeWrapper()
    const successSpy = vi.spyOn(toast, "success").mockImplementation(() => "")
    server.use(
      http.post("*/api/v1/admin/members/1/withdraw", () =>
        HttpResponse.json({
          data: { memberId: 1, userAccountId: 100, withdrawnAt: "2026-04-20T12:00:00", alreadyWithdrawn: true },
        }),
      ),
    )
    const { result } = renderHook(() => useWithdrawMember(), { wrapper })
    result.current.mutate({ memberId: 1 })
    await waitFor(() => expect(result.current.isSuccess).toBe(true))
    expect(successSpy).toHaveBeenCalledWith("이미 탈퇴된 회원입니다")
  })

  it("on success: invalidates ['members'] prefix", async () => {
    const { qc, wrapper } = makeWrapper()
    const invalidateSpy = vi.spyOn(qc, "invalidateQueries")
    server.use(
      http.post("*/api/v1/admin/members/1/withdraw", () =>
        HttpResponse.json({
          data: { memberId: 1, userAccountId: 100, withdrawnAt: "2026-04-29T10:00:00", alreadyWithdrawn: false },
        }),
      ),
    )
    const { result } = renderHook(() => useWithdrawMember(), { wrapper })
    result.current.mutate({ memberId: 1 })
    await waitFor(() => expect(result.current.isSuccess).toBe(true))
    expect(invalidateSpy).toHaveBeenCalledWith({ queryKey: ["members"] })
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

```ts
// src/features/members/api/use-withdraw-member.ts
import { useMutation, useQueryClient } from "@tanstack/react-query"
import { withdrawMember } from "./members-api"
import { mutationSuccessToast, mutationErrorToast } from "@/shared/lib/mutation-toast"

export function useWithdrawMember() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (vars: { memberId: number }) => withdrawMember(vars.memberId),
    onSuccess: (data) => {
      qc.invalidateQueries({ queryKey: ["members"] })
      mutationSuccessToast(data.alreadyWithdrawn ? "이미 탈퇴된 회원입니다" : "탈퇴 처리 완료")
    },
    onError: mutationErrorToast,
  })
}
```

- [ ] **Step 3: 테스트 PASS**

Run: `pnpm exec vitest run src/features/members/api/__tests__/use-withdraw-member.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 4: Commit**

```bash
git add src/features/members/api/use-withdraw-member.ts src/features/members/api/__tests__/use-withdraw-member.test.ts
git commit -m "feat(members): add useWithdrawMember hook with alreadyWithdrawn branching (G2.5)"
```

### Task G2.6: change-tier-dialog (TDD)

**Files:**
- Create: `src/features/members/ui/mutation-dialogs/change-tier-dialog.tsx`
- Test: `src/features/members/ui/mutation-dialogs/__tests__/change-tier-dialog.test.tsx`

- [ ] **Step 1: 실패 테스트**

```tsx
import { describe, expect, it, vi } from "vitest"
import { render, screen } from "@testing-library/react"
import userEvent from "@testing-library/user-event"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { http, HttpResponse } from "msw"
import { server } from "@/test/mocks/server"
import { ChangeTierDialog } from "@/features/members/ui/mutation-dialogs/change-tier-dialog"

function renderWithClient(ui: React.ReactNode) {
  const qc = new QueryClient({ defaultOptions: { mutations: { retry: false } } })
  return render(<QueryClientProvider client={qc}>{ui}</QueryClientProvider>)
}

describe("ChangeTierDialog", () => {
  it("disables submit when selected tier matches currentTier", async () => {
    const onOpenChange = vi.fn()
    renderWithClient(
      <ChangeTierDialog memberId={1} currentTier="FM" open={true} onOpenChange={onOpenChange} />,
    )
    const submit = screen.getByRole("button", { name: /변경/ })
    expect(submit).toBeDisabled()
  })

  it("submits and closes dialog on success", async () => {
    server.use(
      http.patch("*/api/v1/admin/members/1/tier", () =>
        HttpResponse.json({ data: { memberId: 1, oldTier: "AM", newTier: "FM" } }),
      ),
    )
    const onOpenChange = vi.fn()
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(
      <ChangeTierDialog memberId={1} currentTier="AM" open={true} onOpenChange={onOpenChange} />,
    )

    await user.click(screen.getByRole("combobox"))
    await user.click(await screen.findByRole("option", { name: /FM/ }))
    await user.click(screen.getByRole("button", { name: /변경/ }))

    await waitFor(() => expect(onOpenChange).toHaveBeenCalledWith(false))
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

```tsx
// src/features/members/ui/mutation-dialogs/change-tier-dialog.tsx
import { useState, useEffect } from "react"
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { useChangeMemberTier } from "@/features/members/api/use-change-member-tier"
import type { AuthorityTier } from "@/entities/member/model/types"

const TIER_LABELS: Record<AuthorityTier, string> = {
  FM: "FM (정회원)",
  AM: "AM (준회원)",
  GT: "GT (게스트, 강등)",
}

interface Props {
  memberId: number
  currentTier: AuthorityTier
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function ChangeTierDialog({ memberId, currentTier, open, onOpenChange }: Props) {
  const [selected, setSelected] = useState<AuthorityTier>(currentTier)
  const mutation = useChangeMemberTier()

  useEffect(() => {
    if (!open) {
      setSelected(currentTier)
      mutation.reset()
    }
  }, [open, currentTier])

  const isSame = selected === currentTier
  const isDisabled = isSame || mutation.isPending

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    mutation.mutate(
      { memberId, tier: selected },
      { onSuccess: () => onOpenChange(false) },
    )
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <form onSubmit={handleSubmit}>
          <DialogHeader>
            <DialogTitle>회원 등급 변경</DialogTitle>
            <DialogDescription>
              현재 등급: <strong>{TIER_LABELS[currentTier]}</strong>
            </DialogDescription>
          </DialogHeader>
          <div className="py-4">
            <Select
              value={selected}
              onValueChange={(v) => setSelected(v as AuthorityTier)}
            >
              <SelectTrigger aria-label="새 등급 선택">
                <SelectValue />
              </SelectTrigger>
              <SelectContent>
                <SelectItem value="FM">{TIER_LABELS.FM}</SelectItem>
                <SelectItem value="AM">{TIER_LABELS.AM}</SelectItem>
                <SelectItem value="GT">{TIER_LABELS.GT}</SelectItem>
              </SelectContent>
            </Select>
            {isSame && (
              <p className="mt-2 text-sm text-muted-foreground">동일한 등급입니다.</p>
            )}
          </div>
          <DialogFooter>
            <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>
              취소
            </Button>
            <Button type="submit" disabled={isDisabled}>
              {mutation.isPending ? "변경 중..." : "변경"}
            </Button>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

- [ ] **Step 3: 테스트 PASS**

Run: `pnpm exec vitest run src/features/members/ui/mutation-dialogs/__tests__/change-tier-dialog.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 4: Commit**

```bash
git add src/features/members/ui/mutation-dialogs/change-tier-dialog.tsx src/features/members/ui/mutation-dialogs/__tests__/change-tier-dialog.test.tsx
git commit -m "feat(members): add ChangeTierDialog (FM/AM/GT select + 동일 등급 disable) (G2.6)"
```

### Task G2.7: withdraw-dialog (TDD)

**Files:**
- Create: `src/features/members/ui/mutation-dialogs/withdraw-dialog.tsx`
- Test: `src/features/members/ui/mutation-dialogs/__tests__/withdraw-dialog.test.tsx`

- [ ] **Step 1: 실패 테스트**

```tsx
import { describe, expect, it, vi } from "vitest"
import { render, screen } from "@testing-library/react"
import userEvent from "@testing-library/user-event"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { http, HttpResponse } from "msw"
import { server } from "@/test/mocks/server"
import { toast } from "sonner"
import { WithdrawDialog } from "@/features/members/ui/mutation-dialogs/withdraw-dialog"

function renderWithClient(ui: React.ReactNode) {
  const qc = new QueryClient({ defaultOptions: { mutations: { retry: false } } })
  return render(<QueryClientProvider client={qc}>{ui}</QueryClientProvider>)
}

describe("WithdrawDialog", () => {
  it("displays member display name", () => {
    renderWithClient(
      <WithdrawDialog memberId={1} displayName="alice" open={true} onOpenChange={vi.fn()} />,
    )
    expect(screen.getByText(/alice/)).toBeInTheDocument()
  })

  it("submits and closes dialog on success", async () => {
    server.use(
      http.post("*/api/v1/admin/members/1/withdraw", () =>
        HttpResponse.json({
          data: { memberId: 1, userAccountId: 100, withdrawnAt: "2026-04-29T10:00:00", alreadyWithdrawn: false },
        }),
      ),
    )
    const onOpenChange = vi.fn()
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(
      <WithdrawDialog memberId={1} displayName="alice" open={true} onOpenChange={onOpenChange} />,
    )
    await user.click(screen.getByRole("button", { name: /탈퇴 처리/ }))
    await waitFor(() => expect(onOpenChange).toHaveBeenCalledWith(false))
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

```tsx
// src/features/members/ui/mutation-dialogs/withdraw-dialog.tsx
import { useEffect } from "react"
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { useWithdrawMember } from "@/features/members/api/use-withdraw-member"

interface Props {
  memberId: number
  displayName: string  // 닉네임 또는 이메일
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function WithdrawDialog({ memberId, displayName, open, onOpenChange }: Props) {
  const mutation = useWithdrawMember()

  useEffect(() => {
    if (!open) mutation.reset()
  }, [open])

  function handleSubmit() {
    mutation.mutate({ memberId }, { onSuccess: () => onOpenChange(false) })
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>비식별화 탈퇴 처리</DialogTitle>
          <DialogDescription>
            <strong>{displayName}</strong> 회원을 탈퇴 처리합니다. 이메일 PII가 erase되며 되돌릴 수 없습니다.
          </DialogDescription>
        </DialogHeader>
        <DialogFooter>
          <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>
            취소
          </Button>
          <Button type="button" variant="destructive" onClick={handleSubmit} disabled={mutation.isPending}>
            {mutation.isPending ? "처리 중..." : "탈퇴 처리"}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  )
}
```

- [ ] **Step 3: 테스트 PASS**

Run: `pnpm exec vitest run src/features/members/ui/mutation-dialogs/__tests__/withdraw-dialog.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 4: Commit**

```bash
git add src/features/members/ui/mutation-dialogs/withdraw-dialog.tsx src/features/members/ui/mutation-dialogs/__tests__/withdraw-dialog.test.tsx
git commit -m "feat(members): add WithdrawDialog (confirm + idempotent 분기는 hook이 처리) (G2.7)"
```

### Task G2.8: members-actions-dropdown (TDD)

**Files:**
- Create: `src/features/members/ui/members-actions-dropdown.tsx`
- Test: `src/features/members/ui/__tests__/members-actions-dropdown.test.tsx`

- [ ] **Step 1: 실패 테스트**

```tsx
import { describe, expect, it } from "vitest"
import { render, screen } from "@testing-library/react"
import userEvent from "@testing-library/user-event"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { MembersActionsDropdown } from "@/features/members/ui/members-actions-dropdown"

function renderWithClient(ui: React.ReactNode) {
  const qc = new QueryClient()
  return render(<QueryClientProvider client={qc}>{ui}</QueryClientProvider>)
}

describe("MembersActionsDropdown", () => {
  it("renders Actions trigger", () => {
    renderWithClient(<MembersActionsDropdown memberId={1} currentTier="AM" displayName="alice" />)
    expect(screen.getByRole("button", { name: /actions/i })).toBeInTheDocument()
  })

  it("opens menu and triggers change-tier dialog", async () => {
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<MembersActionsDropdown memberId={1} currentTier="AM" displayName="alice" />)
    await user.click(screen.getByRole("button", { name: /actions/i }))
    await user.click(await screen.findByRole("menuitem", { name: /등급 변경/ }))
    expect(screen.getByText(/회원 등급 변경/)).toBeInTheDocument()
  })

  it("opens menu and triggers withdraw dialog", async () => {
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<MembersActionsDropdown memberId={1} currentTier="AM" displayName="alice" />)
    await user.click(screen.getByRole("button", { name: /actions/i }))
    await user.click(await screen.findByRole("menuitem", { name: /탈퇴/ }))
    expect(screen.getByText(/비식별화 탈퇴 처리/)).toBeInTheDocument()
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

```tsx
// src/features/members/ui/members-actions-dropdown.tsx
import { useState } from "react"
import { ChevronDown } from "lucide-react"
import {
  DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem,
} from "@/components/ui/dropdown-menu"
import { Button } from "@/components/ui/button"
import { ChangeTierDialog } from "./mutation-dialogs/change-tier-dialog"
import { WithdrawDialog } from "./mutation-dialogs/withdraw-dialog"
import type { AuthorityTier } from "@/entities/member/model/types"

type ActiveDialog = null | "change-tier" | "withdraw"

interface Props {
  memberId: number
  currentTier: AuthorityTier
  displayName: string
}

export function MembersActionsDropdown({ memberId, currentTier, displayName }: Props) {
  const [active, setActive] = useState<ActiveDialog>(null)

  return (
    <>
      <DropdownMenu>
        <DropdownMenuTrigger asChild>
          <Button variant="outline" aria-label="Actions">
            Actions <ChevronDown className="ml-2 h-4 w-4" />
          </Button>
        </DropdownMenuTrigger>
        <DropdownMenuContent align="end">
          <DropdownMenuItem onSelect={() => setActive("change-tier")}>
            등급 변경
          </DropdownMenuItem>
          <DropdownMenuItem
            onSelect={() => setActive("withdraw")}
            className="text-destructive focus:text-destructive"
          >
            비식별화 탈퇴
          </DropdownMenuItem>
        </DropdownMenuContent>
      </DropdownMenu>

      <ChangeTierDialog
        memberId={memberId}
        currentTier={currentTier}
        open={active === "change-tier"}
        onOpenChange={(open) => !open && setActive(null)}
      />
      <WithdrawDialog
        memberId={memberId}
        displayName={displayName}
        open={active === "withdraw"}
        onOpenChange={(open) => !open && setActive(null)}
      />
    </>
  )
}
```

- [ ] **Step 3: 테스트 PASS**

Run: `pnpm exec vitest run src/features/members/ui/__tests__/members-actions-dropdown.test.tsx`
Expected: PASS (3 tests).

- [ ] **Step 4: Commit**

```bash
git add src/features/members/ui/members-actions-dropdown.tsx src/features/members/ui/__tests__/members-actions-dropdown.test.tsx
git commit -m "feat(members): add MembersActionsDropdown (Actions ▾ → 2 dialogs) (G2.8)"
```

### Task G2.9: members-detail widget integration

**Files:**
- Modify: `src/widgets/members-detail.tsx`

- [ ] **Step 1: 헤더 영역에 dropdown 부착**

`widgets/members-detail.tsx`의 헤더 컨테이너를 `<div className="flex items-center justify-between">`로 감싸 우측에 `<MembersActionsDropdown>` 배치. 14b 코드의 헤더 부분을 찾아 변경.

Read first: `src/widgets/members-detail.tsx`

검토 후 헤더 jsx 변경 (14b 정확한 구조에 맞춰):
- 좌측: 기존 title + back link
- 우측: `<MembersActionsDropdown memberId={detail.memberId} currentTier={detail.authorityTier} displayName={detail.profile.nickname ?? detail.userAccount.email} />`

`MembersActionsDropdown` import 추가.

- [ ] **Step 2: 14b 회귀 테스트 확인**

Run: `pnpm exec vitest run src/widgets`
Expected: 기존 widget 테스트 PASS (헤더 변경이 깨뜨리지 않음).

- [ ] **Step 3: 추가 integration 테스트 1개**

`src/widgets/__tests__/members-detail.test.tsx` 신규 또는 14b 기존 파일 확장:

```tsx
it("renders MembersActionsDropdown in header", async () => {
  // 14b widget render 흐름 그대로 + dropdown trigger assert
  expect(screen.getByRole("button", { name: /actions/i })).toBeInTheDocument()
})
```

Run: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/widgets/members-detail.tsx src/widgets/__tests__/members-detail.test.tsx
git commit -m "feat(widgets): wire MembersActionsDropdown into members-detail header (G2.9)"
```

### Task G2.10: msw mutation handlers + fixtures

**Files:**
- Modify: `src/test/mocks/handlers/members.ts`
- Modify: `src/test/mocks/fixtures/members.ts`

- [ ] **Step 1: 기본 happy path mutation handlers 추가**

`handlers/members.ts`에 append:

```ts
http.patch(`${API}/:id/tier`, async ({ params }) => {
  return HttpResponse.json({
    data: { memberId: Number(params.id), oldTier: "AM", newTier: "FM" },
  })
}),
http.post(`${API}/:id/withdraw`, async ({ params }) => {
  return HttpResponse.json({
    data: {
      memberId: Number(params.id),
      userAccountId: 100,
      withdrawnAt: "2026-04-29T10:00:00",
      alreadyWithdrawn: false,
    },
  })
}),
```

(per-test override는 hook/dialog 테스트에서 server.use로 처리. 본 handlers는 default happy path.)

- [ ] **Step 2: error fixture 추가 (선택)**

`fixtures/members.ts`에 append (이미 있다면 skip):

```ts
export const tierUnchangedErrorFixture = {
  status: 400,
  errorCode: "TIER_UNCHANGED",
  message: "동일 등급",
}
export const memberNotFoundErrorFixture = {
  status: 404,
  errorCode: "MEMBER_NOT_FOUND",
  message: "회원을 찾을 수 없음",
}
```

- [ ] **Step 3: 전체 테스트 sanity**

Run: `pnpm test:run`
Expected: 14b 77 + G1 3 + G2 신규 모두 PASS.

- [ ] **Step 4: Commit**

```bash
git add src/test/mocks/handlers/members.ts src/test/mocks/fixtures/members.ts
git commit -m "test(mocks): add member mutation handlers + error fixtures (G2.10)"
```

### Task G2.11: chunk sanity

- [ ] **Step 1: typecheck**

Run: `pnpm exec tsc --noEmit`
Expected: 0 error.

- [ ] **Step 2: 전체 테스트**

Run: `pnpm test:run`
Expected: 모두 PASS.

---

## Chunk G3: 파티룸 status lifecycle (terminate + suspend + restore + dropdown skeleton)

**목표**: partyrooms-actions-dropdown 신설 + 3 mutation (lifecycle 응집).

### Task G3.1: zod mutation-schema (terminate/suspend/restore 부분)

**Files:**
- Create: `src/features/partyrooms/model/mutation-schema.ts`
- Test: `src/features/partyrooms/model/__tests__/mutation-schema.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
import { describe, expect, it } from "vitest"
import { TerminateReasonSchema, SuspendReasonSchema } from "@/features/partyrooms/model/mutation-schema"

describe("TerminateReasonSchema", () => {
  it("requires non-blank reason ≤ 500", () => {
    expect(TerminateReasonSchema.parse({ reason: "테스트" })).toEqual({ reason: "테스트" })
    expect(() => TerminateReasonSchema.parse({ reason: "" })).toThrow()
    expect(() => TerminateReasonSchema.parse({ reason: "x".repeat(501) })).toThrow()
  })
})

describe("SuspendReasonSchema", () => {
  it("matches Terminate shape", () => {
    expect(SuspendReasonSchema.parse({ reason: "x" })).toEqual({ reason: "x" })
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

```ts
// src/features/partyrooms/model/mutation-schema.ts
import { z } from "zod"

export const TerminateReasonSchema = z.object({
  reason: z.string().min(1, "사유를 입력해주세요").max(500),
})
export type TerminateRequest = z.infer<typeof TerminateReasonSchema>

export const SuspendReasonSchema = TerminateReasonSchema
export type SuspendRequest = z.infer<typeof SuspendReasonSchema>
```

- [ ] **Step 3: PASS**

Run: `pnpm exec vitest run src/features/partyrooms/model/__tests__/mutation-schema.test.ts`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/features/partyrooms/model/mutation-schema.ts src/features/partyrooms/model/__tests__/mutation-schema.test.ts
git commit -m "feat(partyrooms): add Terminate/Suspend reason zod schemas (G3.1)"
```

### Task G3.2: partyrooms-api.ts terminate/suspend/restore 추가

**Files:**
- Modify: `src/features/partyrooms/api/partyrooms-api.ts`
- Modify: `src/features/partyrooms/api/__tests__/partyrooms-api.test.ts`

- [ ] **Step 1: 실패 테스트 추가**

```ts
describe("terminatePartyroom", () => {
  it("POST /admin/partyrooms/:id/terminate, body, 204", async () => {
    let bodySeen: any
    server.use(
      http.post("*/api/v1/admin/partyrooms/1/terminate", async ({ request }) => {
        bodySeen = await request.json()
        return new HttpResponse(null, { status: 204 })
      }),
    )
    await terminatePartyroom(1, { reason: "abuse" })
    expect(bodySeen).toEqual({ reason: "abuse" })
  })

  it("propagates ApiError on 403 ALREADY_TERMINATED", async () => {
    server.use(
      http.post("*/api/v1/admin/partyrooms/1/terminate", () =>
        HttpResponse.json({ status: 403, errorCode: "ALREADY_TERMINATED", message: "이미 종료" }, { status: 403 }),
      ),
    )
    await expect(terminatePartyroom(1, { reason: "x" })).rejects.toMatchObject({
      status: 403, errorCode: "ALREADY_TERMINATED",
    })
  })
})

describe("suspendPartyroom / restorePartyroom", () => {
  it("suspend 204", async () => {
    server.use(http.post("*/api/v1/admin/partyrooms/1/suspend", () => new HttpResponse(null, { status: 204 })))
    await expect(suspendPartyroom(1, { reason: "x" })).resolves.toBeUndefined()
  })
  it("restore 204 no body", async () => {
    let bodySeen = "presence-marker"
    server.use(
      http.post("*/api/v1/admin/partyrooms/1/restore", async ({ request }) => {
        const text = await request.text()
        bodySeen = text
        return new HttpResponse(null, { status: 204 })
      }),
    )
    await restorePartyroom(1)
    expect(bodySeen).toBe("")  // body 없음
  })

  it("suspend propagates 409 ILLEGAL_STATE_TRANSITION", async () => {
    server.use(
      http.post("*/api/v1/admin/partyrooms/1/suspend", () =>
        HttpResponse.json({ status: 409, errorCode: "ILLEGAL_STATE_TRANSITION", message: "전이 불가" }, { status: 409 }),
      ),
    )
    await expect(suspendPartyroom(1, { reason: "x" })).rejects.toMatchObject({
      status: 409, errorCode: "ILLEGAL_STATE_TRANSITION",
    })
  })
})
```

(import `terminatePartyroom`, `suspendPartyroom`, `restorePartyroom` 추가.)

Run: FAIL.

- [ ] **Step 2: 구현**

`partyrooms-api.ts` append:

```ts
import type { TerminateRequest, SuspendRequest } from "../model/mutation-schema"

export async function terminatePartyroom(partyroomId: number, body: TerminateRequest): Promise<void> {
  await http<void>(`/api/v1/admin/partyrooms/${partyroomId}/terminate`, {
    method: "POST", body,
  })
}

export async function suspendPartyroom(partyroomId: number, body: SuspendRequest): Promise<void> {
  await http<void>(`/api/v1/admin/partyrooms/${partyroomId}/suspend`, {
    method: "POST", body,
  })
}

export async function restorePartyroom(partyroomId: number): Promise<void> {
  await http<void>(`/api/v1/admin/partyrooms/${partyroomId}/restore`, {
    method: "POST",
  })
}
```

- [ ] **Step 3: PASS**

Run: `pnpm exec vitest run src/features/partyrooms/api/__tests__/partyrooms-api.test.ts`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/features/partyrooms/api/partyrooms-api.ts src/features/partyrooms/api/__tests__/partyrooms-api.test.ts
git commit -m "feat(partyrooms): add terminate/suspend/restore API fns (G3.2)"
```

### Task G3.3: 3 mutation hooks (TDD, 동일 패턴)

**Files (3 신규 + 3 테스트)**:
- `use-terminate-partyroom.ts` + test
- `use-suspend-partyroom.ts` + test
- `use-restore-partyroom.ts` + test

- [ ] **Step 1: 실패 테스트 (3 파일, 패턴 동일)**

각 파일에 동일 골격으로 작성:

```ts
// src/features/partyrooms/api/__tests__/use-terminate-partyroom.test.ts
import { describe, expect, it, vi } from "vitest"
import { renderHook, waitFor } from "@testing-library/react"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { http, HttpResponse } from "msw"
import { server } from "@/test/mocks/server"
import { toast } from "sonner"
import { useTerminatePartyroom } from "@/features/partyrooms/api/use-terminate-partyroom"

function makeWrapper() {
  const qc = new QueryClient({ defaultOptions: { mutations: { retry: false }, queries: { retry: false } } })
  const wrapper = ({ children }: { children: React.ReactNode }) => (
    <QueryClientProvider client={qc}>{children}</QueryClientProvider>
  )
  return { qc, wrapper }
}

describe("useTerminatePartyroom", () => {
  it("on success: invalidate ['partyrooms'] prefix, toast.success", async () => {
    const { qc, wrapper } = makeWrapper()
    const invalidateSpy = vi.spyOn(qc, "invalidateQueries")
    const successSpy = vi.spyOn(toast, "success").mockImplementation(() => "")
    server.use(http.post("*/api/v1/admin/partyrooms/1/terminate", () => new HttpResponse(null, { status: 204 })))

    const { result } = renderHook(() => useTerminatePartyroom(), { wrapper })
    result.current.mutate({ partyroomId: 1, reason: "abuse" })
    await waitFor(() => expect(result.current.isSuccess).toBe(true))

    expect(invalidateSpy).toHaveBeenCalledWith({ queryKey: ["partyrooms"] })
    expect(successSpy).toHaveBeenCalled()
  })

  it("on 403 ALREADY_TERMINATED: mutationErrorToast", async () => {
    const { wrapper } = makeWrapper()
    const errorSpy = vi.spyOn(toast, "error").mockImplementation(() => "")
    server.use(
      http.post("*/api/v1/admin/partyrooms/1/terminate", () =>
        HttpResponse.json({ status: 403, errorCode: "ALREADY_TERMINATED", message: "이미 종료됨" }, { status: 403 }),
      ),
    )

    const { result } = renderHook(() => useTerminatePartyroom(), { wrapper })
    result.current.mutate({ partyroomId: 1, reason: "x" })
    await waitFor(() => expect(result.current.isError).toBe(true))
    expect(errorSpy).toHaveBeenCalledWith("이미 종료됨", expect.objectContaining({ description: expect.stringContaining("ALREADY_TERMINATED") }))
  })
})
```

`use-suspend-partyroom.test.ts`, `use-restore-partyroom.test.ts`도 동일 골격으로 작성. suspend는 reason vars, restore는 reason 없음.

Run: FAIL (3 hooks 미존재).

- [ ] **Step 2: 3 hook 구현**

```ts
// use-terminate-partyroom.ts
import { useMutation, useQueryClient } from "@tanstack/react-query"
import { terminatePartyroom } from "./partyrooms-api"
import { mutationSuccessToast, mutationErrorToast } from "@/shared/lib/mutation-toast"

export function useTerminatePartyroom() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (vars: { partyroomId: number; reason: string }) =>
      terminatePartyroom(vars.partyroomId, { reason: vars.reason }),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["partyrooms"] })  // prefix — 14b list+detail 모두 stale
      mutationSuccessToast("강제 종료 완료")
    },
    onError: mutationErrorToast,
  })
}
```

```ts
// use-suspend-partyroom.ts
import { useMutation, useQueryClient } from "@tanstack/react-query"
import { suspendPartyroom } from "./partyrooms-api"
import { mutationSuccessToast, mutationErrorToast } from "@/shared/lib/mutation-toast"

export function useSuspendPartyroom() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (vars: { partyroomId: number; reason: string }) =>
      suspendPartyroom(vars.partyroomId, { reason: vars.reason }),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["partyrooms"] })
      mutationSuccessToast("일시 정지 완료")
    },
    onError: mutationErrorToast,
  })
}
```

```ts
// use-restore-partyroom.ts
import { useMutation, useQueryClient } from "@tanstack/react-query"
import { restorePartyroom } from "./partyrooms-api"
import { mutationSuccessToast, mutationErrorToast } from "@/shared/lib/mutation-toast"

export function useRestorePartyroom() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (vars: { partyroomId: number }) =>
      restorePartyroom(vars.partyroomId),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["partyrooms"] })
      mutationSuccessToast("재개 완료")
    },
    onError: mutationErrorToast,
  })
}
```

- [ ] **Step 3: PASS**

Run: `pnpm exec vitest run src/features/partyrooms/api/__tests__/use-terminate-partyroom.test.ts src/features/partyrooms/api/__tests__/use-suspend-partyroom.test.ts src/features/partyrooms/api/__tests__/use-restore-partyroom.test.ts`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/features/partyrooms/api/use-{terminate,suspend,restore}-partyroom.ts src/features/partyrooms/api/__tests__/use-{terminate,suspend,restore}-partyroom.test.ts
git commit -m "feat(partyrooms): add terminate/suspend/restore mutation hooks (G3.3)"
```

### Task G3.4: 3 lifecycle dialogs (terminate, suspend = form / restore = confirm)

**Files (6 — 3 .tsx + 3 test)**:
- `mutation-dialogs/terminate-dialog.tsx` + test
- `mutation-dialogs/suspend-dialog.tsx` + test
- `mutation-dialogs/restore-dialog.tsx` + test

- [ ] **Step 1: terminate-dialog 테스트 + 구현 (TDD)**

테스트:

```tsx
import { describe, expect, it, vi } from "vitest"
import { render, screen } from "@testing-library/react"
import userEvent from "@testing-library/user-event"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { http, HttpResponse } from "msw"
import { server } from "@/test/mocks/server"
import { TerminateDialog } from "@/features/partyrooms/ui/mutation-dialogs/terminate-dialog"

function renderWithClient(ui: React.ReactNode) {
  const qc = new QueryClient({ defaultOptions: { mutations: { retry: false } } })
  return render(<QueryClientProvider client={qc}>{ui}</QueryClientProvider>)
}

describe("TerminateDialog", () => {
  it("requires reason — empty submit blocked", async () => {
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<TerminateDialog partyroomId={1} open={true} onOpenChange={vi.fn()} />)
    const submit = screen.getByRole("button", { name: /강제 종료/ })
    await user.click(submit)
    // RHF + zod resolver는 required 위반 시 onSubmit 호출 안 함 → API 호출 0
    // assert: dialog 여전히 열려 있음
    expect(screen.getByRole("textbox", { name: /사유/ })).toBeInTheDocument()
  })

  it("submits and closes on success", async () => {
    server.use(http.post("*/api/v1/admin/partyrooms/1/terminate", () => new HttpResponse(null, { status: 204 })))
    const onOpenChange = vi.fn()
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<TerminateDialog partyroomId={1} open={true} onOpenChange={onOpenChange} />)
    await user.type(screen.getByRole("textbox", { name: /사유/ }), "스팸 룸")
    await user.click(screen.getByRole("button", { name: /강제 종료/ }))
    await waitFor(() => expect(onOpenChange).toHaveBeenCalledWith(false))
  })
})
```

구현:

```tsx
// src/features/partyrooms/ui/mutation-dialogs/terminate-dialog.tsx
import { useEffect } from "react"
import { useForm } from "react-hook-form"
import { zodResolver } from "@hookform/resolvers/zod"
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { Textarea } from "@/components/ui/textarea"
import { Label } from "@/components/ui/label"
import { TerminateReasonSchema, type TerminateRequest } from "@/features/partyrooms/model/mutation-schema"
import { useTerminatePartyroom } from "@/features/partyrooms/api/use-terminate-partyroom"

interface Props {
  partyroomId: number
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function TerminateDialog({ partyroomId, open, onOpenChange }: Props) {
  const mutation = useTerminatePartyroom()
  const { register, handleSubmit, formState: { errors }, reset } = useForm<TerminateRequest>({
    resolver: zodResolver(TerminateReasonSchema),
    defaultValues: { reason: "" },
  })

  useEffect(() => {
    if (!open) { mutation.reset(); reset() }
  }, [open])

  const onSubmit = (data: TerminateRequest) =>
    mutation.mutate({ partyroomId, reason: data.reason }, { onSuccess: () => onOpenChange(false) })

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <form onSubmit={handleSubmit(onSubmit)}>
          <DialogHeader>
            <DialogTitle>파티룸 강제 종료</DialogTitle>
            <DialogDescription>
              종료된 룸은 14b 기본 필터에서 자동 제외됩니다. 사유는 audit log에 기록됩니다.
            </DialogDescription>
          </DialogHeader>
          <div className="py-4 space-y-2">
            <Label htmlFor="terminate-reason">사유 <span className="text-destructive">*</span></Label>
            <Textarea
              id="terminate-reason"
              aria-label="사유"
              {...register("reason")}
              maxLength={500}
              rows={4}
            />
            {errors.reason && <p className="text-sm text-destructive">{errors.reason.message}</p>}
          </div>
          <DialogFooter>
            <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>취소</Button>
            <Button type="submit" variant="destructive" disabled={mutation.isPending}>
              {mutation.isPending ? "처리 중..." : "강제 종료"}
            </Button>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

Run: PASS.

- [ ] **Step 2: suspend-dialog (terminate와 동일 골격, label 차이)**

구현 — terminate와 동일 form 구조 + zod resolver. 다른 점: variant="default", title "파티룸 일시 정지", description, useSuspendPartyroom 사용.

```tsx
// src/features/partyrooms/ui/mutation-dialogs/suspend-dialog.tsx
import { useEffect } from "react"
import { useForm } from "react-hook-form"
import { zodResolver } from "@hookform/resolvers/zod"
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { Textarea } from "@/components/ui/textarea"
import { Label } from "@/components/ui/label"
import { SuspendReasonSchema, type SuspendRequest } from "@/features/partyrooms/model/mutation-schema"
import { useSuspendPartyroom } from "@/features/partyrooms/api/use-suspend-partyroom"

interface Props {
  partyroomId: number
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function SuspendDialog({ partyroomId, open, onOpenChange }: Props) {
  const mutation = useSuspendPartyroom()
  const { register, handleSubmit, formState: { errors }, reset } = useForm<SuspendRequest>({
    resolver: zodResolver(SuspendReasonSchema),
    defaultValues: { reason: "" },
  })

  useEffect(() => { if (!open) { mutation.reset(); reset() } }, [open])

  const onSubmit = (data: SuspendRequest) =>
    mutation.mutate({ partyroomId, reason: data.reason }, { onSuccess: () => onOpenChange(false) })

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <form onSubmit={handleSubmit(onSubmit)}>
          <DialogHeader>
            <DialogTitle>파티룸 일시 정지</DialogTitle>
            <DialogDescription>운영자 검토 중 일시 정지합니다. 사유는 audit log에 기록됩니다.</DialogDescription>
          </DialogHeader>
          <div className="py-4 space-y-2">
            <Label htmlFor="suspend-reason">사유 <span className="text-destructive">*</span></Label>
            <Textarea id="suspend-reason" aria-label="사유" {...register("reason")} maxLength={500} rows={4} />
            {errors.reason && <p className="text-sm text-destructive">{errors.reason.message}</p>}
          </div>
          <DialogFooter>
            <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>취소</Button>
            <Button type="submit" disabled={mutation.isPending}>
              {mutation.isPending ? "처리 중..." : "일시 정지"}
            </Button>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

테스트는 terminate-dialog 테스트와 동일한 2 케이스 (empty reason 차단 + 정상 submit). path 차이만:
- `*/api/v1/admin/partyrooms/1/suspend` 사용
- button text는 `/일시 정지/`
- title은 `/파티룸 일시 정지/`

- [ ] **Step 3: restore-dialog (confirm 모달, 폼 없음)**

```tsx
import { useEffect } from "react"
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { useRestorePartyroom } from "@/features/partyrooms/api/use-restore-partyroom"

interface Props {
  partyroomId: number
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function RestoreDialog({ partyroomId, open, onOpenChange }: Props) {
  const mutation = useRestorePartyroom()

  useEffect(() => { if (!open) mutation.reset() }, [open])

  const handleSubmit = () =>
    mutation.mutate({ partyroomId }, { onSuccess: () => onOpenChange(false) })

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>파티룸 재개</DialogTitle>
          <DialogDescription>일시 정지된 파티룸을 정상 운영으로 복귀합니다.</DialogDescription>
        </DialogHeader>
        <DialogFooter>
          <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>취소</Button>
          <Button type="button" onClick={handleSubmit} disabled={mutation.isPending}>
            {mutation.isPending ? "처리 중..." : "재개"}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  )
}
```

테스트는 단순 (open 시 trigger 보임 / submit 시 onOpenChange(false)).

- [ ] **Step 4: 3 dialog 테스트 모두 PASS**

Run: `pnpm exec vitest run src/features/partyrooms/ui/mutation-dialogs/__tests__`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/features/partyrooms/ui/mutation-dialogs/{terminate,suspend,restore}-dialog.tsx src/features/partyrooms/ui/mutation-dialogs/__tests__/{terminate,suspend,restore}-dialog.test.tsx
git commit -m "feat(partyrooms): add Terminate/Suspend/Restore dialogs (G3.4)"
```

### Task G3.5: partyrooms-actions-dropdown (status-aware disabled, lifecycle 3개만)

**Files:**
- Create: `src/features/partyrooms/ui/partyrooms-actions-dropdown.tsx`
- Test: `src/features/partyrooms/ui/__tests__/partyrooms-actions-dropdown.test.tsx`

**중요**: 이 dropdown은 G3에서는 lifecycle 3 항목만 노출. updateMeta(G4)와 displayFlag(G5)는 후속 chunk에서 추가.

- [ ] **Step 1: 실패 테스트 (5 케이스 — disable 매트릭스)**

```tsx
import { describe, expect, it } from "vitest"
import { render, screen } from "@testing-library/react"
import userEvent from "@testing-library/user-event"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { PartyroomsActionsDropdown } from "@/features/partyrooms/ui/partyrooms-actions-dropdown"
import type { AdminPartyroomDetail } from "@/entities/partyroom/model/types"

const baseDetail: AdminPartyroomDetail = {
  partyroomId: 1, title: "테스트", status: "ACTIVE", displayFlag: "NORMAL",
  hostUserAccountId: 100, hostNickname: "alice", hostEmail: "alice@example.com",
  crewCount: 5, lastActivityAt: null, stageType: "GENERAL",
  playback: { activated: false, currentTrackName: null, currentDjCrewId: null },
  crews: [], djQueue: [], recentPenalties: [], recentReports: [], recentAdminActions: [],
}

function renderWithClient(ui: React.ReactNode) {
  const qc = new QueryClient()
  return render(<QueryClientProvider client={qc}>{ui}</QueryClientProvider>)
}

describe("PartyroomsActionsDropdown — status-aware disabled", () => {
  it("ACTIVE: terminate enabled, suspend enabled, restore disabled", async () => {
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<PartyroomsActionsDropdown partyroom={{ ...baseDetail, status: "ACTIVE" }} />)
    await user.click(screen.getByRole("button", { name: /actions/i }))

    expect(await screen.findByRole("menuitem", { name: /강제 종료/ })).not.toHaveAttribute("data-disabled")
    expect(screen.getByRole("menuitem", { name: /일시 정지/ })).not.toHaveAttribute("data-disabled")
    expect(screen.getByRole("menuitem", { name: /재개/ })).toHaveAttribute("data-disabled")
  })

  it("SUSPENDED: terminate enabled, suspend disabled, restore enabled", async () => {
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<PartyroomsActionsDropdown partyroom={{ ...baseDetail, status: "SUSPENDED" }} />)
    await user.click(screen.getByRole("button", { name: /actions/i }))

    expect(await screen.findByRole("menuitem", { name: /강제 종료/ })).not.toHaveAttribute("data-disabled")
    expect(screen.getByRole("menuitem", { name: /일시 정지/ })).toHaveAttribute("data-disabled")
    expect(screen.getByRole("menuitem", { name: /재개/ })).not.toHaveAttribute("data-disabled")
  })

  it("TERMINATED: all 3 lifecycle items disabled", async () => {
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<PartyroomsActionsDropdown partyroom={{ ...baseDetail, status: "TERMINATED" }} />)
    await user.click(screen.getByRole("button", { name: /actions/i }))

    expect(await screen.findByRole("menuitem", { name: /강제 종료/ })).toHaveAttribute("data-disabled")
    expect(screen.getByRole("menuitem", { name: /일시 정지/ })).toHaveAttribute("data-disabled")
    expect(screen.getByRole("menuitem", { name: /재개/ })).toHaveAttribute("data-disabled")
  })

  it("clicking enabled terminate opens terminate dialog", async () => {
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<PartyroomsActionsDropdown partyroom={{ ...baseDetail, status: "ACTIVE" }} />)
    await user.click(screen.getByRole("button", { name: /actions/i }))
    await user.click(await screen.findByRole("menuitem", { name: /강제 종료/ }))
    expect(screen.getByText(/파티룸 강제 종료/)).toBeInTheDocument()
  })

  it("clicking enabled restore on SUSPENDED opens restore dialog", async () => {
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<PartyroomsActionsDropdown partyroom={{ ...baseDetail, status: "SUSPENDED" }} />)
    await user.click(screen.getByRole("button", { name: /actions/i }))
    await user.click(await screen.findByRole("menuitem", { name: /재개/ }))
    expect(screen.getByText(/파티룸 재개/)).toBeInTheDocument()
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현 (lifecycle 3 항목만, G4/G5에서 updateMeta/displayFlag 추가될 자리는 코멘트로)**

```tsx
// src/features/partyrooms/ui/partyrooms-actions-dropdown.tsx
import { useState } from "react"
import { ChevronDown } from "lucide-react"
import {
  DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem,
} from "@/components/ui/dropdown-menu"
import { Button } from "@/components/ui/button"
import { TerminateDialog } from "./mutation-dialogs/terminate-dialog"
import { SuspendDialog } from "./mutation-dialogs/suspend-dialog"
import { RestoreDialog } from "./mutation-dialogs/restore-dialog"
import type { AdminPartyroomDetail } from "@/entities/partyroom/model/types"

type ActiveDialog = null | "terminate" | "suspend" | "restore" | "update-meta" | "display-flag"
// G4/G5에서 update-meta, display-flag 항목 추가

interface Props {
  partyroom: AdminPartyroomDetail
}

export function PartyroomsActionsDropdown({ partyroom }: Props) {
  const [active, setActive] = useState<ActiveDialog>(null)
  const status = partyroom.status

  const canTerminate = status === "ACTIVE" || status === "SUSPENDED"
  const canSuspend = status === "ACTIVE"
  const canRestore = status === "SUSPENDED"

  return (
    <>
      <DropdownMenu>
        <DropdownMenuTrigger asChild>
          <Button variant="outline" aria-label="Actions">
            Actions <ChevronDown className="ml-2 h-4 w-4" />
          </Button>
        </DropdownMenuTrigger>
        <DropdownMenuContent align="end">
          <DropdownMenuItem
            disabled={!canSuspend}
            onSelect={() => canSuspend && setActive("suspend")}
          >
            일시 정지
          </DropdownMenuItem>
          <DropdownMenuItem
            disabled={!canRestore}
            onSelect={() => canRestore && setActive("restore")}
          >
            재개
          </DropdownMenuItem>
          <DropdownMenuItem
            disabled={!canTerminate}
            onSelect={() => canTerminate && setActive("terminate")}
            className="text-destructive focus:text-destructive"
          >
            강제 종료
          </DropdownMenuItem>
          {/* G4: 메타 수정, G5: 표시 변경 — 후속 chunk에서 추가 */}
        </DropdownMenuContent>
      </DropdownMenu>

      <TerminateDialog
        partyroomId={partyroom.partyroomId}
        open={active === "terminate"}
        onOpenChange={(o) => !o && setActive(null)}
      />
      <SuspendDialog
        partyroomId={partyroom.partyroomId}
        open={active === "suspend"}
        onOpenChange={(o) => !o && setActive(null)}
      />
      <RestoreDialog
        partyroomId={partyroom.partyroomId}
        open={active === "restore"}
        onOpenChange={(o) => !o && setActive(null)}
      />
    </>
  )
}
```

- [ ] **Step 3: PASS**

Run: `pnpm exec vitest run src/features/partyrooms/ui/__tests__/partyrooms-actions-dropdown.test.tsx`
Expected: PASS (5 tests).

- [ ] **Step 4: Commit**

```bash
git add src/features/partyrooms/ui/partyrooms-actions-dropdown.tsx src/features/partyrooms/ui/__tests__/partyrooms-actions-dropdown.test.tsx
git commit -m "feat(partyrooms): add ActionsDropdown skeleton with status-aware disabled (lifecycle 3) (G3.5)"
```

### Task G3.6: partyrooms-detail widget integration

**Files:**
- Modify: `src/widgets/partyrooms-detail.tsx`

- [ ] **Step 1: 헤더 영역에 dropdown 부착**

`partyrooms-detail.tsx` 헤더에 우측 정렬로 `<PartyroomsActionsDropdown partyroom={detail} />` 추가. 14b 기존 코드 read 후 jsx 반영.

- [ ] **Step 2: 14b 회귀 테스트**

Run: `pnpm exec vitest run src/widgets`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add src/widgets/partyrooms-detail.tsx
git commit -m "feat(widgets): wire PartyroomsActionsDropdown into partyrooms-detail header (G3.6)"
```

### Task G3.7: msw lifecycle handlers

**Files:**
- Modify: `src/test/mocks/handlers/partyrooms.ts`

- [ ] **Step 1: 3 mutation handlers 추가**

```ts
http.post(`${API}/:id/terminate`, () => new HttpResponse(null, { status: 204 })),
http.post(`${API}/:id/suspend`, () => new HttpResponse(null, { status: 204 })),
http.post(`${API}/:id/restore`, () => new HttpResponse(null, { status: 204 })),
```

- [ ] **Step 2: error fixtures (선택)**

`fixtures/partyrooms.ts`에 append:

```ts
export const alreadyTerminatedErrorFixture = {
  status: 403, errorCode: "ALREADY_TERMINATED", message: "이미 종료됨",
}
export const illegalStateTransitionErrorFixture = {
  status: 409, errorCode: "ILLEGAL_STATE_TRANSITION", message: "현재 상태에서 불가",
}
```

- [ ] **Step 3: 전체 sanity**

Run: `pnpm test:run`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/test/mocks/handlers/partyrooms.ts src/test/mocks/fixtures/partyrooms.ts
git commit -m "test(mocks): add partyroom lifecycle mutation handlers + error fixtures (G3.7)"
```

### Task G3.8: chunk sanity

- [ ] **Step 1: typecheck + 전체 테스트**

Run: `pnpm exec tsc --noEmit && pnpm test:run`
Expected: PASS.

---

## Chunk G4: 파티룸 메타 수정 (updateMeta)

**목표**: dropdown에 "메타 수정" 항목 추가 + form 모달 (title/intro/playbackTimeLimit refine).

### Task G4.1: backend validator sanity grep (R3 잔존)

- [ ] **Step 1: backend validator 변동 확인**

Read `pfplay-platform/app/src/main/java/com/pfplaybackend/api/administration/adapter/in/web/payload/request/UpdatePartyroomMetaRequest.java`

Expected (spec §2.2 ground-truth):
- `title @Size(max=100)`
- `introduction @Size(max=500)`
- `playbackTimeLimit @Min(1) @Max(60)`
- `@AssertTrue isAtLeastOnePresent`

변경됐다면 mutation-schema 보정 + spec §12에 reality 박음.

### Task G4.2: zod UpdatePartyroomMetaSchema (TDD)

**Files:**
- Modify: `src/features/partyrooms/model/mutation-schema.ts`
- Modify: `src/features/partyrooms/model/__tests__/mutation-schema.test.ts`

- [ ] **Step 1: 실패 테스트 추가**

```ts
import { UpdatePartyroomMetaSchema } from "@/features/partyrooms/model/mutation-schema"

describe("UpdatePartyroomMetaSchema", () => {
  it("accepts partial: title only", () => {
    expect(UpdatePartyroomMetaSchema.parse({ title: "신규 제목" })).toEqual({ title: "신규 제목" })
  })
  it("accepts all 3 fields", () => {
    expect(UpdatePartyroomMetaSchema.parse({ title: "t", introduction: "i", playbackTimeLimit: 10 }))
      .toEqual({ title: "t", introduction: "i", playbackTimeLimit: 10 })
  })
  it("rejects empty object (refine)", () => {
    expect(() => UpdatePartyroomMetaSchema.parse({})).toThrow(/최소 1개/)
  })
  it("rejects title > 100", () => {
    expect(() => UpdatePartyroomMetaSchema.parse({ title: "x".repeat(101) })).toThrow()
  })
  it("rejects playbackTimeLimit out of [1,60]", () => {
    expect(() => UpdatePartyroomMetaSchema.parse({ playbackTimeLimit: 0 })).toThrow()
    expect(() => UpdatePartyroomMetaSchema.parse({ playbackTimeLimit: 61 })).toThrow()
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

`mutation-schema.ts` append:

```ts
export const UpdatePartyroomMetaSchema = z.object({
  title: z.string().min(1).max(100).optional(),
  introduction: z.string().max(500).optional(),
  playbackTimeLimit: z.coerce.number().int().min(1).max(60).optional(),
}).refine(
  (v) => v.title !== undefined || v.introduction !== undefined || v.playbackTimeLimit !== undefined,
  { message: "최소 1개 필드는 변경 필요" },
)
export type UpdatePartyroomMetaRequest = z.infer<typeof UpdatePartyroomMetaSchema>
```

- [ ] **Step 3: PASS**

Run: `pnpm exec vitest run src/features/partyrooms/model/__tests__/mutation-schema.test.ts`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/features/partyrooms/model/mutation-schema.ts src/features/partyrooms/model/__tests__/mutation-schema.test.ts
git commit -m "feat(partyrooms): add UpdatePartyroomMetaSchema (title/intro/limit refine) (G4.2)"
```

### Task G4.3: partyrooms-api.updatePartyroomMeta + 테스트

**Files:**
- Modify: `src/features/partyrooms/api/partyrooms-api.ts`
- Modify: `src/features/partyrooms/api/__tests__/partyrooms-api.test.ts`

- [ ] **Step 1: 테스트 추가**

```ts
describe("updatePartyroomMeta", () => {
  it("PATCH /admin/partyrooms/:id with body, 204", async () => {
    let bodySeen: any
    server.use(
      http.patch("*/api/v1/admin/partyrooms/1", async ({ request }) => {
        bodySeen = await request.json()
        return new HttpResponse(null, { status: 204 })
      }),
    )
    await updatePartyroomMeta(1, { title: "new", playbackTimeLimit: 30 })
    expect(bodySeen).toEqual({ title: "new", playbackTimeLimit: 30 })
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

```ts
import type { UpdatePartyroomMetaRequest } from "../model/mutation-schema"

export async function updatePartyroomMeta(
  partyroomId: number, body: UpdatePartyroomMetaRequest,
): Promise<void> {
  await http<void>(`/api/v1/admin/partyrooms/${partyroomId}`, { method: "PATCH", body })
}
```

- [ ] **Step 3: PASS + Commit**

```bash
git commit -am "feat(partyrooms): add updatePartyroomMeta API fn (G4.3)"
```

### Task G4.4: useUpdatePartyroomMeta hook (TDD)

**Files:**
- Create: `src/features/partyrooms/api/use-update-partyroom-meta.ts`
- Test: `src/features/partyrooms/api/__tests__/use-update-partyroom-meta.test.ts`

- [ ] **Step 1: 테스트 (G3.3 패턴 동일)**

```ts
// src/features/partyrooms/api/__tests__/use-update-partyroom-meta.test.ts
import { describe, expect, it, vi } from "vitest"
import { renderHook, waitFor } from "@testing-library/react"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { http, HttpResponse } from "msw"
import { server } from "@/test/mocks/server"
import { toast } from "sonner"
import { useUpdatePartyroomMeta } from "@/features/partyrooms/api/use-update-partyroom-meta"

function makeWrapper() {
  const qc = new QueryClient({ defaultOptions: { mutations: { retry: false }, queries: { retry: false } } })
  const wrapper = ({ children }: { children: React.ReactNode }) => (
    <QueryClientProvider client={qc}>{children}</QueryClientProvider>
  )
  return { qc, wrapper }
}

describe("useUpdatePartyroomMeta", () => {
  it("on success: invalidate ['partyrooms'] + toast.success", async () => {
    const { qc, wrapper } = makeWrapper()
    const invalidateSpy = vi.spyOn(qc, "invalidateQueries")
    const successSpy = vi.spyOn(toast, "success").mockImplementation(() => "")
    server.use(http.patch("*/api/v1/admin/partyrooms/1", () => new HttpResponse(null, { status: 204 })))

    const { result } = renderHook(() => useUpdatePartyroomMeta(), { wrapper })
    result.current.mutate({ partyroomId: 1, body: { title: "new" } })
    await waitFor(() => expect(result.current.isSuccess).toBe(true))

    expect(invalidateSpy).toHaveBeenCalledWith({ queryKey: ["partyrooms"] })
    expect(successSpy).toHaveBeenCalledWith("메타 수정 완료")
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

```ts
// src/features/partyrooms/api/use-update-partyroom-meta.ts
import { useMutation, useQueryClient } from "@tanstack/react-query"
import { updatePartyroomMeta } from "./partyrooms-api"
import { mutationSuccessToast, mutationErrorToast } from "@/shared/lib/mutation-toast"
import type { UpdatePartyroomMetaRequest } from "../model/mutation-schema"

export function useUpdatePartyroomMeta() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (vars: { partyroomId: number; body: UpdatePartyroomMetaRequest }) =>
      updatePartyroomMeta(vars.partyroomId, vars.body),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["partyrooms"] })
      mutationSuccessToast("메타 수정 완료")
    },
    onError: mutationErrorToast,
  })
}
```

- [ ] **Step 3: PASS + Commit**

```bash
git add src/features/partyrooms/api/use-update-partyroom-meta.ts src/features/partyrooms/api/__tests__/use-update-partyroom-meta.test.ts
git commit -m "feat(partyrooms): add useUpdatePartyroomMeta hook (G4.4)"
```

### Task G4.5: update-meta-dialog (TDD)

**Files:**
- Create: `src/features/partyrooms/ui/mutation-dialogs/update-meta-dialog.tsx`
- Test: 동일 디렉토리 `__tests__/update-meta-dialog.test.tsx`

- [ ] **Step 1: 실패 테스트 (3 케이스)**

```tsx
describe("UpdateMetaDialog", () => {
  it("submits partial fields successfully", async () => {
    server.use(http.patch("*/api/v1/admin/partyrooms/1", () => new HttpResponse(null, { status: 204 })))
    const onOpenChange = vi.fn()
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(
      <UpdateMetaDialog partyroomId={1} currentTitle="기존" currentIntroduction={null} currentPlaybackTimeLimit={20} open={true} onOpenChange={onOpenChange} />,
    )
    await user.clear(screen.getByRole("textbox", { name: /제목/ }))
    await user.type(screen.getByRole("textbox", { name: /제목/ }), "신규")
    await user.click(screen.getByRole("button", { name: /수정/ }))
    await waitFor(() => expect(onOpenChange).toHaveBeenCalledWith(false))
  })

  it("blocks submit when no field changed (refine error)", async () => {
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(
      <UpdateMetaDialog partyroomId={1} currentTitle="x" currentIntroduction="y" currentPlaybackTimeLimit={20} open={true} onOpenChange={vi.fn()} />,
    )
    // form은 빈 값으로 시작 (현재 값은 placeholder) → 모든 필드 undefined → refine FAIL
    await user.click(screen.getByRole("button", { name: /수정/ }))
    expect(await screen.findByText(/최소 1개/)).toBeInTheDocument()
  })

  it("rejects title > 100 char (zod max)", async () => {
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<UpdateMetaDialog partyroomId={1} currentTitle="" currentIntroduction={null} currentPlaybackTimeLimit={20} open={true} onOpenChange={vi.fn()} />)
    await user.type(screen.getByRole("textbox", { name: /제목/ }), "x".repeat(101))
    await user.click(screen.getByRole("button", { name: /수정/ }))
    expect(await screen.findByText(/100/)).toBeInTheDocument()
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

```tsx
// src/features/partyrooms/ui/mutation-dialogs/update-meta-dialog.tsx
import { useEffect } from "react"
import { useForm } from "react-hook-form"
import { zodResolver } from "@hookform/resolvers/zod"
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Textarea } from "@/components/ui/textarea"
import { Label } from "@/components/ui/label"
import {
  UpdatePartyroomMetaSchema, type UpdatePartyroomMetaRequest,
} from "@/features/partyrooms/model/mutation-schema"
import { useUpdatePartyroomMeta } from "@/features/partyrooms/api/use-update-partyroom-meta"

interface Props {
  partyroomId: number
  currentTitle: string
  currentIntroduction: string | null
  currentPlaybackTimeLimit: number | null
  open: boolean
  onOpenChange: (open: boolean) => void
}

interface FormShape {
  title?: string
  introduction?: string
  playbackTimeLimit?: number | string  // RHF input emits string
}

export function UpdateMetaDialog(props: Props) {
  const { partyroomId, currentTitle, currentIntroduction, currentPlaybackTimeLimit, open, onOpenChange } = props
  const mutation = useUpdatePartyroomMeta()

  const { register, handleSubmit, formState: { errors }, reset } = useForm<FormShape>({
    resolver: zodResolver(UpdatePartyroomMetaSchema),
    defaultValues: { title: undefined, introduction: undefined, playbackTimeLimit: undefined },
  })

  useEffect(() => { if (!open) { mutation.reset(); reset() } }, [open])

  const onSubmit = (data: FormShape) => {
    // empty string → undefined (zod optional 처리)
    const body: UpdatePartyroomMetaRequest = {
      title: data.title?.trim() || undefined,
      introduction: data.introduction?.trim() || undefined,
      playbackTimeLimit: data.playbackTimeLimit ? Number(data.playbackTimeLimit) : undefined,
    }
    mutation.mutate({ partyroomId, body }, { onSuccess: () => onOpenChange(false) })
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <form onSubmit={handleSubmit(onSubmit)}>
          <DialogHeader>
            <DialogTitle>파티룸 메타 수정</DialogTitle>
            <DialogDescription>
              변경할 필드만 입력하세요. 입력하지 않은 필드는 유지됩니다.
            </DialogDescription>
          </DialogHeader>
          <div className="py-4 space-y-3">
            <div className="space-y-1">
              <Label htmlFor="meta-title">제목</Label>
              <Input
                id="meta-title"
                aria-label="제목"
                placeholder={currentTitle}
                maxLength={100}
                {...register("title")}
              />
              {errors.title && <p className="text-sm text-destructive">{errors.title.message}</p>}
            </div>
            <div className="space-y-1">
              <Label htmlFor="meta-intro">소개</Label>
              <Textarea
                id="meta-intro"
                aria-label="소개"
                placeholder={currentIntroduction ?? ""}
                maxLength={500}
                rows={3}
                {...register("introduction")}
              />
              {errors.introduction && <p className="text-sm text-destructive">{errors.introduction.message}</p>}
            </div>
            <div className="space-y-1">
              <Label htmlFor="meta-limit">재생 시간 제한 (분, 1~60)</Label>
              <Input
                id="meta-limit"
                aria-label="재생 시간 제한"
                type="number"
                min={1}
                max={60}
                placeholder={currentPlaybackTimeLimit?.toString() ?? ""}
                {...register("playbackTimeLimit")}
              />
              {errors.playbackTimeLimit && <p className="text-sm text-destructive">{errors.playbackTimeLimit.message}</p>}
            </div>
            {/* refine 에러는 root-level — react-hook-form은 zod refine을 root에 매핑 */}
            {errors.root && <p className="text-sm text-destructive">{errors.root.message}</p>}
            {(errors as any)[""] && <p className="text-sm text-destructive">{(errors as any)[""].message}</p>}
          </div>
          <DialogFooter>
            <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>취소</Button>
            <Button type="submit" disabled={mutation.isPending}>
              {mutation.isPending ? "처리 중..." : "수정"}
            </Button>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

**주의**: zod refine 에러는 RHF에서 root-level로 매핑됨. RHF v7.60에서 refine 에러는 `errors.root` 또는 빈 문자열 키. 테스트가 `findByText(/최소 1개/)`로만 검증하므로 위치 무관.

- [ ] **Step 3: PASS**

Run: `pnpm exec vitest run src/features/partyrooms/ui/mutation-dialogs/__tests__/update-meta-dialog.test.tsx`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/features/partyrooms/ui/mutation-dialogs/update-meta-dialog.tsx src/features/partyrooms/ui/mutation-dialogs/__tests__/update-meta-dialog.test.tsx
git commit -m "feat(partyrooms): add UpdateMetaDialog (title/intro/limit + refine) (G4.5)"
```

### Task G4.6: partyrooms-actions-dropdown에 "메타 수정" 항목 추가

**Files:**
- Modify: `src/features/partyrooms/ui/partyrooms-actions-dropdown.tsx`
- Modify: `src/features/partyrooms/ui/__tests__/partyrooms-actions-dropdown.test.tsx`

- [ ] **Step 1: 테스트에 케이스 추가**

```tsx
it("ACTIVE: 메타 수정 enabled", async () => { ... })
it("TERMINATED: 메타 수정 disabled", async () => { ... })
it("clicking 메타 수정 opens dialog", async () => { ... })
```

- [ ] **Step 2: 구현 — dropdown에 항목 추가 + UpdateMetaDialog mount**

```tsx
const canUpdateMeta = status === "ACTIVE" || status === "SUSPENDED"
// ...
<DropdownMenuItem
  disabled={!canUpdateMeta}
  onSelect={() => canUpdateMeta && setActive("update-meta")}
>
  메타 수정
</DropdownMenuItem>
// ...
<UpdateMetaDialog
  partyroomId={partyroom.partyroomId}
  currentTitle={partyroom.title}
  // 14b AdminPartyroomDetail에는 introduction/playbackTimeLimit 부재 (verified)
  // → placeholder 사용 안 함, dialog는 빈 form으로 시작. 어드민이 새로 타이핑.
  // 백엔드 DTO 확장은 spec §13.2 future polish (AdminPartyroomDetailResponse 노출).
  currentIntroduction={null}
  currentPlaybackTimeLimit={null}
  open={active === "update-meta"}
  onOpenChange={(o) => !o && setActive(null)}
/>
```

**확정 정책**: 14b `AdminPartyroomDetail`에 `introduction`/`playbackTimeLimit` 부재 (`entities/partyroom/model/types.ts` verified). UpdateMetaDialog의 `currentIntroduction`/`currentPlaybackTimeLimit` props는 placeholder 표시용일 뿐 — null이면 placeholder 비움. 어드민은 빈 form에서 새 값을 타이핑. 백엔드 DTO 확장은 spec §13.2 (`AdminPartyroomDetailResponse`에 두 필드 노출) — 14d 이후.

- [ ] **Step 3: PASS + Commit**

```bash
git commit -am "feat(partyrooms): wire UpdateMetaDialog into ActionsDropdown (G4.6)"
```

### Task G4.7: chunk sanity

- [ ] **Step 1: typecheck + 전체 테스트**

Run: `pnpm exec tsc --noEmit && pnpm test:run`
Expected: PASS.

---

## Chunk G5: 파티룸 표시 변경 (display-flag)

**목표**: dropdown에 "표시 변경" 항목 + form 모달 (NORMAL/FEATURED/HIDDEN select).

### Task G5.1: zod DisplayFlag enum (TDD)

**Files:**
- Modify: `src/features/partyrooms/model/mutation-schema.ts`
- Modify: `src/features/partyrooms/model/__tests__/mutation-schema.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
import { UpdateDisplayFlagSchema } from "@/features/partyrooms/model/mutation-schema"

describe("UpdateDisplayFlagSchema", () => {
  it("accepts NORMAL/FEATURED/HIDDEN", () => {
    expect(UpdateDisplayFlagSchema.parse({ flag: "NORMAL" })).toEqual({ flag: "NORMAL" })
    expect(UpdateDisplayFlagSchema.parse({ flag: "FEATURED" })).toEqual({ flag: "FEATURED" })
    expect(UpdateDisplayFlagSchema.parse({ flag: "HIDDEN" })).toEqual({ flag: "HIDDEN" })
  })
  it("rejects unknown", () => {
    expect(() => UpdateDisplayFlagSchema.parse({ flag: "PUBLIC" })).toThrow()
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현**

```ts
export const DisplayFlagEnum = z.enum(["NORMAL", "FEATURED", "HIDDEN"])
export const UpdateDisplayFlagSchema = z.object({ flag: DisplayFlagEnum })
export type UpdateDisplayFlagRequest = z.infer<typeof UpdateDisplayFlagSchema>
```

- [ ] **Step 3: PASS + Commit**

```bash
git commit -am "feat(partyrooms): add UpdateDisplayFlagSchema (G5.1)"
```

### Task G5.2: API fn + hook (병렬 — TDD)

**Files:**
- Modify: `src/features/partyrooms/api/partyrooms-api.ts`
- Create: `src/features/partyrooms/api/use-update-partyroom-display-flag.ts`
- Tests 추가

- [ ] **Step 1: API fn 테스트 + 구현**

테스트:
```ts
describe("updatePartyroomDisplayFlag", () => {
  it("PATCH /admin/partyrooms/:id/display-flag with body, 204", async () => {
    let bodySeen: any
    server.use(
      http.patch("*/api/v1/admin/partyrooms/1/display-flag", async ({ request }) => {
        bodySeen = await request.json()
        return new HttpResponse(null, { status: 204 })
      }),
    )
    await updatePartyroomDisplayFlag(1, { flag: "FEATURED" })
    expect(bodySeen).toEqual({ flag: "FEATURED" })
  })
})
```

구현:
```ts
import type { UpdateDisplayFlagRequest } from "../model/mutation-schema"

export async function updatePartyroomDisplayFlag(
  partyroomId: number, body: UpdateDisplayFlagRequest,
): Promise<void> {
  await http<void>(`/api/v1/admin/partyrooms/${partyroomId}/display-flag`, {
    method: "PATCH", body,
  })
}
```

- [ ] **Step 2: hook 테스트 + 구현**

테스트:
```ts
// src/features/partyrooms/api/__tests__/use-update-partyroom-display-flag.test.ts
import { describe, expect, it, vi } from "vitest"
import { renderHook, waitFor } from "@testing-library/react"
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { http, HttpResponse } from "msw"
import { server } from "@/test/mocks/server"
import { toast } from "sonner"
import { useUpdatePartyroomDisplayFlag } from "@/features/partyrooms/api/use-update-partyroom-display-flag"

function makeWrapper() {
  const qc = new QueryClient({ defaultOptions: { mutations: { retry: false }, queries: { retry: false } } })
  const wrapper = ({ children }: { children: React.ReactNode }) => (
    <QueryClientProvider client={qc}>{children}</QueryClientProvider>
  )
  return { qc, wrapper }
}

describe("useUpdatePartyroomDisplayFlag", () => {
  it("on success: invalidate ['partyrooms'] + toast", async () => {
    const { qc, wrapper } = makeWrapper()
    const invalidateSpy = vi.spyOn(qc, "invalidateQueries")
    const successSpy = vi.spyOn(toast, "success").mockImplementation(() => "")
    server.use(http.patch("*/api/v1/admin/partyrooms/1/display-flag", () => new HttpResponse(null, { status: 204 })))

    const { result } = renderHook(() => useUpdatePartyroomDisplayFlag(), { wrapper })
    result.current.mutate({ partyroomId: 1, body: { flag: "FEATURED" } })
    await waitFor(() => expect(result.current.isSuccess).toBe(true))

    expect(invalidateSpy).toHaveBeenCalledWith({ queryKey: ["partyrooms"] })
    expect(successSpy).toHaveBeenCalledWith("표시 변경 완료")
  })
})
```

구현:
```ts
// src/features/partyrooms/api/use-update-partyroom-display-flag.ts
import { useMutation, useQueryClient } from "@tanstack/react-query"
import { updatePartyroomDisplayFlag } from "./partyrooms-api"
import { mutationSuccessToast, mutationErrorToast } from "@/shared/lib/mutation-toast"
import type { UpdateDisplayFlagRequest } from "../model/mutation-schema"

export function useUpdatePartyroomDisplayFlag() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (vars: { partyroomId: number; body: UpdateDisplayFlagRequest }) =>
      updatePartyroomDisplayFlag(vars.partyroomId, vars.body),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["partyrooms"] })
      mutationSuccessToast("표시 변경 완료")
    },
    onError: mutationErrorToast,
  })
}
```

- [ ] **Step 3: PASS + Commit**

```bash
git commit -am "feat(partyrooms): add updatePartyroomDisplayFlag API + hook (G5.2)"
```

### Task G5.3: display-flag-dialog (TDD)

**Files:**
- Create: `src/features/partyrooms/ui/mutation-dialogs/display-flag-dialog.tsx`
- Test: `__tests__/display-flag-dialog.test.tsx`

- [ ] **Step 1: 실패 테스트**

```tsx
describe("DisplayFlagDialog", () => {
  it("disables submit when current flag selected", () => {
    renderWithClient(<DisplayFlagDialog partyroomId={1} currentFlag="NORMAL" open={true} onOpenChange={vi.fn()} />)
    expect(screen.getByRole("button", { name: /변경/ })).toBeDisabled()
  })

  it("submits when different flag selected", async () => {
    server.use(http.patch("*/api/v1/admin/partyrooms/1/display-flag", () => new HttpResponse(null, { status: 204 })))
    const onOpenChange = vi.fn()
    const user = userEvent.setup({ pointerEventsCheck: 0, delay: null })
    renderWithClient(<DisplayFlagDialog partyroomId={1} currentFlag="NORMAL" open={true} onOpenChange={onOpenChange} />)
    await user.click(screen.getByRole("combobox"))
    await user.click(await screen.findByRole("option", { name: /FEATURED/ }))
    await user.click(screen.getByRole("button", { name: /변경/ }))
    await waitFor(() => expect(onOpenChange).toHaveBeenCalledWith(false))
  })
})
```

Run: FAIL.

- [ ] **Step 2: 구현 (ChangeTierDialog와 동일 패턴 — Select + 동일 값 disable)**

```tsx
import { useState, useEffect } from "react"
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { useUpdatePartyroomDisplayFlag } from "@/features/partyrooms/api/use-update-partyroom-display-flag"

type DisplayFlag = "NORMAL" | "FEATURED" | "HIDDEN"

const FLAG_LABELS: Record<DisplayFlag, string> = {
  NORMAL: "NORMAL (일반)",
  FEATURED: "FEATURED (추천)",
  HIDDEN: "HIDDEN (숨김)",
}

interface Props {
  partyroomId: number
  currentFlag: DisplayFlag
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function DisplayFlagDialog({ partyroomId, currentFlag, open, onOpenChange }: Props) {
  const [selected, setSelected] = useState<DisplayFlag>(currentFlag)
  const mutation = useUpdatePartyroomDisplayFlag()

  useEffect(() => {
    if (!open) { setSelected(currentFlag); mutation.reset() }
  }, [open, currentFlag])

  const isSame = selected === currentFlag
  const isDisabled = isSame || mutation.isPending

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault()
    mutation.mutate(
      { partyroomId, body: { flag: selected } },
      { onSuccess: () => onOpenChange(false) },
    )
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <form onSubmit={handleSubmit}>
          <DialogHeader>
            <DialogTitle>파티룸 표시 변경</DialogTitle>
            <DialogDescription>현재: <strong>{FLAG_LABELS[currentFlag]}</strong></DialogDescription>
          </DialogHeader>
          <div className="py-4">
            <Select value={selected} onValueChange={(v) => setSelected(v as DisplayFlag)}>
              <SelectTrigger aria-label="표시 선택"><SelectValue /></SelectTrigger>
              <SelectContent>
                <SelectItem value="NORMAL">{FLAG_LABELS.NORMAL}</SelectItem>
                <SelectItem value="FEATURED">{FLAG_LABELS.FEATURED}</SelectItem>
                <SelectItem value="HIDDEN">{FLAG_LABELS.HIDDEN}</SelectItem>
              </SelectContent>
            </Select>
            {isSame && <p className="mt-2 text-sm text-muted-foreground">동일한 표시입니다.</p>}
          </div>
          <DialogFooter>
            <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>취소</Button>
            <Button type="submit" disabled={isDisabled}>
              {mutation.isPending ? "처리 중..." : "변경"}
            </Button>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

**중요**: `currentFlag`를 받으려면 14b `AdminPartyroomDetail.displayFlag`가 string인 것을 union으로 cast 필요. props 타입은 `DisplayFlag`(좁은 union)이지만 `partyroom.displayFlag`는 string — dropdown에서 cast 또는 fallback 처리 (G5.4에서).

- [ ] **Step 3: PASS + Commit**

```bash
git commit -am "feat(partyrooms): add DisplayFlagDialog (Select + 동일 값 disable) (G5.3)"
```

### Task G5.4: dropdown에 "표시 변경" 항목 추가

**Files:**
- Modify: `src/features/partyrooms/ui/partyrooms-actions-dropdown.tsx`
- Modify: dropdown 테스트

- [ ] **Step 1: 테스트 케이스 추가**

```tsx
it("ACTIVE: 표시 변경 enabled", async () => { ... })
it("TERMINATED: 표시 변경 disabled", async () => { ... })
it("clicking 표시 변경 opens dialog", async () => { ... })
```

- [ ] **Step 2: 구현**

```tsx
const canUpdateDisplayFlag = status === "ACTIVE" || status === "SUSPENDED"
// ...
<DropdownMenuItem
  disabled={!canUpdateDisplayFlag}
  onSelect={() => canUpdateDisplayFlag && setActive("display-flag")}
>
  표시 변경
</DropdownMenuItem>
// ...
<DisplayFlagDialog
  partyroomId={partyroom.partyroomId}
  currentFlag={(["NORMAL", "FEATURED", "HIDDEN"].includes(partyroom.displayFlag)
    ? partyroom.displayFlag
    : "NORMAL") as "NORMAL" | "FEATURED" | "HIDDEN"}
  open={active === "display-flag"}
  onOpenChange={(o) => !o && setActive(null)}
/>
```

(unknown displayFlag value fallback to NORMAL — 14b §9.1 forward-compat 정책 일관)

- [ ] **Step 3: PASS + Commit**

```bash
git commit -am "feat(partyrooms): wire DisplayFlagDialog into ActionsDropdown (G5.4)"
```

### Task G5.5: chunk sanity

- [ ] **Step 1: typecheck + 전체 테스트 + build sanity**

Run: `pnpm exec tsc --noEmit && pnpm test:run && pnpm build`
Expected: PASS, 0 error.

---

## Chunk G6: spec catch-up

**목표**: G1~G5 진행 중 spec ↔ 실제 코드 불일치 항목을 SHA + 사유 + impact로 §12에 backfill. 14b §15.2 forward-evolution backfill (14b가 14c에서 backfill되어야 한다고 명시한 항목 SHA 기록).

### Task G6.1: §12 Implementation Reality 작성

**Files:**
- Modify: `docs/specs/2026-04-29-admin-pr14c-design.md`

- [ ] **Step 1: chunk 작업 중 reality 항목 수집**

G1~G5 진행 중 발견된 불일치/추가 항목을 정리. 예상 후보 (실제 발견 시 추가):
- shadcn dropdown-menu/tooltip/dialog 추가 시 jsdom polyfill 추가 필요 여부
- RHF zod refine 에러 위치 (root vs ""키)
- 14b `AdminPartyroomDetail`에 `introduction`/`playbackTimeLimit` 필드 부재 여부 (G4.6)
- `displayFlag` string fallback 정책 적용
- 백엔드 validator 변동 (G4.1 sanity grep 결과)
- mutation hook reset 패턴 (모달 close 시)

- [ ] **Step 2: §12 본문 작성**

각 항목을 `[Gn SHA xxxxx]` prefix로 시작하는 번호 항목으로 정리. 14b §14 패턴.

- [ ] **Step 3: §13.2 (Future Polish 14c 신규) 갱신**

새로 발견된 future polish 항목 추가:
- 예: react-query 5.62.12 → 5.100.5 bump (14b §15.2 상속)
- 예: AdminMemberDetailResponse `withdrawn` 필드 추가 (R1 α 잔존)
- 예: G4.1에서 backend가 변경됐다면 그 항목

- [ ] **Step 4: 14b §15.2 backfill 검증**

14b가 14c에서 backfill 약속한 항목 (mutation chunk)이 실제 14c에서 어떻게 다뤄졌는지 14b spec §13.2 / §15.2를 조회 — 본 14c PR에서 처리된 항목은 SHA 기록.

- [ ] **Step 5: Commit**

```bash
git add docs/specs/2026-04-29-admin-pr14c-design.md
git commit -m "docs(spec): PR 14c §12 Implementation Reality + §13.2 Future Polish + 14b §15.2 backfill (G6)"
```

### Task G6.2: 최종 sanity

- [ ] **Step 1: 전체 검증**

Run:
```bash
pnpm exec tsc --noEmit
pnpm test:run
pnpm build
```
Expected: 0 error, 모든 테스트 PASS, 빌드 성공.

- [ ] **Step 2: chunk closing summary commit (선택)**

(없으면 skip)

---

## Closing — Plan 체인 정리

**Total chunks (이 plan 범위)**: G1 (infra) + G2 (member) + G3 (lifecycle) + G4 (updateMeta) + G5 (displayFlag) + G6 (catch-up) = 6 main chunks. spec G0/G0.1/G0.2/G0.3은 이미 commit됨. 각 chunk 진입 시 polish가 필요하면 G2.1 / G3.1 / G4.1 / G5.1 등으로 in-flight 추가 (메모리 PR 시리즈 패턴).

**Total commits 예상**: ~30 (14b 17과 비슷한 규모, mutation 7개 분량 반영).

**Total tests 신규 예상**: ~60 (14b 77 → 14c 약 137).

**브랜치**: `feature/admin-platform-frontend` 그대로. 14d (bulk-action) 또는 14d 이후 mutation은 후속 plan.

---

## 참고 자료

- 14a spec: `docs/specs/2026-04-28-admin-pr14a-design.md`
- 14a plan: `docs/plans/2026-04-28-admin-pr14a.md`
- 14b spec: `docs/specs/2026-04-29-admin-pr14b-design.md`
- 14b plan: `docs/plans/2026-04-29-admin-pr14b.md`
- 14c spec: `docs/specs/2026-04-29-admin-pr14c-design.md` (HEAD `22d0b80`)
- 백엔드 controllers:
  - `pfplay-platform/.../AdminMemberTierCommandController.java`
  - `pfplay-platform/.../AdminMemberWithdrawCommandController.java`
  - `pfplay-platform/.../AdminPartyroomCommandController.java`
- shadcn 컴포넌트 source: https://ui.shadcn.com/docs/components/{dropdown-menu,tooltip,dialog}
